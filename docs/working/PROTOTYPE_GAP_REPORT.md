# Prototype Gap Report

The prototype is useful as visual and workflow reference, not production truth.

| Gap | Impact | Where Seen | Recommended Handling |
|---|---|---|---|
| Most production screens still use mock data | Frontend/backend contract mismatch risk | `sfpcl-lms/src/data/mockData.ts` and module pages not listed as API-backed in `PROTOTYPE_INVENTORY.md` | Continue replacing one vertical slice at a time. Do not add mock fallbacks to screens already wired to backend APIs. |
| Backend implementation is partial | Cannot validate later module business rules server-side yet | Modules beyond completed auth/admin/audit/document/config/communication/dashboard foundations | Each backend slice must start from source docs and tests, then expose only source-backed behavior. |
| Demo role handling | Not production-grade auth/RBAC | `RoleContext`, login/dashboard | Implement auth and permission tests before real access control. |
| Module test coverage is uneven outside completed slices | Low confidence for future mock-to-API replacements | Prototype-only module pages | Add focused frontend/backend tests with each vertical slice before replacing mock behavior. |
| Missing error/loading/empty states | User experience and API readiness risk | Many screens | Include state coverage in frontend/API slices. |
| Financial and compliance rules in UI/mock form | High-risk drift from source docs | Appraisal, sanction, repayment, default | Move to backend/service layer with tests when backend exists. |
| Mock eligibility, loan-limit, appraisal, review, and sanction-submission facts | Credit staff could act on local calculations or states | Appraisal Workbench; Application Detail credit tab | Closed by 006H using Epic 006 APIs, canonical permissions, stored projections/history, and standard stale errors. |
| Prototype MP screen numbering diverges from `screen-spec-member-portal.md` | Risk of wiring a screen against the wrong spec section | `MP22_ProduceSupply.tsx` (spec MP22 = Grievance Detail & Tracking); `MP24_SupportGrievance.tsx` covers spec MP21+MP22 (spec MP24 = Help Guide) | Slices 005FB and 011NA name both the spec IDs and the prototype files explicitly; spec IDs are authoritative. |
| No real end-to-end proof | Structural frontend/API/database issues surface late | Whole staff lifecycle | Partially closed by 002EX with a dev-only Tracer screen backed by real APIs, persistent SQLite smoke evidence, audit/workflow rows, and explicit `tracer.lifecycle.run`; full-fidelity module rules remain in later epics. |
| Missing admin user-management screen | System Administrator had no reachable shell for user role/team/status administration | Settings/admin area | Closed by 002G with `AdminUsers`, real `/api/v1/admin/users/` API calls, `manage_users` navigation/route guard, assignment controls, and backend audit/session-revocation enforcement. |
| Mock dashboard summary and tasks | Staff dashboard could drift from role-based dashboard contract | `Dashboard` | Closed for the staff shell in 003H with `GET /api/v1/dashboard/`. Backend cards remain zero-count shells and `tasks[]` remains empty until downstream application, appraisal, sanction, compliance, treasury, reminder, default, and reporting data exists. |
| Mock notification/profile data | Staff could not see real S04 inbox state or backend identity details | `NotificationsCenter`, `MyProfile` | Closed for staff in 003IA/003IA2 with `GET /api/v1/notifications/`, versioned mark-read persistence/audit, `409 STALE_WRITE` refresh/error handling, and read-only My Profile rendering from `/api/v1/auth/me/`. Borrower portal notifications remain future work. |
| Mock member directory/profile data | Staff member screens could drift from source §13.1/§13.3/S07 and expose mock-only exposure, supply, nominee, loan, communication, audit, or bank rows | `MemberDirectory`, `MemberProfile`, `Borrower360` | Closed for the directory list in 004A, the masked profile shell in 004B, and Borrower 360 in 004K. `Borrower360` now uses Epic 004 member detail/subresource APIs, keeps PAN/Aadhaar masked unless the 004I reveal endpoint returns a temporary value, renders bank/cancelled-cheque metadata masked only, and shows empty states for loan/application/repayment/communication/risk/audit modules until later APIs exist. Member create/update, witness, and loan-intake wiring remain future slices. |
| Mock borrower portal dashboard/profile/supply data | Borrowers could see another mock member's profile, application counts, deficiencies, or produce history instead of their authenticated own-data scope | `MP03_Dashboard`, `MP04_MyProfile`, `MP22_ProduceSupply` | Closed in 005FB with `GET /api/v1/portal/dashboard/`, `/profile/`, and `/produce-supply/`. Portal APIs derive member scope from the active `PortalAccount`, ignore query `member_id` as authority, keep sensitive values masked, and show an empty produce-supply shell until the documented `produce_supply_records` model exists. |
| Mock borrower portal application start/list/status data | Borrowers could start from mock loan facts, see static application rows, or open a hard-coded status record instead of their own backend application lifecycle | `MP05_NewApplication`, `MP09_MyApplications`, `MP10_ApplicationStatus` | Closed in 005G with `GET/POST/PATCH /api/v1/portal/applications/`, detail, and submit endpoints. Portal APIs derive member scope from the active `PortalAccount`, block cross-member attempts, keep submitted applications reference-less until completeness pass, and render returned-incomplete applications as borrower rectification work. Deficiency response/upload/resubmission remains future scope. |
| Task Inbox has no backend source | S03 actionable work queues could be mistaken for implemented scheduler output | `TaskInbox` | Owned by `012EA` (workflow task engine + task APIs) and `012EB` (Task Inbox UI wiring), added 2026-07-07 after an owner audit found S03 had no owning slice. Keep marked as prototype/mock until those slices run. 003J `scheduled_jobs` is internal metadata only; it is not an operational queue screen and does not generate dashboard tasks or notifications. |
| Audit/document UI components still use mock data | Users could mistake visual components for real audit/document evidence | `AuditTimeline`, `DocumentPackModal` | Backend audit/workflow and generic document file APIs exist, but these components remain prototype references until dedicated UI wiring slices connect them to source-backed APIs and permissions. Owning slices: `005I` (ApplicationDetail's AuditTimeline) and `008M` (DocumentationHub's AuditTimeline, DocumentChecklist, DocumentPackModal) — each requires that no mock-data reads remain in its screens. |
| Completeness Workbench still mock-backed despite 005E/005I Complete | Completeness decisions could be simulated client-side, including client-generated loan references | `CompletenessWorkbench.tsx` | Closed by 005E2: the screen now consumes status-filtered application rows, backend checklist/completeness projections, append-only deficiency history, exact mutation APIs, canonical permission authority, and backend-issued references with no mock fallback. |
| App shell seeds workflow state from mock data | Mock applications flow into SanctionWorkbench and look real | `App.tsx` | Closed by 006H5/007I: the shell no longer seeds applications and SanctionWorkbench now loads scoped approval cases, frozen review facts, resource actions, conflicts, meeting evidence, and terminal sanction decisions from Epic 007 APIs. |
| Header search and notification dropdown are mock | Client-side search over sensitive mock data; phantom notifications | `Header.tsx` | Search owned by `010N` (server-side only, no local sensitive index); notification summary and final Header mock removal owned by `010O`, added 2026-07-11. |
| Member create/update, witness UI, produce history, portal deficiency response, portal KYC correction, SettingsHub panels, tracer isolation, field-key separation previously unowned | Source-required behaviour had no queued owner | See PRODUCTION_COMPLETION_BLUEPRINT.md §6.3 | Owned since 2026-07-11 by `006Y`, `006Y2`, `006Z`, `008L2`, `011M2`, `007J2`, `012E2`, `012E3` respectively. |
| Portal login demo fallback bypasses authentication (latent) | `MP00_Login.tsx` fell back to `handleDemoLogin('borrower')` — not gated by `DEMO_AUTH_ENABLED` — when credentials were filled but `onSubmitLogin` was absent; pre-login RoleContext defaulted to a staff fixture user | `MP00_Login.tsx`, `App.tsx`, `RoleContext.tsx` | CLOSED 2026-07-11 by owner fix recorded in `005FA2` (Complete): fallback removed, `onSubmitLogin` required, unauthenticated default user, `clearUser()` on logout, regression tests added. |
| Wired portal New Application computed loan limits client-side | Wrong formula (`shares × valuationPerShare`, missing the 30% factor), hard-coded land limit ₹6,75,000, local `Math.min`, and a local submission gate — in a slice marked Complete (005G) | `MP05_NewApplication.tsx` | Closed in 006Z2: the active `PortalAccount` receives a redacted, provenance-verified server projection from effective policy plus persisted verified share/land facts; MP05 restores the approved three-card composition and server advisory with no local limit arithmetic or fallback. |
| ApprovalPanel hard-codes the approval authority matrix | ₹5,00,000 threshold routing, approver-slot matching, and decision gating are client logic in a reusable component | `components/loan/ApprovalPanel.tsx` | Closed by 007I: authority/approvers come from the frozen case snapshot and controls require an enabled resource action intersected with the matching `/auth/me` permission. |
| Sanction/exception registers and Approval Matrix settings used local application/config fixtures | Readers could see client-derived authority, amounts, outcomes, and exception reasons; admins could believe an inline matrix edit was persisted | `RegistersHub` S23/S25; `SettingsHub` S71 | Closed by 007J with independent scoped register APIs, server pagination/frozen fields, canonical permission gates, and governed successor-version proposals. Export remains an explicit no-request deferred state until 012B/012C; unrelated register tabs remain 012DA and remaining SettingsHub panels remain 007J2. |

## Major Conflict Policy
If prototype behavior conflicts with `docs/source/`, source docs win. Record conflicts in `ASSUMPTIONS.md`, an ADR, or a slice blocker.
