# Assumptions

| ID | Assumption | Why Made | Source Checked | Affected Slice | Risk | Needs Confirmation | Date Added | Resolved | Notes |
|---|---|---|---|---|---|---|---|---|---|
| A-135 | A communication exception is assigned to the retained job actor and may be reviewed/resolved only while that actor still has the source send permission for the job type. Once the retained per-job `max_attempts` is exhausted, the implemented resolution action is `manual_closed`; a requested retry fails closed because the source grants no attempt beyond the configured cap. | Integrations §22.3 requires assigned-owner and resolution facts but does not define an assignment algorithm, resolution vocabulary, or a post-cap retry grant. Retaining the initiating actor avoids inventing another business role, job-type send authority preserves existing scope, and manual closure records operator handling without resetting attempts or fabricating delivery. | `docs/source/integrations.md` §§7.3/10.5-10.6/22.1-22.3/29/33.3; `docs/source/codebase-design.md` §§22.1-22.3/26/34/40.1/42.4; `docs/slices/009H9B-communication-final-attempt-and-exception-queue-closure.md` | 009H9B and future provider-specific retry-policy governance | High | Yes | 2026-07-18 | If governance authorises a new post-cap attempt, create a new governed attempt/job with its own cap and immutable predecessor evidence; never reset or redispatch the exhausted row. |
| A-134 | A claimed communication job has a five-minute durable lease, and each due/recovery scan is bounded to 100 rows. The values are environment-configurable for operations, but changing them never resets attempts, claim history, provider acceptance, or terminal evidence. | Integrations §§7.3/10.2/10.6/22 require bounded asynchronous retry and visible recovery, but do not prescribe a dead-worker timeout or batch size. Five minutes and 100 rows are conservative operational defaults: they avoid promptly duplicating a slow external call while guaranteeing a dead worker is not stranded, and bound each periodic transaction. | `docs/source/integrations.md` §§7.3/10.2-10.6/22/29/33.3; `docs/source/codebase-design.md` §§34/40.1/42.4; `docs/slices/009H8-communications-worker-runtime-and-crash-recovery-closure.md` | 009H8 and future communications operations configuration | High | Yes | 2026-07-18 | If production telemetry or provider SLAs justify different values, configure them prospectively. Never shorten a live claim by rewriting its retained expiry or bypass provider idempotency. |
| A-128 | Disbursement-advice delivery makes at most three worker attempts. Retryable provider timeout/rejection/malformed-result or worker-crash outcomes use exponential backoff of 60, then 120 seconds, capped at 15 minutes for any future larger configured attempt count; exhaustion is terminal `failed` and creates one safe operator task. | Integrations §§10.2/10.5/10.6 and 22 require configured bounded retry with backoff and visible failures but do not prescribe counts or intervals. Three attempts and bounded exponential delay are the conservative industry-standard default: they recover transient failures without creating an unbounded borrower-contact loop. | `docs/source/integrations.md` §§10.2/10.5/10.6/22/33.3; `docs/source/codebase-design.md` §§20.3/20.6/40.1/42.4; `docs/slices/009H5-communications-dispatcher-job-and-dependency-closure.md` | 009H5 and future communications operations configuration | High | Yes | 2026-07-18 | If operations governance supplies provider-specific retry classes/counts/delays, version new jobs prospectively; never reset attempts or redispatch an accepted/finalized job. |
| A-127 | A manually confirmed transfer timestamp may be at most five minutes ahead of application-server time to tolerate ordinary clock skew; it must still be timezone-aware and no earlier than the exact CFC authorisation. | The source requires the transfer date and the sharpened slice rejects times materially in the future, but no numeric tolerance is defined. Five minutes is a conservative industry-standard distributed-system clock-skew allowance and does not permit success before approval. | `docs/source/api-contracts.md` §31.4; `docs/source/integrations.md` §§9.2/9.7/9.10; `docs/slices/009G-utr-capture-and-transfer-success.md` | 009G and downstream advice/register projections | Medium | Yes | 2026-07-17 | If bank/governance defines another clock tolerance, change validation prospectively without rewriting the retained accepted transfer time. |
| A-126 | A governed active SFPCL source-bank decision may exist only after an actor with an explicit unassigned Critical `config.source_bank_account.activate` grant activates the exact verified active SFPCL RBL row with reason/request evidence; no business role receives that grant by default. | Integrations §9.4 requires the source account, but the cited source does not name its provisioner. A narrow explicit-grant mechanism supplies governance without inventing a Treasury/Admin/CFO role assignment; production remains fail-closed until governance makes that grant and records activation. | `docs/source/integrations.md` §§9.1-9.4; `docs/source/data-model.md` §12.3/§19.3-19.4; `docs/source/api-contracts.md` §§31.1-31.2; architecture reviews `2026-07-17_075837_architecture_review`, `2026-07-17_105635_architecture_review`, and `2026-07-17_164724_architecture_review` | 009D, 009E2, 009E3, 009E4, 009F2, and future source-bank administration | High | Yes | 2026-07-16 |  | 009E3 added the canonical unassigned Critical permission, database-required activation proof, predecessor/deactivation ledgers, closed effective ranges, fail-closed history resolution, and PostgreSQL races. 009E4 now retains bounded reviewable rationale plus exact request/author/role/team/network evidence, clears known false approval attribution, and preserves fail-closed tamper/race behavior. The evidence mechanism is complete, but governance must still name the provisioner; raw mutable bank labels remain insufficient. |
| A-125 | Until governance provides an application-scoped authorised-attorney selector, the staff documentation workspace withholds Power of Attorney creation instead of choosing an active Company Secretary or another user by ordering. | S28 requires an authorised person, but the selected source sections and existing identity/security owners provide no governed attorney configuration or selector. Choosing `.first()` would silently invent legal authority. | `docs/source/screen-spec.md` S28; `docs/source/codebase-design.md` §§14-15, 36-37; `docs/slices/008M4-documentation-workspace-deep-module-and-design-closure.md` | 008M4 and future attorney-governance configuration | High | Yes | 2026-07-16 | When governance adds the selector, consume its exact decision from the security owner. Do not infer attorney authority from role membership alone. |
| A-124 | Until the loan-account owner can prove outstanding-loan state, one already-active SAP customer code for the exact member is the conservative reuse authority. A request created before another request completes for that member loses rather than becoming a concurrent reuse; a genuinely later request may bind to the retained code without changing its creator or evidence. | BR-048 ties reuse to an outstanding loan, but Epic 009 has not yet created loan accounts and OC-019 leaves multiple-active-loan semantics open. Inferring outstanding state from names, PAN/Aadhaar, application status, or an invented balance would fabricate financial truth; exact member ownership plus active retained status prevents duplicate customer masters without that inference. | `docs/source/functional-spec.md` BR-048/M07-FR-001/002/007/008 and OC-019; `docs/source/integrations.md` §8.5; `docs/source/data-model.md` §19.2; `docs/slices/009B-sap-customer-code-confirmation-and-reuse.md` | 009B and future loan-account/outstanding-state owner | High | Yes | 2026-07-16 | When a governed outstanding-loan projection exists, require it prospectively at reuse without rewriting retained code/request history. Never match or reuse by identity text. |
| A-123 | Annexure-I uses a bounded dependency-free OOXML renderer for its single fixed worksheet, and its physical bytes are then protected through the shared authenticated field-encryption seam. | The source requires a genuine Excel artifact but names no formatting template or XLSX library; the backend has no maintained XLSX dependency installed. A minimal standards-conformant package avoids adding an unavailable dependency during the isolated run, while the separate encrypted-storage module keeps cryptography in the existing shared owner. | `docs/source/integrations.md` §8.4; `docs/source/functional-spec.md` M07-FR-004/005; `docs/working/DECISION_POLICY.md` §3; `docs/slices/009A-sap-customer-code-request.md` | 009A and 009B Annexure send/download handling | Medium | Yes | 2026-07-16 | If Finance supplies an approved Annexure template or the platform adopts a maintained XLSX library, replace only the renderer behind the same tuple-to-bytes interface; retain encrypted physical storage and programmatic compatibility proof. |
| A-122 | A newly created pre-disbursement loan account uses source status `sanctioned` with `disbursed_amount`, `principal_outstanding`, `interest_outstanding`, `charges_outstanding`, and `total_outstanding` all zero. Successful transfer, not account creation, establishes the funded principal and activates the account. | The source enumerates `sanctioned` as the first account state and M08-FR-008 says activation occurs only after successful disbursement, but §18.1 does not spell out initial balance values. Recording a receivable before funds move would invent financial exposure; zero is the conservative accounting default. | `docs/source/data-model.md` loan-account status vocabulary and §18.1; `docs/source/functional-spec.md` M08-FR-008; `docs/source/api-contracts.md` §§30.1/31 | 009C and later disbursement-success/repayment owners | High | Yes | 2026-07-16 | If accounting governance requires a sanctioned-but-undisbursed memorandum balance, add a separate governed field; do not overload principal outstanding or mark the account active. |
| A-121 | `finance.loan_account.create` remains a seeded but ungranted Critical permission until governance names the role that may create the account. Slice 009C may implement the exact permission-gated mechanism and test an explicit persisted grant, but must not grant it to Credit Manager, Senior Manager Finance, or another role by inference. | Source defines the permission, route, and “sanction decision exists” workflow rule but the canonical role-permission matrix grants this code to no role. Assigning a Critical finance write by workflow proximity would invent approval authority. | `docs/source/auth-permissions.md` §§12.9/15/25.7/34; `docs/source/api-contracts.md` §30.1; `docs/source/data-model.md` §18.1-18.2 | 009C and the future finance-role governance owner | High | Yes | 2026-07-16 | Governance must name the creating role before a seeded production user can exercise the route. Keep object scope, terminal sanction, SAP-code, replay, and maker evidence independent of that future grant. |
| A-120 | 009A rejects a member PAN/Aadhaar source stored as the historical one-way `enc:v1` token because full identity values cannot be recovered for the source-required Annexure I. Only values already held through the shared reversible field-encryption seam may be consumed and re-encrypted into the immutable SAP request; no token, hash, or guessed plaintext is exported. | M07 requires full PAN/Aadhaar in the manual Excel handoff, while A-115 explicitly says historical `enc:v1` identity tokens cannot be upgraded or revealed. Treating their retained last four/hash as the full value would fabricate regulated identity data. | `docs/source/integrations.md` §8.4; `docs/source/functional-spec.md` M07-FR-004/005; `docs/source/data-model.md` §19.1; A-115 | 009A, member identity remediation, and 012E3 | High | Yes | 2026-07-16 | A governed identity remediation must recapture or migrate full values through `shared.encryption`; 009A then works without contract changes. Never log the rejected source token or weaken Annexure completeness. |
| A-118 | The reviewed §27 checklist and §28 security-instrument POST actions retain their resource-specific exact-payload replay/action-identity contracts and do not require an `Idempotency-Key` header. | API §5.3 gives a broad critical-POST recommendation, while §45 explicitly enumerates the actions requiring `Idempotency-Key` and contains only disbursement, repayment, allocation, capitalisation, closure, and recovery actions. Applying the specific enumerated contract avoids inventing a new required header for already-defined legal/security routes. | `docs/source/api-contracts.md` §§5.3, 27-28, and 45; architecture review `2026-07-15_034859_architecture_review` | 008K2, 008K3, and later §27/§28 consumers | Medium | Yes | 2026-07-15 | If the API authority adds §27/§28 actions to §45, version the request boundary and replay ledger; do not silently reuse request ids as idempotency keys. |
| A-117 | Checklist item `remarks` and ordered-approval `comments` are bounded to 4,000 characters; approval comments must be non-empty after trimming while completion remarks may be null. | API §27.3-§27.7 fixes the exact fields and the selected slice requires bounded text, but the source gives no numeric maximum. Four thousand matches the existing legal stamp/notary/signature remark boundary and avoids a second arbitrary legal-text limit. | `docs/source/api-contracts.md` §27.3-§27.7; `docs/slices/008K-final-documentation-approval-sequence.md`; existing legal request contracts | 008K and 008M | Medium | Yes | 2026-07-15 | If governance specifies another bound, version the request contract prospectively; never truncate retained signed comments. |
| A-119 | Portal documentation uploads accept only non-empty PDF/JPEG/PNG files up to 5 MiB, with notes bounded to 4,000 characters. The existing static MP07/MP13 tabs select the member's newest sanctioned application (falling back to an honest no-sanctioned-application state) before calling the application-scoped route. Stable upload action codes reuse the canonical checklist item codes. | Source §26.1 requires bounded secure multipart upload but gives no numeric/type limit; the approved MP07 modal already states PDF/JPG/PNG and 5 MB. The portal tabs have no application route parameter, and checklist codes are the only source-owned stable action vocabulary. These conservative defaults avoid a second business identifier or client-side applicability rule. | `docs/source/api-contracts.md` §§3/5.2/26-28; `docs/source/data-model.md` §16; `docs/source/implementation-roadmap.md` §13/§18.2; `docs/slices/008L-member-portal-documentation-actions.md`; existing MP07/MP13 markup | 008L and 008L2 | High | Yes | 2026-07-15 | If governance expands types/limits, supplies a portal application selector, or defines distinct action codes, version the request/client adapter prospectively; never weaken application scope or infer applicability in the client. |
| A-116 | A successful CDSL BO-account reveal is limited to one response per actor and pledge in the existing five-minute reveal-expiry window. BO accounts are classified High, not Very High, so the central policy records that re-authentication is not required; a repeated request returns `429 RATE_LIMITED` and is denial-audited. | Auth §21 requires one-time or short-lived reveal and says re-authentication may be required for Very High sensitivity, but does not name a numeric BO rate. Aligning one permitted reveal with its five-minute expiry prevents repeated disclosure without inventing a password/OTP contract for a High field. | `docs/source/auth-permissions.md` §§6.5/21.1-21.3; `docs/source/codebase-design.md` §9.4; A-114; `docs/slices/008I4-sensitive-field-encryption-and-cdsl-null-contract-closure.md` | 008I4 and future sensitive-data adapters | High | Yes | 2026-07-15 | If governance defines another rate window or requires BO re-authentication, change the central policy only; preserve denial auditing and never move the decision back into an instrument module. |
| A-115 | New reversible CDSL BO-account values use the source-defined `shared.encryption` owner with pinned AES-256-GCM, random 96-bit nonces, field-bound authenticated metadata, explicit current/previous key versions, and a separately configured lookup-HMAC key. Existing one-way `enc:v1` identity tokens remain unchanged and cannot be upgraded or revealed. | The 008I temporary construction was independently reviewed as non-compliant because it hand-rolled reversible cryptography and derived it from Django `SECRET_KEY`. The selected corrective slice explicitly approves a pinned authenticated-encryption implementation and independently configured keys. | `docs/source/codebase-design.md` §§39.1-39.2; `docs/source/data-model.md` §§17.4/29; `docs/source/deployment-ops.md` §§9.2/10; `docs/slices/008I4-sensitive-field-encryption-and-cdsl-null-contract-closure.md` | 008I4 and 012E3 field-encryption/key-rotation closure | High | No | 2026-07-14 | Resolved by 008I4. The custom reversible production adapter was removed; its decryptor exists only inside the one-time historical data migration. 012E3 must extend this single seam for whole-repository rotation and production secret-store boot enforcement. |
| A-114 | Full CDSL BO-account reveal uses a dedicated `security.cdsl_pledge.reveal` permission and `POST /api/v1/cdsl-share-pledges/{id}/reveal-bo-accounts/` accepting exactly a non-empty bounded `reason`. The permission is granted only to the source-defined Company Secretary CDSL manager; ordinary package/CDSL reads and Compliance preparation remain masked. A permitted response returns both retained BO values once with a short expiry timestamp, while every permitted reveal and denied attempt is separately audited without placing plaintext in the ledger. | Source requires explicit permission, object scope, reason, one-time/short-lived return, and audit for BO-account reveal, but names neither a BO-specific permission nor a CDSL reveal route. Reusing mutation or package-read authority would make sensitive disclosure implicit, while routing through the member PAN/Aadhaar endpoint would give the wrong aggregate and field vocabulary. | `docs/source/auth-permissions.md` §§12.8/21.1-21.3/30; `docs/source/api-contracts.md` §§3/6-8/28.5; `docs/slices/008I-cdsl-pledge-workflow.md` | 008I4 and later security-register/UI consumers | High | Yes | 2026-07-14 | Architecture review `2026-07-14_234031_architecture_review` requires 008I4 to move masking, authority, reason, expiry, rate/re-auth decision, and audit behind `documents.modules.sensitive_data_access`. Governance may rename the permission or route; never grant reveal from read/manage/download permissions or persist plaintext response values. |
| A-113 | Every CDSL pledge row retains `future_shares_pledged_flag=true` as a non-client-editable, database-constrained governance fact and includes it in masked responses and immutable evidence. `evidence_document_id` identifies the retained `DocumentFile` of a same-application, current-renderer legal `LoanDocument` whose type is `cdsl_pledge_evidence`; the relationship grants metadata reference only, never download. | The SOP explicitly says future shares issued to the borrower stand pledged, while API §28.5 omits a request field and data-model §17.4 omits a column. The selected slice requires the obligation to remain explicit rather than silently hard-coded or dropped. A protected constrained field makes the obligation reviewable without widening the exact request shape. The source calls the evidence value a document id but supplies no separate pledge-evidence aggregate, so the established current-renderer file relationship is the narrow legal-owned provenance proof. | Epic 008 digest, V10 p.14 §4.2 and pp.15-16 §4.6; `docs/source/api-contracts.md` §28.5; `docs/source/data-model.md` §17.4; A-106/A-107; `docs/slices/008I-cdsl-pledge-workflow.md` | 008I and later 008K/008M/011I consumers | High | Yes | 2026-07-14 | If governance makes the future-shares obligation configurable or adds a distinct signed pledge-evidence aggregate, version future rows and the legal selector; never rewrite retained accepted evidence or infer file access. |
| A-112 | Active PoAs retained from before 008F2 remain readable terminal history with an explicit `legacy_activation_evidence=true` marker, but PATCH replay conflicts because no truthful durable activation workflow identity or frozen evidence snapshot can be reconstructed. | The source requires terminal activation replay to return the exact durable §6.3 workflow identity and reviewable frozen evidence. Historical active rows predate both facts. Inventing an event id, maker/checker snapshot, or request identity would falsely attribute legal authority; downgrading or rewriting the active row would destroy retained truth. | `docs/source/api-contracts.md` §§6.3/28.3; `docs/source/data-model.md` §§17.1-17.2/34; `docs/slices/008F2-security-instrument-boundary-and-poa-lifecycle-closure.md` | 008F2 and later security-package/closure consumers | High | Yes | 2026-07-14 | A future governed remediation may append verified evidence without rewriting the retained activation. Until then the row stays active/terminal, exposes the legacy marker, and cannot claim a fabricated action identity. |
| A-111 | The current tri-party verification remark is retained as nullable bounded text on `loan_documents`, while every real correction also retains immutable old/new audit, version, and workflow history. The remark is projected separately from checklist-item remarks and never overwrites completion-owned evidence. | API §26.6 requires `remarks`, exact replay/correction must return retained truth, and the slice requires retained history, but data-model §16.3 names current verification status/verifier/time without a remarks column. Retaining the API fact on its owning loan document is the smallest standard mechanism; using checklist remarks would corrupt another owner's evidence, while audit-only storage would make the current response dependent on ledger reconstruction. | `docs/source/api-contracts.md` §26.6; `docs/source/data-model.md` §§16.3-16.5; `docs/slices/008G-tri-party-agreement-workflow.md` | 008G and later 008K/008M document consumers | Medium | Yes | 2026-07-14 | If governance removes remarks from the action or defines a distinct verification aggregate, migrate prospectively without rewriting historical evidence or checklist remarks. |
| A-110 | 008F creates the narrow §28 security-package parent with `poa_required_flag=true` and keeps SH-4, CDSL, blank-cheque, and cancelled-cheque requirement flags false/pending until their named owners refresh those facts. Package status and `security_ready_flag` remain pending/false even after PoA activation. | The selected slice explicitly forbids implementing SH-4, CDSL, cheque, custody, or readiness outcomes, while §17.1 requires non-null package flags. Treating unimplemented instruments as required, complete, or ready would invent applicability/readiness truth. | `docs/source/api-contracts.md` §28.1-§28.6; `docs/source/data-model.md` §17.1; `docs/source/functional-spec.md` M06-FR-006-012/M06-FR-019; `docs/slices/008F-power-of-attorney-workflow.md` | 008F and later 008H/008I/008J/008K security-package owners | High | Yes | 2026-07-14 | Later instrument owners must refresh only their governed flags/outcomes and preserve PoA history. No partial package may claim disbursement readiness. |
| A-109 | Pre-008E2 signature rows retain nullable capture-maker and resolution-workflow attribution rather than receiving inferred identities. Exact replay may return retained legacy evidence without writing, but an unresolved legacy mismatch cannot be resolved and any changed legacy capture is rejected until a governed remediation flow establishes truthful ownership. | Source maker-checker rules require distinct attributable capture/resolution users prospectively, but no truthful source identifies the capture maker or exact resolution workflow event for rows written before those links existed. Inferring either from the current verifier, mutable role, or nearby audit ordering could misattribute legal evidence. | `docs/source/auth-permissions.md` §§18.1-18.2/26.4; `docs/source/data-model.md` §16.6; `docs/source/api-contracts.md` §§6.3/26.7-26.8; `docs/slices/008E2-signature-identity-mismatch-lifecycle-closure.md` | 008E2 and later 008F/008G/008K consumers | High | Yes | 2026-07-14 | A governed remediation may establish missing ownership without rewriting retained signer/resolution facts. Until then downstream consumers must not treat null maker attribution as newly maker-checker-verified evidence. |
| A-108 | Pre-008D2 stamp/notary rows retain nullable preparer identity rather than receiving an inferred maker. Exact replay of a legacy checker row remains readable and zero-write, but any changed checker outcome requires a real retained Compliance preparation by a different user; no remediation or override path is invented. | Source maker-checker rules require attributable distinct users prospectively, but no truthful source identifies the maker for a checker outcome created before preparer storage existed. Backfilling from the current row, current role, or latest audit actor could falsely identify a Company Secretary as Compliance maker or legitimize the adverse-authority defect. | `docs/source/auth-permissions.md` §§18.1-18.2/26.4; `docs/source/data-model.md` §§16.7-16.8; `docs/slices/008D2-stamp-notary-verification-authority-closure.md`; architecture review `2026-07-14_155832_architecture_review` | 008D2 and later 008F/008K consumers | High | Yes | 2026-07-14 | A governed remediation flow may establish a missing preparer with independent evidence; until then downstream consumers may read retained outcome history but must not rewrite or treat null maker attribution as newly verified evidence. |
| A-107 | A signature mismatch resolution evidence id identifies the retained `DocumentFile` linked by a same-application, 008B4 current-renderer `LoanDocument` whose exact type is `bank_verification_letter` or `borrower_declaration`; declaration evidence additionally requires that loan document's exact 008D stamp-duty row to be `adequate`. Bank-letter evidence is not treated as a download, checklist completion, or proof of later execution fields that the current source model does not represent. | API §26.8 calls the value a document id while data-model §16.6 names a protected resolution-document FK and the source gives no separate bank-letter or declaration table. Requiring the current legal loan-document/file relationship, type, application, renderer provenance, and declaration stamp is the narrow existing-owner proof; accepting a free file id would make existence authority, while inventing bank-signature fields or a new evidence aggregate would exceed the slice. | `docs/source/api-contracts.md` §26.7-§26.8; `docs/source/data-model.md` §16.3/§16.6-§16.8; Epic 008 digest V10 p.17 §4.11; `docs/slices/008E-signature-mismatch-workflow.md`; A-106 | 008E and later 008K/008M signed-copy/checklist owners | High | Yes | 2026-07-14 | If governance adds a signed-copy upload/version or bank attestation aggregate, strengthen the single evidence resolver prospectively without rewriting retained mismatch history. Never infer download or checklist completion from the reference. |
| A-106 | Renderer contract `legal-renderer-v1` is current only when its immutable provenance snapshots match both the retained generated `document_id` and its SHA-256 metadata. Pre-contract or mismatched rows remain listable as `legacy_unverified`, conflict on exact replay, and are excluded from checklist/stamp/notary current-output selectors. 008B4 provides no automatic remediation path: a future governed successor flow must preserve any executed, verified, checklist-linked, stamped, or notarised history. | Source requires template/file history and genuine Word/PDF generation but does not name a renderer-contract version, legacy status vocabulary, or remediation operation. A versioned all-or-none binding is the smallest non-destructive mechanism that prevents metadata flags/extensions from upgrading old bytes; refusing remediation avoids inventing overwrite authority. | `docs/source/codebase-design.md` §§14.1/26.1-26.3/27.1/36.1-36.2/42.2; `docs/source/api-contracts.md` §§6-8/26.4-26.5; `docs/source/data-model.md` §§16.1-16.3/34; `docs/source/functional-spec.md` §15.1/M06-FR-013/M06-FR-015; architecture review `2026-07-14_124337_architecture_review` | 008B4 and later checklist/stamp/notary/remediation owners | High | Yes | 2026-07-14 | Governance may version a future renderer contract or authorize a successor/remediation endpoint; never backfill provenance from extension, MIME type, flags, prefix, or unchecked legacy bytes, and never mutate retained provenance. |
| A-105 | Checklist applicability treats frozen `physical` as SH-4-only and frozen `demat` as CDSL-only. A missing mode or repository `mixed` mode is exposed as an explicit blocker on both conditional items rather than interpreted as either or both. Missing/conflicting application-linked signature-mismatch rows and missing frozen subsidiary-route facts likewise remain non-applicable with an explicit source blocker; a persisted true fact alone activates the conditional item. | Source requirements define mutually exclusive physical/demat package rules and require persisted subsidiary/mismatch facts, but do not define `mixed`, absent, or contradictory fact handling. Failing closed with a visible blocker preserves “do not guess” and prevents absence from becoming a client-side business inference. | `docs/source/functional-spec.md` M06-FR-009-017; `docs/source/implementation-roadmap.md` §13.6; `docs/source/api-contracts.md` §27.1; Epic 008 digest; `docs/slices/008C-documentation-checklist-applicability.md` | 008C and later 008E/008H/008I/008K readiness owners | High | Yes | 2026-07-14 | Governance may define mixed-mode package behavior and authoritative correction flows. Until then blockers must remain visible and cannot be counted as completed or disbursement-ready. |
| A-104 | The shared `GET /api/v1/loan-applications/{id}/document-checklist/` route returns the retained 005D application-intake checklist before sanction and the source §27.1 persisted legal checklist after sanction. The legacy POST refresh remains only on its existing `/refresh/` route and cannot create/update the legal checklist. Checklist loan-account and four signature identifiers are database-constrained to null until 009C and 008K install their real protected target models. | 005D already shipped the same GET path for pre-sanction completeness under A-039, while §27.1 assigns it to post-sanction legal documentation. State-discriminated backward compatibility avoids breaking the wired completeness workbench without inventing a second frontend contract. The real loan/signature FK targets do not yet exist, so unconstrained UUIDs would falsely claim referential integrity. | `docs/source/api-contracts.md` §20/§27.1-§27.7; `docs/source/data-model.md` §16.4-§16.6; A-039/A-102; `docs/slices/005D-application-document-checklist.md`; `docs/slices/008C-documentation-checklist-applicability.md` | 008C, 008K, 009C, and 008M frontend wiring | High | Yes | 2026-07-14 | 008K must replace signature null-only constraints with protected signature links; 009C must do the same for loan account. Do not allow the pre-sanction compatibility branch to write legal checklist facts or grant legal-document access. |
| A-103 | Retained DOCX packages are merged in-process and PDF records are rendered in-process with pinned ReportLab plus uharfbuzz text shaping, then independently reopened with pinned pypdf layout extraction before storage. PDF rendering uses a configured Unicode TrueType font (`LEGAL_DOCUMENT_PDF_FONT_PATH`) plus explicitly checked standard host fallbacks; every retained text token must have complete glyph coverage or rendering fails closed. Input/output limits are 8 MiB source, 256 archive entries, 32 MiB expanded total, 8 MiB per entry, 100:1 compression, 4 MiB Word XML, 1 MiB extracted text, 200 placeholders, 16 KiB per replacement, 16 MiB output, and 100 PDF pages. | Source requirements demand genuine readable DOCX/PDF output, Unicode/Indian-currency text, bounded pages, pathological-input rejection, and no network conversion service, but do not choose an implementation or numeric safety limits. The selected in-process adapters are boring, pinned, locally verifiable, and isolated behind the legal renderer interface; conservative limits cover ordinary legal templates while failing compressed/pathological inputs before persistence. | `docs/source/codebase-design.md` §§14.1/18/27.1/36.1/42.2/44.1; `docs/source/implementation-roadmap.md` §13.3; `docs/slices/008B3-document-renderer-and-output-proof-closure.md`; `docs/working/DEPENDENCY_POLICY.md` | 008B3 and deployment configuration | Medium | Yes | 2026-07-14 | Deployment must provide host fonts whose combined coverage includes every retained legal-text token and may set `LEGAL_DOCUMENT_PDF_FONT_PATH`. Any future limit change must retain zero-write bounded-input tests; conversion must remain local and must never use a network service. |
| A-102 | `loan_documents.loan_account_id` remains nullable but is database-constrained to `NULL` until Epic 009 installs the production loan aggregate. The retained column is not linked to the synthetic tracer loan model and cannot accept an unconstrained live UUID. | Data-model §16.3 requires a nullable FK, but the production `loans` aggregate does not yet exist and the only current loan model is explicitly a tracer demonstration. A nullable-only transition preserves the retained table/rows and makes the incomplete relation honest without inventing a target. | `docs/source/data-model.md` §§16.3/34; `docs/source/codebase-design.md` §36.2; `docs/working/CONTEXT.md`; architecture review `2026-07-14_093142_architecture_review`; `docs/slices/008B2-legal-document-generation-boundary-closure.md` | 008B2 and 009C | High | Yes | 2026-07-14 | 009C must replace the nullable-only constraint with a protected nullable FK to its real loan aggregate in one governed migration. Validate any imported non-null legacy reference against the target before accepting it; never link this field to `tracer.LoanAccount`. |
| A-101 | The implemented M05 terminal path does not yet own numeric interest rate, repayment date, penal rate, charges policy, or a distinct dispute-resolution clause; therefore a source-complete 13-field Term Sheet must remain configuration-blocked when an approved template declares those missing fields. A test that directly inserts a fully populated `SanctionDecision` proves renderer capability only, not an end-to-end M05-to-M06 workflow. | A-079 deliberately retained honest null/empty sanction terms because no approved frozen source supplies them. 008B correctly refuses missing required merge facts, but its positive tracer bypasses the real terminal writer and cannot establish M06-FR-013 production readiness. Inventing rates, dates, fees, or legal clauses would violate DECISION_POLICY §1.3. | `docs/source/functional-spec.md` §15.1 and M06-FR-013; `docs/source/api-contracts.md` §§25.8/26.4; Epic 008 digest V10 p.17 §4.9; A-079; architecture review `2026-07-14_093142_architecture_review` | 008B3 and a future source-governed sanction-term/configuration owner | High | Yes | 2026-07-14 | 008B3 must label the real-path blocker and renderer fixture honestly. When governance identifies the authoritative rate/repayment/fee/clause owners and approval semantics, freeze those facts before terminal decision and add a real public-workflow tracer; never derive them from current mutable policy or test-only rows. |
| A-100 | Exact loan-document replay is identified by application, retained immutable template row, and output format and returns the first retained result without new bytes or evidence. Generated legal-package rows start as category `legal`, party required `borrower`, execution `pending`, and verification `pending`. Retained template bodies use exact `{{ field_name }}` placeholders for every declared merge field. The initial Term Sheet merge vocabulary is `borrower_name`, `nominee_name`, `shares_held`, `facility`, `loan_amount`, `loan_purpose`, `interest_rate`, `interest_tenure`, `repayment_date`, `penal_interest_rate`, `charges_and_fees`, `security`, and `dispute_resolution`; optional `witness_name` is supported for agreement templates. Nominee, verified witness, and shareholding values come only from the approval-owned frozen review package, never mutable live rows. Until a distinct configured clause store exists, `dispute_resolution` resolves only from explicitly persisted sanctioned `conditions_precedent` text and is missing/blocked when that text is absent; generation never supplies clause wording. | API §26.4 fixes request/response fields but not replay behavior, category/party defaults, initial state literals, template placeholder syntax, merge-field identifiers, or a separate persisted dispute-clause field. These conservative values preserve the §16.3 lifecycle, require an attributable frozen value for every merge, consume retained approved template bytes, and retain one unambiguous evidence set without inventing later execution/checklist completion or legal wording. | `docs/source/api-contracts.md` §26.4-§26.5; `docs/source/data-model.md` §16.3; `docs/source/functional-spec.md` §15.1; Epic 008 digest V10 p.17 §4.9; `docs/slices/008B-document-generation-shell.md` | 008B and later execution/checklist/template-governance slices | Medium | Yes | 2026-07-14 | If governance adds a dedicated configured dispute-resolution clause or defines regeneration/reason semantics, party/category configuration, placeholder syntax, or different merge identifiers, version future templates and generation identities; never rewrite retained generated documents. |
| A-099 | A template file is referenceable only when its immutable upload ledger labels it `document_category=template_source`, `related_entity_type=global`, and no related entity id; the actor holds the dedicated `documents.template.file_reference` permission. That authority is seeded only to the existing Compliance document-preparation owner and does not imply template read/manage or file download. All four source-defined sensitivity levels remain valid when the file row and upload ledger agree. | 008A2 requires global/template-source provenance, sensitivity validation, and an exact reference permission separate from read/manage/download, but the published upload contract has free-text provenance and the source RBAC catalogue names no atomic template-reference code or finer template sensitivity matrix. These narrow labels and permission avoid treating application/loan files or download capability as generation input without inventing a business mapping. | `docs/source/api-contracts.md` §§3/26.1-26.4; `docs/source/data-model.md` §§16.1-16.3; architecture review `2026-07-14_064206_architecture_review`; `docs/slices/008A2-template-effective-integrity-and-file-reference-boundary.md` | 008A2 and 008B | High | Yes | 2026-07-14 | If governance defines different provenance labels, eligible roles, or a narrower sensitivity matrix, update the single documents-owned reference decision and catalogue grant; never infer reference authority from file ids, metadata reads, or download permission. |
| A-098 | No required template `change_reason` field is added until governance reconciles API design §3 (sensitive actions capture a reason) with §26.3's exact document-template request, which contains no rationale field. Existing audit/version history retains actor, timestamp, status, effective dates, file identity, and old/new facts, but a generated change summary is not represented as a user rationale. | Requiring an uncited request field would break the published §26.3 shape; silently claiming the generated summary is the user's reason would be false. | `docs/source/api-contracts.md` §§3/26.3; `docs/slices/008A-document-template-model-and-versioning.md`; architecture review `2026-07-14_064206_architecture_review` | 008A2 and future template-governance/API revision | Medium | Yes | 2026-07-14 | If governance adds a rationale field or header, make it bounded/required for approved/retired changes and retain it in both audit and version-history evidence. |
| A-097 | Template generation must not implicitly equate source `FPO` with repository member labels `fpc` or `producer_institution`. 008A2 owns one explicit resolver; unresolved non-individual variants fail closed before template selection/generation. | Data-model §16.2 names Individual/FPO, while the implemented member aggregate uses `individual_farmer`/`fpc`/`producer_institution`; guessing equivalence would invent a legal-document variant rule. | `docs/source/data-model.md` §16.2; Epic 008 digest V10 p.29; `sfpcl_credit/members/models.py`; architecture review `2026-07-14_064206_architecture_review` | 008A2 and 008B | High | Yes | 2026-07-14 | Governance should define the canonical template-variant mapping. Retained template rows are not rewritten until that mapping exists. |
| A-096 | S23 formal entry numbers use an immutable `CSR-<UUID>` system-generated value. S25 `financial_impact` freezes the routed reviewed amount involved in the exception (not a client-calculated excess/delta), while unavailable legacy source facts remain null. | S23 requires a system-generated formal number without prescribing a sequence/format; S25 says “amount involved if any” without defining a delta formula. UUID identity avoids a new sequence business rule, and the already frozen reviewed amount is the only governed amount attributable to every exception type. | `docs/source/screen-spec.md` S23/S25; `docs/source/functional-spec.md` M05-FR-006/M05-FR-009; `docs/slices/007Q-register-source-fields-and-visual-evidence-closure.md` | 007Q and downstream register export/report consumers | Medium | Yes | 2026-07-14 | If governance defines a formal register numbering sequence or financial-impact formula, version future rows/exports; never rewrite retained identifiers or amounts. |
| A-095 | Document-template lifecycle uses data-model §16.2 and API §26.3 `approval_status` values `draft`/`approved`/`retired`; no separate activate endpoint or stored `active` status is invented in 008A. S72's “active” wording remains a UI vocabulary conflict, while 008B generation requires an approved template effective on the generation date. | S72 labels template status Draft/active/retired and names Activate, but data-model §16.2 labels the persisted field Draft/approved/retired and API §26.3 defines only collection GET/POST and successor PATCH. Adding an uncited action/state would invent governance. | `docs/source/screen-spec.md` S72; `docs/source/api-contracts.md` §26.3-26.4; `docs/source/data-model.md` §16.2; `docs/working/digests/epic-008-documentation-security-package.md` | 008A-document-template-model-and-versioning; 008B-document-generation-shell; 008M-documentation-hub-frontend-wiring | Medium | Yes | 2026-07-14 | If governance defines approval versus activation as separate stages, add an explicit versioned action/field and maker-checker rules before exposing S72 Activate; do not rewrite retained template or generated-document history. |
| A-070 | Until a persisted service-evidence shape proves dates, continuity, recipient entity, and evidence reference, `employment_or_service_years >= 3` alone must not satisfy BR-006; the active-member module returns `manual_evidence_required` for that route. | BR-006 names three continuous years to SFPCL/subsidiary/step-down entities, but the current profile stores only a numeric summary and the source schema does not define service-event rows. Treating the scalar as complete proof invents missing evidence. | `docs/source/functional-spec.md` BR-006; `docs/source/data-model.md` §10.2/§11.5; `docs/source/codebase-design.md` §10.2; architecture review `2026-07-12_220748_architecture_review` | 006Z5-active-member-evidence-and-verification-governance-closure | High | No | 2026-07-12 | Resolved by 006Z5: `MemberServiceEvidence` persists the conservative dated recipient/evidence facts and the scalar remains summary-only. Missing qualifying evidence still returns `manual_evidence_required`. |
| A-001 | The existing React/Vite app is the prototype visual reference, not production-complete implementation. | The repo currently has only frontend code with mock data. | `docs/source/technical-architecture.md`, `docs/source/api-contracts.md`, `sfpcl-lms/package.json` | Future product slices | Medium | No | 2026-07-01 | Backend/database work should start from source docs. |
| A-002 | Current quality gate starts with `npm run build` because lint/typecheck/unit test scripts are not configured. | `package.json` has only `dev`, `build`, `preview`, and placeholder `test`. | `docs/source/test-plan.md`, `sfpcl-lms/package.json` | Slice 001 | Low | No | 2026-07-01 | Add test infrastructure as a future slice. |
| A-003 | Slice 002A may use the execution environment's installed Django 5.0.6 without editing dependency files. | `.ralph/config.yaml` sets `max_new_dependencies: 0`, and package files require approval. | `.ralph/config.yaml`, `.ralph/permissions.json`, local `django-admin --version` | 002A-backend-scaffold-and-health-endpoint | Medium | Yes | 2026-07-02 | Future backend dependency management should add approved requirements or project packaging in a dedicated slice. |
| A-004 | Slice 002B uses a local HMAC-signed JWT implementation rather than adding PyJWT/DRF Simple JWT. | `.ralph/config.yaml` sets `max_new_dependencies: 0`, package files require approval, and the selected slice still needs verifiable JWT login/refresh/logout behavior. | `docs/source/auth-permissions.md`, `docs/source/technical-architecture.md`, `.ralph/config.yaml`, `.ralph/permissions.json` | 002B-user-model-and-jwt-login-refresh-logout | High | Yes | 2026-07-02 | Resolved by 002B2 on 2026-07-03: token encode/decode now uses PyJWT HS256 and `SFPCL_SECRET_KEY` configuration. |
| A-005 | Prototype permission calls `can('export')`, `can('export_reports')`, `can('view_loans')` were remapped to existing permissions `export_registers` / `view_loan_accounts` during typecheck cleanup. | Those permission names do not exist in the prototype `Permission` union; the real catalogue arrives in slice 002C from `docs/source/auth-permissions.md` §12. | `sfpcl-lms/src/contexts/RoleContext.tsx`, `docs/source/auth-permissions.md` §12 | 002C-role-and-permission-catalogue-seed | Medium | No | 2026-07-02 | Resolved (backend) by 002C on 2026-07-03: `PROTOTYPE_PERMISSION_ALIASES` in `sfpcl_credit/identity/catalogue.py` maps `export`/`export_reports` → `reports.export` and `view_loans` → `finance.loan_account.read` (both seeded, test-verified). Frontend `Permission` union re-wiring to backend codes is deferred to 002D/002E. |
| A-007 | The role→permission seed links only the §15 "Key Permissions" codes that exist in the §12 catalogue. `communications.communication.create`, `grievances.manage`, `credit.appraisal.read`, `credit.loan_limit.read`, and `finance.repayment.read` are referenced in §15 but absent from §12, so they are not seeded. Roles with no source-backed permission detail (`sales_team_user`, `it_head`, and all external/future roles) are seeded with no permission links. A-023 later grants `management_viewer` the source-backed `management_readonly` dashboard scope from §19.1. | DECISION_POLICY §1.3: inventing permission codes or role→permission grants the source docs do not state is forbidden; the mechanism is seeded, the gaps are recorded. | `docs/source/auth-permissions.md` §12, §15.1-15.12, §19.1 | 002C-role-and-permission-catalogue-seed; 003G-dashboard-task-summary-api | Medium | Yes | 2026-07-03 | A future slice (or a source-doc correction) should either add the five missing codes to the §12 catalogue and/or define the permission sets for `sales_team_user` and `it_head`. If source docs add exact dashboard scopes, update A-023. |
| A-006 | Frontend tests run on vitest 3 (not 4) because the machine runs Node 18; react-router-dom 7 also wants Node 20. | vitest 4 requires Node 20+ (`styleText` import fails on Node 18.20.4). | `sfpcl-lms/package.json`, local `node --version` | Quality gates | Low | Yes | 2026-07-02 | Recommend upgrading the machine to Node 20 LTS, then vitest can move to v4. |
| A-008 | `GET /api/v1/auth/me/` uses session-bound access validation: the access token must verify by signature/expiry/type and its `session_id` must reference an active `UserSession` whose user can still authenticate. `auth_service.validate_access_token` remains the low-level stateless decode helper for callers that only need JWT validation. | `auth-permissions.md` says access tokens include `session_id`, session tracking is required, inactive users must be blocked, and the slice requires revoked sessions to be rejected for current-user data. | `docs/source/auth-permissions.md` §5.2-5.3, §7.1-7.3, §8.2; `docs/source/technical-architecture.md` §10.1-10.3 | 002D-current-user-api-with-permissions-and-teams | Medium | No | 2026-07-03 | Resolved by 002D on 2026-07-03: `auth_service.validate_access_session()` and `current_user_payload_for_access_token()` enforce active session + active user for `/auth/me/`; tests cover logout-revoked sessions and suspended users. |
| A-009 | The safe development default for `SFPCL_ALLOWED_HOSTS` is `localhost`, `127.0.0.1`, and Django's `testserver`. | Slice 002D2 says the default should be localhost; adding loopback IP and test client host is the standard Django local/test default and avoids `ALLOWED_HOSTS` failures without using `"*"`. | `docs/slices/002D2-backend-dev-infrastructure.md`, `docs/working/DECISION_POLICY.md` §3 | 002D2-backend-dev-infrastructure | Low | No | 2026-07-03 | Production/staging can override with comma-separated `SFPCL_ALLOWED_HOSTS`; no business rule affected. |
| A-010 | The 002E frontend permission bridge maps only canonical backend permission codes needed by existing prototype route/sidebar/action checks; unmapped canonical permissions do not grant UI access. | The prototype has a smaller `Permission` union than the backend catalogue, and inventing new frontend grants for unmapped codes would create role behavior the source docs do not state. | `docs/working/digests/epic-002-platform-auth.md`, `docs/working/API_CONTRACTS.md`, `sfpcl_credit/identity/catalogue.py`, `sfpcl-lms/src/contexts/RoleContext.tsx` | 002E-protected-frontend-route-shell and future UI wiring slices | Medium | Yes | 2026-07-03 | Future slices that add screens/actions must extend the explicit canonical-to-prototype mapping and add tests for the new permission checks. |
| A-011 | The tracer-only canonical permission is `tracer.lifecycle.run`; it is intentionally not part of the source RBAC catalogue and is granted only in tests/dev smoke setup for the early end-to-end proof. The guarded demo seed isolates this grant on the local/dev-only `local_demo_tracer_user` role so shared source-catalogue roles such as `sales_team_user` stay permission-neutral. | 002EX needs one explicit route/action permission without inventing broad member/finance grants or changing source RBAC rules before real modules arrive. 002K2 corrected the demo seed so the smoke permission does not become a source role grant. | `docs/slices/002EX-early-end-to-end-tracer-bullet.md`, `docs/slices/002K2-demo-tracer-permission-isolation.md`, `docs/source/api-contracts.md` §3, `docs/working/DECISION_POLICY.md` §1.3 | 002EX-early-end-to-end-tracer-bullet, 002EY-e2e-and-visual-regression-harness, 002K2-demo-tracer-permission-isolation | Medium | Yes | 2026-07-04 | Later production slices must replace this tracer-only permission with real module permissions and remove or isolate the dev tracer route before production hardening. |
| A-012 | The E2E dev server may use a `SFPCL_DB_PATH` env override for the sqlite `NAME` (default unchanged: `BASE_DIR/db.sqlite3`). The Playwright harness points it at an isolated `sfpcl_credit/e2e.sqlite3`. | 002EY needs a bindable dev server with a persistent, isolated DB (002D2) without clobbering the local dev DB; env-var config is the boring industry-standard default (DECISION_POLICY §3). | `docs/slices/002EY-e2e-and-visual-regression-harness.md`, `sfpcl_credit/config/settings.py`, `docs/working/DECISION_POLICY.md` §3 | 002EY-e2e-and-visual-regression-harness | Low | No | 2026-07-04 | Production/staging supply their own DB config; no business rule affected. |
| A-013 | The Playwright suite and its committed screenshot baselines are AUTHORED in the AFK run but must be RUN and generated on a networked machine with browsers. This sandbox binds localhost (002EX EPERM resolved) but has no network, so `@playwright/test` + Chromium cannot install and the suite cannot execute here. | Run rules: pin the dependency, write code/tests, note the missing module, finish; the orchestrator/operator installs and runs before the visual-evidence loop is closed. Hard gates never run Playwright (`e2e_tests: optional`; no e2e gate in ralph-validate.sh). | `docs/slices/002EY-e2e-and-visual-regression-harness.md` (reqs 3,4,10,12,13,15), `sfpcl-lms/e2e/README.md`, `.ralph/config.yaml` | 002EY-e2e-and-visual-regression-harness | Medium | Yes | 2026-07-04 | Operator: `cd sfpcl-lms && npm install && npx playwright install chromium && E2E_DJANGO_PYTHON=../.ralph/venv/bin/python npm run e2e -- --update-snapshots`, commit `e2e/*-snapshots/`, then the owner may flip `quality_gates.e2e_tests` from `optional` (protected config) once the suite is stable. |
| A-014 | Admin user-management must block any role or status change that would leave zero active users whose primary role is `system_admin`. The guard applies when changing a user's primary role away from `system_admin` or setting a `system_admin` user's status to anything other than `active`. | Source docs require System Administrator user/role administration and suspended users being denied access, but do not state the exact lock-out rule. Slice 002G requires the default guard to prevent removing or suspending the last active `system_admin`. | `docs/source/auth-permissions.md` §12.1, §15.12; `docs/source/api-contracts.md` §12; `docs/working/digests/epic-002-platform-auth.md` | 002G-admin-user-and-role-management-shell | Medium | No | 2026-07-04 | Implemented as an API validation rule returning `400 VALIDATION_ERROR` with `field_errors.status` or `field_errors.role_code`; future account lifecycle slices may refine this if source docs add a more specific break-glass process. |
| A-015 | Admin user list/detail read is gated by `users.user.read` OR any write user-admin permission (`users.user.create`/`update`/`disable`). §19 maps `/settings/users` to `users.user.read`, but the seeded `system_admin` role (`catalogue.py`) holds create/update/disable and NOT `users.user.read`, so a read-only gate would lock the platform's only seeded user-admin out of the screens it must operate. | Preserve current `system_admin` read access (slice 002G2 req 2/5) without inventing a new `users.user.read` seed grant for `system_admin` — that would be a source-doc change (§15.12 lists System Administrator key permissions without `users.user.read`). Write actions stay gated by their specific §12.1 permission. | `docs/source/auth-permissions.md` §12.1, §15.12, §19; `sfpcl_credit/identity/catalogue.py`; `sfpcl_credit/identity/modules/admin_users.py` | 002G2-admin-user-action-permission-granularity | Medium | No | 2026-07-04 | If source docs later add `users.user.read` to the System Administrator grant (or a dedicated read-only user-admin role), tighten `READ_USERS_PERMISSION_CODES` to `{users.user.read}` and update the seed + tests. |
| A-016 | The 002J API contract harness asserts the `available_actions` object-item target shape only against an internal sample. Current `/api/v1/auth/me/` keeps returning a flat list of canonical permission-code strings that mirrors `permissions`. | The selected slice explicitly says §44's object item shape is the target contract, while verified current `/auth/me/` output is flat strings; changing `/auth/me/` would be a public API change outside this harness slice. | `docs/source/api-contracts.md` §44; `docs/working/API_CONTRACTS.md` current-user response; `docs/slices/002J-api-contract-test-harness.md` | 002J-api-contract-test-harness and future detail endpoint slices | Medium | Yes | 2026-07-04 | Future detail endpoints should return §44 object-shaped `available_actions`; a later reconciliation slice may decide whether `/auth/me/` also moves from flat strings to objects or remains a compatibility permission list. |
| A-017 | The `GET /api/v1/audit-logs/` list orders newest-first by `created_at` with `audit_log_id` as a deterministic secondary tiebreak, uses `page_size` default 20 / max 100 (mirroring the 002G admin-user list), and rejects any query parameter outside `{entity_type, entity_id, actor_user_id, page, page_size}` with `400 VALIDATION_ERROR`. `user_agent` is stored on `AuditLog` but not exposed, matching the §42.1 item shape. | `api-contracts.md` §42.1 fixes the item fields and newest-first intent but is silent on tiebreak, page-size bounds, and unknown-parameter strictness; these are boring industry-standard defaults consistent with the existing admin list and the slice's "unknown filters return 400" rule. | `docs/source/api-contracts.md` §42.1; `docs/slices/003A-audit-log-foundation.md`; `sfpcl_credit/identity/modules/admin_users.py` | 003A-audit-log-foundation | Low | No | 2026-07-04 | If source docs later specify a different audit ordering, page-size ceiling, or additional filters, extend `identity/modules/audit_log.py` and its tests accordingly. |
| A-018 | The `GET /api/v1/workflow-events/` list orders newest-first by `created_at` with `workflow_event_id` as a deterministic secondary tiebreak, uses `page_size` default 20 / max 100, and rejects any query parameter outside `{entity_type, entity_id, page, page_size}` with `400 VALIDATION_ERROR`. `record_workflow_event(...)` accepts `action_code` and `metadata` as explicit boundary facts but does not persist them because `data-model.md` §26.2 defines no action or metadata columns. | `api-contracts.md` §42.2 only names the endpoint and example filters; `data-model.md` §26.2 fixes the canonical table columns. Persisting extra action/metadata would drift from the source schema, while accepting them at the service boundary keeps callers explicit for later workflow slices. | `docs/source/api-contracts.md` §42.2; `docs/source/data-model.md` §26.2; `docs/slices/003B-workflow-event-foundation.md` | 003B-workflow-event-foundation | Low | No | 2026-07-05 | If source docs later add action or metadata columns, add a migration and persist those fields in `sfpcl_credit/workflows/events.py`. |
| A-019 | `POST /api/v1/document-files/` returns the existing standard `200` success envelope, case-normalizes `sensitivity_level`, accepts `document_category` as required free text from the source vocabulary, and records `document_category`/related entity facts in upload audit metadata rather than `document_files` columns. | `api-contracts.md` §26.1 defines the field names and response data but does not specify HTTP status, case behavior, or persistence columns for category/related entity; `data-model.md` §16.1 fixes the metadata table columns and omits category/related entity fields. | `docs/source/api-contracts.md` §26.1; `docs/source/data-model.md` §16.1; `docs/source/technical-architecture.md` §15-16; `docs/source/auth-permissions.md` endpoint map | 003C-document-metadata-and-storage-adapter | Low | No | 2026-07-05 | If source docs later require enumerated categories or category/related-entity persistence, add columns/domain tables in the owning document-workflow slice rather than overloading `document_files`. |
| A-020 | `GET /api/v1/document-files/{document_id}/download/` returns a 15-minute local download descriptor shaped as `/api/v1/local-document-files/{document_id}/download/?expires_at=...` rather than a true external signed URL, and all sensitivity levels use the same `documents.file.download` permission until the source documents define a concrete role/sensitivity matrix. | `api-contracts.md` §26.2 allows either signed URL response or native stream but does not define local-dev URL mechanics or expiry length. `auth-permissions.md` maps the endpoint to `documents.file.download` plus a sensitivity check, but the generic document-file foundation has no implementable object-level matrix yet. | `docs/source/api-contracts.md` §26.2; `docs/source/auth-permissions.md` endpoint map; `docs/source/data-model.md` §16.1; `docs/slices/003D-secure-document-download-with-audit.md` | 003D-secure-document-download-with-audit | Medium | Yes | 2026-07-05 | Replace the local descriptor with real object-store signed URLs when a production storage adapter exists. Add sensitivity-specific object checks when the source matrix is defined; current tests assert the generic permission path and no metadata leakage. |
| A-021 | Activating a draft loan-policy config retires any previously active loan-policy config and sets its `effective_to` to the day before the new config's `effective_from`. New configs must start as `draft`; activation is the only API path to `active`. | `api-contracts.md` §41.1 names an activate endpoint and `data-model.md` §25.1 defines `draft`/`active`/`retired`, but the source does not state exact overlap/retirement semantics. Retiring the prior active config preserves one current policy version without inventing calculation rules. | `docs/source/api-contracts.md` §41.1; `docs/source/data-model.md` §25.1 and §26.3; `docs/source/functional-spec.md` M01-FR-001/M01-FR-002/M01-FR-015 | 003E-versioned-configuration-shell | Medium | Yes | 2026-07-05 | Future policy-governance slices should confirm whether overlapping active policies are ever allowed by loan product/member segment; if so, replace the global retirement rule with a source-backed product/segment scope. |
| A-022 | Content-template metadata APIs use narrow permissions `communications.content_template.read` and `communications.content_template.manage`. The catalogue seeds those codes and grants them to the existing `compliance_team_member` role as the current source-backed Compliance owner; no broad `communications.communication.create`, `documents.template.*`, or config permission grants are reused. | `auth-permissions.md` identifies Content templates as Medium risk owned by Communication / Compliance, but §12 has no exact content-template codes and A-007 already records adjacent communication permission gaps. The slice must still enforce 401/403 and avoid broad communication-send access. | `docs/source/auth-permissions.md` risk table and System Admin constraints; `docs/source/api-contracts.md` §39.1; `docs/source/data-model.md` §24.1; `docs/slices/003F-communication-template-shell.md` | 003F-communication-template-shell | Medium | Yes | 2026-07-05 | If source docs add a dedicated Communication role or exact §12 permission catalogue entries, update the seed and tests to that source language. |
| A-023 | `GET /api/v1/dashboard/` uses the source-backed `management_readonly` scope as its dashboard-read permission and maps supported dashboard shells from the authenticated user's primary role: Credit Manager → `credit_manager`, CFO/Director → `sanction_committee`, Compliance Team Member/Company Secretary/Internal Auditor → `compliance`, Senior Manager Finance/CFC/Accounts Head → `treasury`, and Management Viewer or other dashboard-authorized roles → `management`. The catalogue seeds `management_readonly` for the dashboard role contexts implemented in 003G; it does not reuse report/export permissions or invent `dashboard.read`. 003G2 corrected the seed so `internal_auditor` (mapped to `compliance`) also holds `management_readonly`, closing the gap where its documented mapping returned `403`; a consistency test now asserts every role in `dashboard.services._ROLE_CONTEXTS` holds `management_readonly`. | `auth-permissions.md` §19.1 names `management_readonly` as dashboard/summary access, while the seeded §12 catalogue has report/compliance read permissions but no exact `dashboard.read` code. The API must still enforce 401/403 and return role-context shells without downstream calculations. | `docs/source/auth-permissions.md` §19.1; `docs/source/api-contracts.md` §43.1-§43.4; `docs/source/functional-spec.md` §12.2-§12.6; `docs/slices/003G-dashboard-task-summary-api.md` | 003G-dashboard-task-summary-api | Medium | Yes | 2026-07-05 | If source docs later add exact dashboard permission codes or role-specific dashboard scopes, replace `management_readonly` and update `sfpcl_credit/dashboard/services.py`, seed links, and tests. |
| A-024 | The 003H dashboard UI maps known backend dashboard `cards[].link` URL families to the nearest existing prototype workspace route (`/credit/appraisals` → Appraisal, `/sanctions*` → Sanction, `/documents*` and `/security*` → Documentation, `/finance/disbursements*` → Disbursement, `/reports*` → Reports, etc.). Query-string filters are not applied by the target screens yet; unknown future links remain non-clickable with the existing KPI-card subtitle pattern. | The backend contract returns source-style links, but the current React router uses internal `Page` keys and many filtered destination screens are not implemented yet. This keeps cards reachable where a workspace exists without inventing new screens or filters in a dashboard-wiring slice. | `docs/working/API_CONTRACTS.md` dashboard section; `docs/slices/003H-dashboard-task-ui-wiring.md`; `sfpcl-lms/src/services/navigationPermissions.ts` | 003H-dashboard-task-ui-wiring | Low | Yes | 2026-07-06 | When destination list screens implement source filters, update `pageFromDashboardLink` and tests to carry filter state instead of dropping query parameters. |
| A-025 | Communication send/list APIs use narrow permissions `communications.communication.read` and `communications.communication.send`, seeded to the existing Compliance owner role. Approved template effectiveness is evaluated against the server's current local date, and `merge_data` must exactly match `ContentTemplate.variables_json`: missing and extra keys both return `400 VALIDATION_ERROR` before any `Communication` or `AuditLog` write. | Source §39.2-§39.3 and §24.2 require communication APIs and snapshots, while `auth-permissions.md` still lacks exact §12 communication history/send codes and the source is silent on extra merge keys. Exact merge keys are the deterministic default for auditable rendered snapshots; broad document/config/report permissions are not reused. | `docs/source/api-contracts.md` §39.2-§39.3; `docs/source/data-model.md` §24.2; `docs/source/auth-permissions.md` communication/content-template risk notes; `docs/slices/003I-notification-adapter-shell.md` | 003I-notification-adapter-shell | Medium | Yes | 2026-07-06 | If source docs add a dedicated Communication role, exact §12 permission catalogue entries, timezone/effective-date semantics, or permissive merge-key handling, update the seed, service validation, tests, and this assumption. |
| A-026 | Notification inbox APIs use narrow permission `communications.notification.read`, seeded to active internal roles with existing source-backed permission sets. The deliberately permission-neutral `it_head` and `sales_team_user` roles remain ungranted pending source confirmation. `communications/send` creates inbox notifications only for staff user recipients (`recipient_party_type` `user`, `staff_user`, or `internal_user`) whose `recipient_party_id` matches a backend user; role/team notification creation can be extended when source docs define exact recipient payloads. | `screen-spec.md` S04 requires in-app notifications with read/unread/action state, while §12 permission catalogue and §39 communication APIs do not name a notification permission or recipient payload for roles/teams. A narrow current-user permission and direct-user generation avoid overloading communication history, broad communication-send access, and prior zero-permission demo/source-role invariants. | `docs/source/screen-spec.md` §5.8 and S04; `docs/source/content-spec.md` S04; `docs/source/api-contracts.md` §8.1, §39, §43; `docs/slices/003IA-notifications-center-ui-wiring.md` | 003IA-notifications-center-ui-wiring | Medium | Yes | 2026-07-06 | If source docs add exact notification permission codes, per-user read-state for role/team broadcasts, role/team recipient payloads, snooze/dismiss rules, or richer action authorization, update the model/API/tests and this assumption. |
| A-027 | Background scheduling starts as a database-backed metadata shell (`scheduled_jobs`) and internal service boundary only. 003J does not add Celery/Redis, a worker process, public scheduler endpoints, scheduler administration permissions, dashboard task generation, notification generation, reminder cadence, report generation, or provider calls. Allowed job types are a conservative source-backed set for future report exports, notifications, reminders, interest accrual, DPD calculation, and compliance reminders. | Source roadmap names Redis/Celery for later notifications/jobs/reports and says silent job failures require job run records/alerts, but the slice explicitly forbids adding new scheduler dependencies unless already pinned/importable and forbids inventing reminder/report business rules. A local metadata table gives later slices an auditable foundation without external execution semantics. | `docs/source/implementation-roadmap.md` §10.3, §30.2, §31.1; `docs/source/api-contracts.md` §40.7-§40.8; `docs/slices/003J-background-job-scheduling-foundation.md` | 003J-background-job-scheduling-foundation and future async/reminder/report slices | Medium | Yes | 2026-07-07 | Future slices that add real workers, admin APIs, retry policies, job alerts, report export endpoints, reminder generation, or notification generation must confirm source-backed permissions, audit actions, and business timing/cadence before implementation. |
| A-028 | Future data-import execution requires dedicated import-administration permissions for plan/read, dry-run, commit, rollback/cancel or correction, and sensitive-data reveal/export. 003L does not reuse communication, dashboard, notification, document-download, or report-export permissions for imports, and it does not implement import execution until exact source permission codes are defined. | The migration roadmap and data model require migration batches, audit records, validation, reconciliation, and sensitive-data controls, but the opened source sections do not name exact import administration permission codes. Reusing adjacent permissions would grant high-control migration authority from unrelated capabilities. | `docs/source/implementation-roadmap.md` §26; `docs/source/data-model.md` §31; `docs/source/component-spec.md` permission and audit controls; `docs/source/design-system.md` traceability/masking principles; `docs/slices/003L-data-import-and-migration-planning.md` | 003L-data-import-and-migration-planning and future import implementation slices | High | Yes | 2026-07-07 | A future source update or dedicated permission slice should define canonical import permission codes before any import command, API, worker, or UI is implemented. |
| A-029 | `GET /api/v1/members/` initially accepts the narrow enum values needed by the source examples and S05 directory labels: `member_type` = `individual_farmer`/`fpc`/`producer_institution`, `membership_status` = `active`/`inactive`/`pending_verification`, `kyc_status` = `verified`/`missing`/`rekyc_due`/`pending`, and `default_status` = `no_default`/`existing_default`/`past_default`. Broader lifecycle/status vocabularies are deferred to their owning profile, KYC, active-member, and eligibility slices. | `api-contracts.md` §13.1 names the filters and shows representative values, while `screen-spec.md` S05 gives display labels but no canonical full enum table. The slice must validate filters without inventing eligibility/KYC/default business rules. | `docs/source/api-contracts.md` §13.1; `docs/source/screen-spec.md` S05; `docs/source/data-model.md` §10.1; `docs/slices/004A-member-directory-api-and-ui.md` | 004A-member-directory-api-and-ui and future member/KYC status slices | Medium | Yes | 2026-07-07 | If source docs later define a richer member/KYC/default status catalogue, extend `sfpcl_credit/members/models.py`, `members/services.py`, API contract docs, and frontend filter options with matching tests. |
| A-030 | 004B treats masked member profile detail as readable by actors with `members.member.read` and defers full object-scope narrowing until member ownership/team facts are modeled. Full PAN/Aadhaar reveal remains unimplemented and always returns `can_view_full: false`. | `auth-permissions.md` maps `GET /members/{id}/` to `members.member.read` plus object access and maps reveal to field-specific permissions, but no current member owner/team scope table exists in the implemented schema. | `docs/source/auth-permissions.md` §12.2 and endpoint map; `docs/source/api-contracts.md` §13.3/§13.5; `docs/slices/004B-member-profile-api-and-ui.md` | 004B-member-profile-api-and-ui and future member object-access/reveal slices | Medium | Yes | 2026-07-08 | Add object-level scope enforcement when source-backed member ownership/team facts exist. Implement §13.5 only with reason capture, expiry/no-cache behavior, and audit rows that never store full sensitive values. |
| A-031 | 004D treats legal majority for nominee validation as 18 years, calculated from `date_of_birth` on the server's current date at create time. | Source docs say nominees must not be minors and `api-contracts.md` §14.3 names `NOMINEE_MINOR_NOT_ALLOWED`, but they do not state the exact legal-majority age or jurisdiction-specific calculation date. Age 18 is the standard Indian legal-majority default and keeps the rule explicit without inventing a nominee-count or relationship policy. | `docs/source/api-contracts.md` §14.1-§14.3; `docs/source/data-model.md` §10.4; `docs/source/screen-spec.md` S08; `docs/slices/004D-nominee-validation-and-ui.md` | 004D-nominee-validation-and-ui and future nominee/application submission slices | Medium | Yes | 2026-07-09 | If source docs later define a different nominee majority threshold or require age-at-application to use loan application submission date, update `members.services`, tests, and stored snapshot behavior. |
| A-032 | 004G gates land-holding and crop-plan lists with `members.member.read` and creates with `members.member.update`; no new land/crop permission codes are seeded. | `auth-permissions.md` §12.2 and the endpoint map define member, nominee, shareholding, and active-status permissions, but no land/crop-specific codes. The slice requires using the narrowest source-compatible existing member permission rather than inventing codes. | `docs/source/auth-permissions.md` §12.2 and endpoint map; `docs/source/api-contracts.md` §17.1-§17.2; `docs/slices/004G-landholding-and-crop-plan-records.md` | 004G-landholding-and-crop-plan-records and future land/crop permission hardening slices | Medium | Yes | 2026-07-09 | If source docs add exact land/crop permissions, seed them and update `sfpcl_credit.members.services`, backend tests, frontend route/action checks, and this assumption. |
| A-033 | 004H lets a KYC document verification result update the member KYC profile/member status and sets re-KYC due two years after a verified result; it does not infer disbursement or appraisal blockers. | Source docs define KYC profile status, document verification, and two-year re-KYC, but do not yet define the required-document rollup rule or blocker workflow. | `docs/source/api-contracts.md` §18.1-§18.4; `docs/source/data-model.md` §12.1-§12.2; `docs/source/screen-spec.md` S17 | 004H-kyc-upload-and-verification and future KYC completeness/disbursement slices | High | Yes | 2026-07-09 | Replace this with a source-backed completeness rule when application completeness/disbursement gates define exact PAN/Aadhaar/photo/CKYC requirements. |
| A-034 | 004J gates member bank-account and cancelled-cheque metadata lists with `members.member.read` and creates with `members.member.update`; no new bank-account permission codes are seeded. Full bank-account reveal remains unimplemented and is not granted by PAN/Aadhaar reveal, KYC, document, disbursement, export, or security permissions. | `auth-permissions.md` §12.2 and endpoint map define member read/update plus field-specific PAN/Aadhaar reveal, but no exact bank-account metadata permission. The slice needs a member-profile foundation without broad disbursement/document/security authority. | `docs/source/auth-permissions.md` §12.2, §33, endpoint map; `docs/source/data-model.md` §12.3-§12.4; `docs/slices/004J-bank-account-and-cancelled-cheque-profile-foundation.md` | 004J-bank-account-and-cancelled-cheque-profile-foundation and future bank permission hardening slices | High | Yes | 2026-07-09 | If source docs add exact bank-account metadata read/create/update permissions, seed them and update `sfpcl_credit.members.services`, backend tests, frontend action checks, and this assumption. |
| A-035 | 005A draft loan applications use a stable UUID primary identifier with nullable `application_reference_number`; `LO...` formal sequence generation is deferred until the source-backed reference/completeness slice. Draft `completeness_status` is stored as `not_started`, and draft updates write audit metadata but no workflow event because no source-backed state transition occurs. | The source API/data model shows formal reference numbers and completeness lifecycle for broader loan origination, while the selected slice explicitly narrows scope to draft create/read/update and forbids submit, reference-number generation, completeness, deficiencies, eligibility, and appraisal. | `docs/source/api-contracts.md` §19.2-§19.4; `docs/source/data-model.md` §13.1 and §28; `docs/source/screen-spec.md` S10; `docs/slices/005A-loan-application-draft-create-update.md` | 005A-loan-application-draft-create-update and 005B+ submit/completeness slices | Medium | Yes | 2026-07-09 | 005B confirmed submit keeps `application_reference_number` nullable because the member portal source says the reference number is issued after submitted details/documents are checked. A later reference/completeness slice owns sequence behavior. |
| A-036 | 005B submit enforces only the selected slice's persisted request facts: member, positive requested amount, declared purpose, and purpose category. Nominee and application-document placeholder gates from the broader source submit endpoint are deferred to the document/completeness slices. | `api-contracts.md` §19.5 lists broader gate conditions, but the selected 005B slice explicitly excludes completeness, document checklist verification, deficiencies, and nominee/document requirements unless the source pass puts them in 005B. Implementing them now would require models and business rules owned by later slices. | `docs/source/api-contracts.md` §19.5; `docs/source/auth-permissions.md` §20.1; `docs/source/screen-spec-member-portal.md` MP08; `docs/slices/005B-application-submit-and-status-transition.md` | 005B-application-submit-and-status-transition and future document/completeness slices | Medium | Yes | 2026-07-09 | Future document/completeness slices must add exact nominee and document placeholder validations before appraisal/completeness advancement. |
| A-037 | 005C stores successful reference generation as `application_status = reference_generated` even though the data-model enum table omits that value, because the screen source explicitly says the application status changes to `Reference Generated` when completeness passes and the official `LO...` number is generated. | Source documents conflict: `screen-spec.md` S12 names the output status, while `data-model.md` §28 lists later lifecycle statuses but not `reference_generated`. The slice needs an auditable transition point before appraisal without inventing document/completeness checks. | `docs/source/screen-spec.md` S12; `docs/source/data-model.md` §13.1 and §28; `docs/source/screen-spec-member-portal.md` MP08 | 005C-reference-number-generation-and-loan-request-register and future completeness/appraisal slices | Medium | Yes | 2026-07-09 | Future completeness/appraisal slices should either confirm `reference_generated` as a canonical stored status or map it into `appraisal_in_progress` after the appraisal workbench is implemented. |
| A-038 | 005C2 does not write audit rows for application object-access denials, and treats `credit_manager` role access as a source-backed global override only once an application is in the `credit_assessment` stage. Created/received users remain the implemented Field Officer-style owner facts until assignment queues are modeled. | Existing application audit conventions record successful create/update/submit/reference actions, while no existing authorization-denial audit convention exists for application endpoints. `auth-permissions.md` §19.2 gives Credit Manager access to the Credit Assessment domain, and the current schema represents that domain with `LoanApplication.current_stage = credit_assessment` but has no richer queue or assignment table yet. | `docs/source/auth-permissions.md` §19.2, §34.3, §37.3; `docs/slices/005C2-application-object-access-hardening.md`; `docs/working/digests/epic-005-application-intake.md` | 005C2-application-object-access-hardening and future application assignment/completeness slices | Medium | Yes | 2026-07-09 | Future assignment/queue slices should replace the created/received-only scope with explicit assigned-user/team facts, and add denial audit rows if a project-wide authorization-denial audit convention is introduced. |
| A-039 | 005D implements `POST /api/v1/loan-applications/{loan_application_id}/document-checklist/refresh/` as a derived read operation gated by `applications.loan_application.read`; it does not persist checklist rows, write audit/workflow events, or invent a new checklist-refresh permission. | Source documents name a checklist refresh endpoint but the source-backed permission catalogue provides application read/upload/verify and broad document-checklist permissions without an exact application-checklist refresh mutation rule. A read-derived refresh keeps the endpoint available for clients without creating unsourced business authority or side effects. | `docs/working/digests/epic-005-application-intake.md`; `docs/source/api-contracts.md` §20-§21 as distilled; `docs/source/auth-permissions.md` §12.4 as distilled | 005D-application-document-checklist and future completeness/checklist slices | Medium | Yes | 2026-07-09 | 005E or a later completeness owner should replace this with persisted checklist/evaluation state only when the exact refresh side effects and permission are source-confirmed. |
| A-040 | 005F `return-with-deficiencies` accepts `items[].item_code` selected from current 005E blocking checklist facts, rather than the source §19.7 example's `deficiency_ids`. | The source API example assumes deficiency rows already exist before return, but 005E deliberately did not persist deficiency rows and 005F's scope is to create them from blocking completeness facts. Accepting source-backed item codes prevents arbitrary free-form deficiencies while allowing the return action to create the first deficiency records. | `docs/source/api-contracts.md` §19.7 and §21.1-§21.2; `docs/source/data-model.md` §13.5; `docs/slices/005F-deficiency-creation-and-resolution.md`; `docs/working/digests/epic-005-application-intake.md` | 005F-deficiency-creation-and-resolution and future portal deficiency response slices | Medium | Yes | 2026-07-09 | If a future source update requires a separate "draft deficiencies" creation step before return, add that endpoint and adapt `return-with-deficiencies` to accept persisted `deficiency_ids` while keeping item-code validation at creation time. |
| A-041 | 005F2 blocks additional `return-with-deficiencies` attempts once an application is already `application_status = incomplete_returned`. | The opened source docs define the first deficiency return to incomplete state and borrower resubmission, but do not define repeat staff returns before resubmission. Blocking repeats avoids duplicate open deficiency rows and duplicate borrower communications without inventing a repeat-return business rule. | `docs/source/data-model.md` `loan_application_status`; `docs/source/functional-spec.md` M03 deficiency flow; `docs/source/screen-spec.md` S12; `docs/slices/005F2-deficiency-return-status-contract-hardening.md` | 005F2-deficiency-return-status-contract-hardening and future portal deficiency response/resubmission slices | Medium | Yes | 2026-07-09 | Future borrower resubmission work should define the source-backed transition from `incomplete_returned` back to submitted/completeness review before any later repeat-return behavior is allowed. |
| A-042 | 005FA generates portal activation/password-reset OTP challenges server-side, stores only OTP hashes, and records a pending communication-shell row without calling a real SMS/email provider. Tests use an explicit `PORTAL_AUTH_TEST_OTP` setting to simulate receipt. PAN/Aadhaar last-four verification currently compares against the stored encrypted-value placeholder strings because no decrypt/search adapter exists yet. | MP01/MP02 require OTP, but source docs do not specify a delivery provider and this sandbox has no network. Security docs forbid storing raw OTPs and sensitive values. The existing 003I communication shell is the source-compliant boundary until a provider adapter exists. | `docs/source/screen-spec-member-portal.md` MP01/MP02; `docs/source/security-privacy.md` §10 and sensitive-data list; `docs/source/auth-permissions.md` §5; `docs/slices/005FA-member-portal-authentication.md` | 005FA-member-portal-authentication and future notification/provider/security-hardening slices | High | Yes | 2026-07-09 | Replace placeholder last-four checks with a proper decrypt/hash-verification adapter when sensitive-value services exist; wire OTP delivery to the real notification provider when selected. |
| A-043 | Resolved by 006Z: `/api/v1/portal/produce-supply/` reads persisted `produce_supply_records` scoped exclusively through the active PortalAccount. | `data-model.md` §11.6 now has a corresponding non-destructive members migration and verified supply records feed active-member eligibility. | `docs/source/data-model.md` §11.6; `docs/source/functional-spec.md` BR-004/M02-FR-004; `docs/slices/006Z-produce-supply-history-persistence.md` | 006Z-produce-supply-history-persistence | Medium | No | 2026-07-12 | Portal and staff views now show the same persisted truth; no mock or crop-plan derivation remains. |
| A-044 | 005G2 returns `401 INVALID_TOKEN` for old-token portal dashboard/profile/supply/application/password-change calls after the linked `PortalAccount` becomes suspended or inactive, rather than a portal-route-level `403`, because those routes use the shared session-bound bearer validator before portal scope is resolved. The validator revokes the session with reason `portal_account_status_changed`. | The slice allowed choosing exact route-level status by existing auth helper semantics, and the existing route stack authenticates the bearer session before route-specific permission checks. A single invalid-session boundary avoids exposing stale portal claims or own-data permissions. | `docs/slices/005G2-member-portal-session-and-audit-contract-hardening.md`; `docs/working/digests/epic-005-application-intake.md`; `docs/working/API_CONTRACTS.md` | 005G2-member-portal-session-and-audit-contract-hardening and future portal/staff route slices | Medium | No | 2026-07-10 | Implemented and tested for `/auth/me`, password change, portal own-data, and portal application routes. Staff/non-portal valid sessions still receive `403 PERMISSION_DENIED` on portal routes. |
| A-045 | 005H rejection-note creation keeps `LoanApplication.application_status` unchanged as `submitted` and stores only `RejectionNote.note_status = draft/sent`; it does not invent a generic intake rejection status. | `screen-spec.md` S14 says the application status is updated after rejection-note output, but the implemented/source-backed application status vocabulary currently has `draft`, `submitted`, `incomplete_returned`, and `reference_generated` for this intake stage, while data-model rejection statuses are tied to later credit-assessment/sanction concepts. | `docs/source/screen-spec.md` S14; `docs/source/data-model.md` §13.6 and loan-application status vocabulary; `docs/source/api-contracts.md` §21.3-§21.4; `docs/slices/005H-rejection-note-shell.md` | 005H-rejection-note-shell and future eligibility/appraisal/sanction rejection slices | Medium | Yes | 2026-07-10 | Future appraisal/rejection slices should define the source-backed stored rejected status and then decide whether rejection-note creation, approval, or send performs that transition. |
| A-046 | 006A returns `member_active_check = manual_evidence_required` when a member is active but no source-backed continuous produce/service history exists; it does not infer BR-004 through BR-007 from membership status alone. Existing verified `Member.active_member_status` facts can still produce `pass` or `relaxation`. | BR-004 through BR-007 require produce/service history or relaxation evidence, but current persistence has only member/profile summary fields and no source-backed history rows for continuous-year calculation. Inventing a supply-history calculation would create a business rule the source documents do not state. | `docs/source/functional-spec.md` BR-004 through BR-007; `docs/source/data-model.md` §14.1; `docs/working/digests/epic-006-eligibility-loan-limit-appraisal.md` | 006A-active-member-eligibility-service and future active-member/supply-history slices | Medium | Yes | 2026-07-10 | Add source-backed produce/service history models and replace this manual-evidence result when the active-member history slice is implemented. |
| A-047 | 006C treats exactly one active, effective, Board-referenced `LoanPolicyConfig` as the explicit operative loan-limit configuration. A configured percentage produces `valuation_per_share × percentage`; a configured per-share cap is the per-share ceiling; when both are present the lower per-share value applies. No applicable policy, multiple applicable policies, or neither positive share rule blocks calculation without success evidence. | The source requires percentage or per-share-cap configuration and says the API must expose the exact rule/version/source, but it does not spell out the interaction when both fields are populated. Treating the cap as a ceiling is the standard meaning of cap, reproduces the source §23.1 example (`10% of ₹2,000 = ₹200` with a ₹200 cap), and avoids choosing 30%, 10%, or ₹200 in code. | `docs/source/api-contracts.md` §23.1-§23.3; `docs/source/data-model.md` §14.2/§35.1; `docs/source/functional-spec.md` BR-019 through BR-025 and M01-FR-005/M01-FR-006 | 006C-loan-limit-configuration-and-calculator and future policy-governance slices | High | Yes | 2026-07-10 | The values remain versioned configuration, never hard-coded. If the client confirms mutually exclusive percentage/cap modes or another interaction, add an explicit rule-mode field and migrate calculation tests without rewriting historical assessment snapshots. |
| A-048 | 006D's policy-source snapshot columns allow null/blank only for database compatibility with assessment rows created before the migration. Every successful post-006D calculate/rerun stores policy config UUID, name, and Board reference; a legacy row is never enriched by reading mutable policy configuration during GET. | Existing 006C rows did not retain enough data to reconstruct policy source immutably. Backfilling from current policy rows could attach changed or ambiguous metadata and would violate historical integrity. | `docs/source/api-contracts.md` §23.3; `docs/source/data-model.md` §14.2, §35.1, §36.1; `docs/source/test-plan.md` MOD-LIMIT-009/MOD-LIMIT-010 | 006D-loan-limit-snapshot-storage | Medium | No | 2026-07-10 | If a deployed environment contains pre-006D assessments, an authorized successful rerun is the safe path to populate the new snapshot fields. GET returns null policy-source values for untouched legacy rows rather than consulting mutable config. |
| A-049 | Until the source defines how selected `LandHolding.area_acres`, `CropPlan.planned_area_acres`, and nullable profile `land_area_under_cultivation_acres` are reconciled for BR-020, 006C2 will block calculation when the applicable verified acreage facts disagree; it will not invent a min/max/priority formula. | Functional BR-020 requires acreage under cultivation, while the API/data model expose three related acreage facts without a precedence or reconciliation rule. Current 006C sums selected owned acreage and can overstate the land limit when cultivation evidence is lower. | `docs/source/functional-spec.md` BR-020/BR-022; `docs/source/api-contracts.md` §23.1-§23.3; `docs/source/data-model.md` §10.2, §11.7-§11.8, §14.2, §35.1; architecture review `2026-07-10_092630_architecture_review` | 006C2-cultivated-acreage-source-hardening and future policy confirmation | High | Yes | 2026-07-10 | Safe interim behavior is a structured validation blocker with no assessment/audit/workflow mutation. If the client confirms an authoritative source or formula, replace the equality guard, version the rule, and preserve existing snapshots. |
| A-050 | Application Detail composes API checklist rows with the existing card, badge, grid, tab, and empty-state patterns instead of reusing `DocumentChecklist`, because that prototype component imports `mockData` and synthesizes borrower/nominee/witness identifiers, documentation execution, security, and readiness facts. | 005I4 requires the real application detail to display API checklist rows directly and forbids synthetic lifecycle/readiness facts; reusing the existing mock-backed component would violate the slice and expose invented values. | `docs/slices/005I4-application-detail-backend-state-hardening.md`; `docs/working/FRONTEND_DESIGN_RULES.md`; `sfpcl-lms/src/components/loan/DocumentChecklist.tsx` | 005I4-application-detail-backend-state-hardening and future documentation UI wiring | Medium | No | 2026-07-10 | The composition reuses only approved prototype classes/components and introduces no new colours, typography, card, badge, table, or layout system. Replace it with the owning future API-backed documentation component when that workflow lands. |
| A-051 | `LoanAppraisalNote` stores UUID snapshots of the eligibility and loan-limit assessments used at creation, rather than foreign keys to their concrete Django models. | The slice requires appraisal to consume and preserve prerequisite IDs through projection-only module seams, while the source §14.4 table omits explicit prerequisite columns and 006D3 prohibits coupling appraisal to concrete assessment ownership. UUID snapshots preserve traceability and historical locality without allowing appraisal to navigate or mutate assessment rows. | `docs/source/data-model.md` §14.3-§14.4; `docs/source/codebase-design.md` §12.3; `docs/slices/006E-appraisal-note-create-edit-submit.md`; Epic 006 digest | 006E-appraisal-note-create-edit-submit and future appraisal review/reporting slices | Medium | Yes | 2026-07-10 | If source governance later requires formal FK constraints, add them behind the credit module while keeping projection-only callers and immutable historical response behavior. |
| A-052 | Supersedes A-051's claim that assessment UUIDs alone preserve historical locality: explicit successful assessment reruns retain the UUID while replacing current facts, so 006E2 will keep the IDs only as provenance and persist immutable canonical projection copies on the appraisal. | Architecture review proved a same-UUID rerun can replace cultivated acreage, limits, policy, eligibility, and exception facts after appraisal creation. API design §3 requires decision snapshots; no FK to a mutable current assessment can provide that evidence. | `docs/adr/ADR-0003-freeze-appraisal-prerequisite-projections.md`; `docs/source/api-contracts.md` §3/§23-§24; architecture review `2026-07-10_173305_architecture_review` | 006E2-appraisal-source-contract-and-snapshot-hardening and later review/sanction/reporting slices | High | Yes | 2026-07-10 | Implement the additive immutable snapshots and safe legacy provenance rules before 006F. Do not alter the already reviewed explicit assessment-rerun contract. |
| A-053 | M04-FR-001/M04-FR-002 appraisal task creation/role assignment remain intentionally deferred to the owner-planned generic task engine in 012EA; `prepared_by_user` is an action fact, not a substitute for assignment. | The current schema has no persisted workflow task/assignment resource, and 005I5 explicitly prohibits relabelling receiver/creator as assignment. 012EA already owns persisted task generation after workflow states exist. | `docs/source/functional-spec.md` M04-FR-001/M04-FR-002; `docs/slices/012EA-workflow-task-engine-and-task-inbox-apis.md`; architecture review `2026-07-10_173305_architecture_review` | 012EA-workflow-task-engine-and-task-inbox-apis | High | Yes | 2026-07-10 | Epic 006 must not claim these IDs complete before 012EA creates one idempotent Deputy Manager – Finance appraisal task after application-number generation and backfills existing applications. |
| A-054 | Until source governance selects between application receipt and completeness confirmation for M04-FR-003, the current appraisal TAT anchor remains `LoanApplication.created_at`, the timestamp used by 006E as the available application-receipt proxy. | Functional M04-FR-003 says receipt / completeness confirmation, data-model §14.4 says application receipt + 2 days, and the current model has no completeness-confirmed timestamp. Choosing a different inferred timestamp would invent a rule. | `docs/source/functional-spec.md` M04-FR-003; `docs/source/data-model.md` §14.4; `docs/slices/006E-appraisal-note-create-edit-submit.md`; architecture review `2026-07-10_173305_architecture_review` | 006E2 and future workflow-task/SLA governance | High | Yes | 2026-07-10 | Keep the anchor explicit in API/contracts and task due-date generation. If the owner confirms completeness confirmation, add a persisted confirmation timestamp and version the SLA rule without rewriting historical due dates. |
| A-055 | Authoritative loan-limit concurrency tests run only through `sfpcl_credit.config.postgres_test_settings`; the default SQLite suite explicitly skips them because SQLite does not implement `SELECT ... FOR UPDATE`. PostgreSQL connection values are environment supplied, with conventional local defaults. | Codebase-design §26.3 requires concurrency tests for financial modules, while the existing local settings are SQLite-only and the source requires PostgreSQL for production. Treating SQLite success as row-lock evidence would be false assurance. | `docs/source/codebase-design.md` §22.3/§26.3; `docs/source/data-model.md` §34; `docs/slices/006D2C-loan-limit-concurrency-and-boundary-regression.md`; `docs/working/DEPENDENCY_POLICY.md` | 006D2C and every future change to loan-limit locking | High | Yes | 2026-07-10 | Independent validation must install pinned `psycopg[binary]`, provision PostgreSQL, and run `manage.py test sfpcl_credit.tests.test_credit_modules.LoanLimitConcurrencyTests --settings=sfpcl_credit.config.postgres_test_settings -v 2`; a SQLite skip is never acceptance evidence. |
| A-056 | Ambiguous legacy appraisal repair uses `POST /api/v1/appraisal-notes/{id}/revalidate-prerequisites/` with an empty body, appraisal-update plus risk-management permission, object scope, and draft-only state. | ADR-0003 and 006E2 require explicit authorised revalidation but the source API document does not name its path/body. A dedicated action endpoint matches API §3, avoids overloading PATCH, and changes only frozen prerequisite facts. | `docs/source/api-contracts.md` §3/§24; `docs/adr/ADR-0003-freeze-appraisal-prerequisite-projections.md`; `docs/slices/006E2-appraisal-source-contract-and-snapshot-hardening.md` | 006E2 and later appraisal review/sanction slices | High | Yes | 2026-07-10 | If governance names a different repair action, version/alias the route while retaining draft-only authorisation, immutable caller-authored facts, and metadata-only evidence. |
| A-057 | A rejected Credit Manager review extends the §24.4 request with top-level `rejection_reason_category`, `detailed_reason`, `reapply_allowed_flag`, and `communication_mode`; `rejection_stage` is fixed by the workflow to `credit_assessment`. These extra fields are required only for `decision = rejected`, while `reviewed` and `returned` continue to accept exactly `decision` and `review_comments`. | M04-FR-011 requires the review action to generate the existing rejection note, but §24.4 names only decision/comments while §21.3 and data-model §13.6 require category, detailed reason, reapply flag, and communication mode. Requiring the source fields avoids inventing defaults; fixing the stage is an invariant of the appraisal workflow rather than caller choice. | `docs/source/functional-spec.md` M04-FR-011; `docs/source/api-contracts.md` §21.3/§24.4; `docs/source/data-model.md` §13.6; `docs/slices/006F2-credit-manager-appraisal-rejection.md` | 006F2 and later rejection/sanction slices | High | Yes | 2026-07-10 | If source governance defines a nested request or different review fields, version the adapter while retaining the public appraisal/rejection-note module seams and existing stored note contract. |
| A-058 | Appraisal responses expose chronological `review_history[]`; new rows use `history_provenance = native`, while the single reconstructable migration backfill uses `legacy_latest_only`. Reversing migration 0005 removes the derived history table but preserves conservative `legacy_unverified` provenance rather than recreating a known false verification. | ADR-0004 requires append-only appraisal-owned review decisions and an explicitly labelled latest-only legacy backfill, but the source API/schema does not name the response field, provenance enum values, or unsafe reverse-label behavior. These explicit names keep latest projections backward compatible and make incomplete legacy evidence visible. | `docs/adr/ADR-0003-freeze-appraisal-prerequisite-projections.md`; `docs/adr/ADR-0004-append-only-appraisal-review-decisions.md`; `docs/source/api-contracts.md` §3/§24.4; `docs/source/data-model.md` §14.4/§34 | 006E3 and later appraisal/sanction/reporting slices | High | Yes | 2026-07-10 | If governance standardises different history/provenance names, version the response adapter without rewriting stored decisions or upgrading unproven prerequisite provenance. |
| A-059 | The 006G `ApprovalCase` is a unique pending sanction-submission shell linked one-to-one to its application and appraisal; approval-matrix rule, required/excluded approver snapshots, decision actions, and exception records remain unset/unmodelled until Epic 007 enriches that same row. | Source data-model §15.3 expects a matrix rule and approver snapshots, while API §24.5 permits an exception route to be created or flagged. The selected slice explicitly excludes matrix evaluation/assignment and requires the smallest case persistence, so fabricating approvers or a matrix rule would invent governance facts. | `docs/source/api-contracts.md` §24.5/§25.1-§25.2; `docs/source/data-model.md` §15.2-§15.4; `docs/source/implementation-roadmap.md` §12; `docs/slices/006G-submit-to-sanction.md` | 006G and 007A/007B | High | Yes | 2026-07-10 | 007A defines effective rules; 007B must enrich the existing unique 006G case rather than create a second case. |
| A-060 | Sanction submission changes `application_status` and `appraisal_status` to `submitted_to_sanction_committee` but leaves `LoanApplication.current_stage = credit_assessment` until source governance names the corresponding one-of-six SOP stage value. | Source data-model and auth workflow tables name the submitted status but do not name a separate `current_stage` value for sanction; inventing `sanction`, `approval`, or another stage would create a new persisted vocabulary. | `docs/source/data-model.md` loan-application status/§13.1; `docs/source/auth-permissions.md` §20.1; `docs/source/api-contracts.md` §24.5 | 006G and Epic 007 | Medium | Yes | 2026-07-10 | Epic 007 should set a stage only if a source-backed canonical value is added; historical status facts must not be rewritten. |
| A-061 | Explicitly revalidating a non-draft `legacy_unverified` appraisal must never make an old Credit Manager decision approve newly pinned prerequisite facts. A `review_pending` row may remain pending after revalidation because no terminal review exists; a `reviewed` row must return to draft, clear only its latest-review projection, preserve immutable review history, and require maker resubmission plus a fresh independent review. Rejected/submitted terminal rows remain quarantined until a separately governed repair exists. | ADR-0003 requires conservative provenance and fresh frozen facts, while migration 0005 can downgrade every state but the current repair is draft-only. Preserving a reviewed status after changing its evidence would silently transfer authority to facts the reviewer never saw; reopening is the conservative workflow default. The source does not define a legacy-remediation state machine, so the exception is explicit and reviewable. | `docs/adr/ADR-0003-freeze-appraisal-prerequisite-projections.md`; `docs/adr/ADR-0004-append-only-appraisal-review-decisions.md`; `docs/source/api-contracts.md` §3/§24; `docs/source/data-model.md` §14.4/§34; architecture review `2026-07-10_213352_architecture_review` | 006E4-legacy-appraisal-remediation-and-history-backfill | High | Yes | 2026-07-10 | If governance defines another remediation flow, version the explicit action. Never silently relabel unproven facts, erase immutable review history, or let a pre-remediation review satisfy sanction gating. |
| A-062 | Witness list/create uses `GET/POST /api/v1/loan-applications/{id}/witnesses/` with narrow `members.witness.read/create` permissions. Compliance Team Member and Company Secretary receive both; Credit Manager and Internal Auditor receive read only. A qualifying witness must match a member name, have verified member KYC, and hold at least one active shareholding with positive shares. | Source schema and S09 make witnesses application-scoped; the permission naming convention and stage matrix state the actions/roles, but the permission catalogue and endpoint map omit exact codes/path, and the sources do not define how persisted shareholding status/count qualifies a shareholder. The nested path and conservative active-positive test are standard defaults that prevent caller-forged verification. | `docs/source/data-model.md` §10.5; `docs/source/screen-spec.md` S09; `docs/source/auth-permissions.md` §11, §12.2, §15.4, §26.4, §34; `docs/source/api-contracts.md` §7.2 | 004E-witness-shareholder-validation and future documentation workflow slices | Medium | Yes | 2026-07-10 | If governance adds canonical witness permission codes, endpoint shape, name-matching rules, or shareholder qualification, version the adapter and seed while preserving application scope and persisted verification evidence. |
| A-063 | Legacy witness evidence is backfilled only when one `applications.witness.created` audit folio resolves to exactly one shareholding owned by the witness member; all other legacy provenance remains nullable. | 004E2 forbids guessing and requires audit/member/shareholding facts, but the source does not define a historical matching algorithm. Exact folio plus member ownership is the narrowest conservative match and does not reinterpret later status/count. | `docs/source/data-model.md` §10.5, §29-§30; `docs/slices/004E2-witness-evidence-snapshot-and-input-hardening.md`; Epic 004 digest | 004E2-witness-evidence-snapshot-and-input-hardening | Medium | Yes | 2026-07-11 | If authoritative legacy import identifiers become available, a later remediation migration may fill only currently-null evidence; never rewrite already-snapshotted witness evidence. |
| A-064 | Repair run `2026-07-11_215244_repair` proceeds as a fresh 005E4 corrective execution because no prior failed run, `failure-summary.md`, or leftover worktree exists. | The orchestrator selected repair mode, but state and all recorded prior final summaries report success; ambiguity is not a stopping condition under the autonomous decision policy. | `.ralph/state.json`; `.ralph/runs/*/final-summary.md`; `docs/working/DECISION_POLICY.md` §1 | 005E4-completeness-action-authority-and-browser-proof | Low | No | 2026-07-11 | No partial work was salvaged; the selected sharpened slice was implemented from the current staging-derived worktree. |
| A-065 | Verified PAN/Aadhaar corrections use `POST /api/v1/members/{id}/reverification/` with current `version`, at least one identity value, and a reason. The action atomically resets member KYC to pending; the last member creator/editor cannot verify the resulting member KYC document. | M02-FR-012 requires an approved change request and the slice requires a reverification path, but API §13 names no approval-case resource or endpoint. A dedicated reasoned transition avoids silently overloading PATCH; maker-checker is enforced at the existing KYC verification boundary without inventing an approval authority. | `docs/source/functional-spec.md` M02-FR-012; `docs/source/api-contracts.md` §13; `docs/source/auth-permissions.md` §18/§25.1; `docs/slices/006Y-member-create-update-and-identity-governance.md` | 006Y and future governance confirmation | High | Yes | 2026-07-12 | If governance defines a formal identity-change approval case or separate reverification permission, version this endpoint and preserve masked history/audit, optimistic locking, and verifier separation. |
| A-066 | Witness correction is read-only in 006Y2 after capture because the delivered 004E/004E2 contract exposes only application-scoped GET/POST and names no editable fields, PATCH endpoint, version rule, update permission, or audit event. | Implementing a client-only edit or guessing a write contract would invent governance behavior; 006Y2 explicitly makes backend contract changes out of scope and requires mismatches to be recorded. | `docs/source/screen-spec.md` S09; `docs/source/api-contracts.md`; `docs/slices/004E-witness-shareholder-validation.md`; `docs/slices/004E2-witness-evidence-snapshot-and-input-hardening.md`; `docs/working/API_CONTRACTS.md` | 006Y2 and a future witness-correction contract slice | Medium | Yes | 2026-07-12 | Add edit only after the backend projects a resource action and defines optimistic update, permitted fields, immutable evidence behavior, and metadata-only audit evidence. |
| A-067 | The member registration/edit surface is isolated in `MemberGovernanceForm` while using only the existing card, field, alert, button, grid, colour, spacing, and typography classes. | Registration and reasoned identity correction have shared state/payload/error behavior and no existing component can express that form without duplicating it across Directory and Profile; the binding design rules require recording an absolutely-required new component. | `docs/working/FRONTEND_DESIGN_RULES.md`; existing `MemberDirectory.tsx` and `MemberProfile.tsx` patterns | 006Y2 | Low | No | 2026-07-12 | This is a composition boundary, not a new visual system; do not add bespoke styles or generalize it beyond governed member mutations. |
| A-068 | `members.member.identity_change.approve` is seeded as a dedicated high-risk permission with no default role assignment; administrators must explicitly grant it to the checker role selected by governance. | M02-FR-012 requires approval and 006Y3 forbids a hard-coded approver role, while the source permission catalogue does not assign a named identity-change checker. | `docs/source/functional-spec.md` M02-FR-012; `docs/source/auth-permissions.md` §12.2, §17-§18; `docs/slices/006Y3-member-registry-and-identity-change-approval-closure.md` | 006Y3 | High | Yes | 2026-07-12 | The requester must also differ from the approver; add a default role grant only when governance names that role. |
| A-069 | Witness correction uses `members.witness.update` and application-scoped `GET/PATCH .../witnesses/{witness_id}/`. Name may be corrected by an authorised actor; PAN/Aadhaar correction additionally requires a different actor from the stored verifier. Verification member/shareholding/folio/qualified fact/verifier/time remain immutable. | S09 permits witness identity/display capture and the slice requires correction plus maker-checker where verification changes, but the source permission catalogue/path and approval resource are silent. A narrow update code and verifier separation close the governed mechanism without assigning a hard-coded role or rewriting verification evidence. | `docs/source/screen-spec.md` S09; `docs/source/data-model.md` §10.5, §29-§30; `docs/source/auth-permissions.md` §12.2, §15.4, §26.4, §34; `docs/slices/006Y4-witness-correction-and-resource-action-closure.md` | 006Y4 | High | Yes | 2026-07-12 | If governance defines a formal witness-change approval case or canonical permission/path, version the adapter while preserving optimistic locking, protected values, masked evidence, and immutable verification provenance. |
| A-072 | Superseded by 006Z11: member action permissions never imply global object scope. `MemberScopeAssignment` stores action-specific `global`, `team`, `assigned`, or `created_by` facts; creator ownership remains the source-required Field Officer baseline, and no default management authority is seeded. | Auth §§3/19/25 separate action permission from team/assignment/object scope and do not name a management assignee. Persisting the mechanism without a default grant avoids inventing business authority. | `docs/source/auth-permissions.md` §§3-3.1, §§18-19, §25.1, §34.2; `docs/slices/006Z11-member-scope-assignment-and-list-nondisclosure-closure.md` | 006Z11 and future management-assignee governance | High | Yes | 2026-07-13 | Governance may assign explicit action-specific global/team/member scope through the persisted model; never infer it from role provenance, permission possession, unowned rows, or caller flags. |
| A-073 | Exact ₹5,00,000 belongs to the lower approval-matrix rule and the above-threshold rule begins at ₹5,00,000.01; all amount bounds are inclusive. | Functional M05-FR-004 says "up to ₹5,00,000" and M05-FR-005 says "above", while the open-decisions map still asks for owner confirmation. Inclusive lower/upper bounds are the source-compliant interpretation for two-decimal INR configuration and avoid a gap or overlap. | `docs/source/functional-spec.md` M05-FR-003-006; `docs/source/auth-permissions.md` §16.2; `docs/source/api-contracts.md` §25.1; `docs/slices/007A-approval-matrix-configuration.md` | 007A and downstream 007B/007C routing | High | Yes | 2026-07-13 | If the owner changes the threshold treatment, supersede the seeded rules with a new effective version; never rewrite historical rule projections. |
| A-074 | Approval-configuration decisions use proposal `version` for optimistic concurrency and return a stable conflict for terminal replay; persisted CFO/Company Secretary checker authority requires no additional unnamed action-permission grant. | Auth §§31.1-31.2 names Admin plus CFO/CS approval but defines no idempotency header, checker permission, or replay response. Optimistic locking preserves one decision, while inventing a permission could deny the source-named checker. | `docs/source/auth-permissions.md` §§31.1-31.2; `docs/source/api-contracts.md` §25.1; `docs/slices/007A3-approval-matrix-maker-checker-governance.md` | 007A3 and later configuration UI wiring | High | Yes | 2026-07-13 | If governance introduces a dedicated checker permission or replay-key standard, version the adapter while preserving distinct persisted authority and single activation. |
| A-075 | For a forced or assessment-required exception route, the §25.2 `reason_for_approval` is also frozen as the case's exception reason; the canonical condition remains `exceeds_permissible_limit`. | API §25.2 supplies one reason plus `force_exception_route`, while data-model §15.3 and the 007B slice require the exception condition/reason on the case but name no second request field. Reusing the explicit caller-authored reason avoids inventing text or expanding the source request. | `docs/source/api-contracts.md` §25.2; `docs/source/data-model.md` §15.3; `docs/slices/007B-approval-case-creation-from-appraisal.md` | 007B and 007F exception register | Medium | Yes | 2026-07-13 | If governance defines a distinct exception-reason field, version the adapter and snapshot it separately without rewriting historical cases. |
| A-076 | Superseded by 007K: approval-case detail groups the M05-FR-002 checklist values under `review_facts`, but those values are frozen from the credit-owned review package at enrichment and are never queried from mutable application/appraisal owners at read time. | API §25.4 requires the case detail and Functional M05-FR-002 requires the seven review fact groups. Architecture review proved the original live read-through could change routed truth; 007K replaced it with one complete, typed, attributable frozen package. | `docs/source/api-contracts.md` §25.4/§44; `docs/source/functional-spec.md` M05-FR-002 and Sanction Committee Checklist; `docs/source/codebase-design.md` §13.1; `docs/slices/007K-frozen-review-snapshot-and-selector-boundary-closure.md` | 007K and every approval/register reader | High | Yes | 2026-07-14 | If a source contract later names a different checklist wrapper, version the response while preserving the immutable package and never restoring live read-through. |
| A-077 | The optional `approval_actions.digital_signature_id` is stored as a nullable UUID reference until the source §16.6 `signature_records` owner is implemented; 007C/007D must not treat an arbitrary UUID as verified signature evidence. | Data-model §15.4 names a foreign key, but the repository has no `SignatureRecord` model yet and creating the documentation/signature aggregate in approval routing would cross the selected slice boundary. Retaining the nullable identifier preserves the source column without inventing a signature target. | `docs/source/data-model.md` §15.4 and §16.6; `docs/slices/007C-cfo-and-director-threshold-routing.md` | 007C, 007D, and the signature-record owning documentation slice | Medium | Yes | 2026-07-13 | Replace the UUID field with a protected nullable FK when `signature_records` lands; reject non-null action signature ids until then. |
| A-078 | A permissioned member-list caller with no persisted member scope receives the source-required filtered `200` empty page rather than a collection-level `403`; detail and mutations retain `403 OBJECT_ACCESS_DENIED`. Actorless active-member calculation is authorized only by its owning application or authenticated portal account, not by a direct member calculation scope. | 006Z15's blanket no-scope/assigned wording conflicts with auth §32.1 queryset filtering and with its own requirements 5-6, which require owning-object derivation and forbid restoring a direct actor calculation interface. The source-compliant public behavior is therefore empty collection nondisclosure plus application/portal ownership. | `docs/source/auth-permissions.md` §§24.1, 25.1, 32.1, 34.2; `docs/source/codebase-design.md` §§26.1-26.3; `docs/slices/006Z15-member-public-action-authority-matrix-closure.md` requirements 2-6; A-072 | 006Z15-member-public-action-authority-matrix-closure | High | No | 2026-07-13 | Matrix evidence executes list, detail, mutation, application, portal, and borrower-limit interfaces separately; no permission, role, or member-scope fact is presented as authority for the actorless calculation. |
| A-079 | A sanction decision must copy recommended amount, tenure, interest-rate type, and security summary exclusively from the frozen review package. Numeric interest/penal rates and repayment date remain null, charges remain `{}`, and conditions precedent remain blank until an approved source-owned frozen rate/repayment/condition snapshot exists. | API §25.8 requires these decision fields, but the current reviewed appraisal stores no numeric rate, repayment date, charges, penal rate, or conditions-precedent facts. The 2026-07-14 architecture review reopened 007D's claimed copy boundary after finding terminal creation still reads mutable appraisal-note values; 007O owns closure. | `docs/source/api-contracts.md` §25.8; `docs/source/data-model.md` §8.1/§15.5; `docs/source/functional-spec.md` M05-FR-007/008; `docs/source/auth-permissions.md` §20.1; `docs/slices/007O-frozen-terminal-decision-and-register-source-closure.md` | 007O and later rate/documentation owners | High | Yes | 2026-07-14 | Later owners may populate nullable fields only from versioned approved source facts; historical sanction decisions and register rows remain immutable. A-081 owns returned-cycle state. |
| A-080 | Approval-case read scope is persisted separately from `approvals.case.read`: immutable approvers keep assignment scope; the submitting Credit Manager keeps owner scope; Internal Auditor uses source `audit_readonly`; Company Secretary uses a narrowly named `legal_readonly` grant that permits routed package reads but never approval actions or assigned-queue membership. | Auth §§14.1/26.3 explicitly require Credit Manager, Company Secretary, and Auditor sanction-package reads, while §19.1 names audit/management scope but no exact Company Secretary object-scope code. A dedicated read-only grant is the conservative technical mechanism; treating the action permission or broad `management_readonly` as global case authority would recreate the 007C defect. | `docs/source/auth-permissions.md` §§14.1, 19.1, 26.3, 27.1, 32.1, 37.3; architecture review `2026-07-13_131622_architecture_review` | 007C3-approval-case-source-read-scope-closure | High | Yes | 2026-07-13 | If governance names a different legal-read scope, migrate the grant vocabulary without widening action authority or rewriting immutable case history. |
| A-081 | A returned approval cycle moves the application to `appraisal_reviewed` but the appraisal to editable `draft`; only an actual persisted appraisal/risk value change with a non-empty `appraisal.updated` changed-field ledger is accepted as correction. A no-op PATCH cannot create resubmission authority. This supersedes A-079's appraisal-state sentence while preserving its sanction-decision field assumptions. | The source requires resubmission after clarification and a new cycle after material change but names no separate clarification-edit state or materiality formula. Reusing the existing governed draft/update/review path and requiring attributable changed-field evidence is the narrow industry-standard mechanism; treating an unchanged PATCH as correction would bypass the source invariant. | `docs/source/functional-spec.md` returned workflow state and M05-FR-007/008; `docs/source/codebase-design.md` §13.1; `docs/source/data-model.md` §15.3-15.5/§34; `docs/slices/007D3-returned-approval-cycle-and-resubmission-closure.md` | 007D3 and future clarification UI/governance | High | Yes | 2026-07-13 | If governance defines a dedicated clarification state or a field-level materiality policy, version the transition while retaining immutable prior cycles and mandatory fresh independent review. |
| A-082 | Borrower/Director-relative/material-interest conflict facts are supplied through persisted application/user declarations until the member master owns a governed person-to-user relationship source. Own-application and maker-checker conflicts are derived from the cycle's frozen application creator/receiver/submitter and appraisal preparer/reviewer facts. Excluded snapshotted actors retain limited detail/history read only; they receive no permission-derived global scope, queue membership, or action authority. | Auth §17 and Security §12 require these conflict classes and limited legal read but the current schema has no authoritative user-to-member/director-relative/material-interest relationship. A typed persisted seam avoids guessing identity from names/email and allows later member-master capture to feed the same evaluator. | `docs/source/auth-permissions.md` §17.1-17.3; `docs/source/security-privacy.md` §12.2-12.3; `docs/source/codebase-design.md` §13.2; `docs/slices/007E-conflict-of-interest-blocking.md` | 007E and the future member-relationship capture owner | High | Yes | 2026-07-13 | Replace declaration entry with the governed relationship owner when available, preserving case/cycle assessment snapshots and historical reasons. Never infer a legal relationship from display fields. |
| A-083 | Exception enrichment uses a distinct mandatory `business_reason` and optional `risk_assessment`, superseding A-075's reuse of `reason_for_approval`. Credit Manager holds `approvals.exception.create` only for the existing case-enrichment system path. CFO, Director, and Internal Auditor hold generated-register read. Returned/conflict-blocked closed cases retain register status `pending` while copying `closed_at`. | Slice 007F explicitly separates the register reason, no seeded actor previously held both required create permissions, Functional §6.2 gives exception authority to committee/CFO and view to Auditor, and §15.7 defines only pending/approved/rejected. | `docs/source/data-model.md` §15.7; `docs/source/api-contracts.md` §25.2/§25.10; `docs/source/auth-permissions.md` §12.6/§16.2; `docs/source/functional-spec.md` §6.2/M05-FR-006; `docs/slices/007F-exception-approval-workflow.md` | 007F and downstream 007G/007H/007J | High | Yes | 2026-07-13 | If governance names a different system principal, reader set, or returned/conflict status, version the adapter without rewriting historical entries. |
| A-084 | Nullable `exception_register_entries.loan_account_id` remains a UUID reference until the production finance loan-account aggregate lands; it must not point at `tracer.LoanAccount`, which is synthetic demo lifecycle data. | Data-model §15.7 names a nullable FK, but the repository's only current `LoanAccount` class is owned by the tracer demonstration module rather than the future finance/servicing aggregate. A cross-link would create false production integrity. | `docs/source/data-model.md` §15.7; `sfpcl_credit/tracer/models.py`; `docs/working/CONTEXT.md` current backend modules; `docs/slices/007F-exception-approval-workflow.md` | 007F and the production loan-account model owner in Epic 009 | Medium | Yes | 2026-07-13 | Replace the nullable UUID with a protected nullable FK when the finance loan-account model lands; migrate existing non-null ids only after target validation. |
| A-085 | Resolved by 007G2: a §25.11 recorder needs the Critical record permission, canonical case object access, document-download permission, and §19.4 legal-category authority (Compliance, Company Secretary, Credit Manager, or attributable case approver). Audit-only/generic case read is insufficient. Each distinct file needs public-upload provenance for the exact application, category `legal`, and a matching source-defined sensitivity. The approval owner proves related-party workflow/attribution; the documents owner decides every file. Pending cases project current evidence; every terminal outcome freezes it. | Auth §19.4 defines the Legal audience and requires related entity, category, sensitivity, role permission, and workflow scope but names no finer sensitivity matrix. Using every source-defined sensitivity avoids inventing compliance policy; exact legal/application provenance, canonical case access, related-party workflow, and the source-named legal audience provide the defined facts. Data-model nullable FKs do not relax the all-three-artifacts rule. | `docs/source/api-contracts.md` §§25.4/25.11; `docs/source/data-model.md` §15.8; `docs/source/auth-permissions.md` §§12.6/17.2/19.2/19.4/32.1/34.5; `docs/source/functional-spec.md` M05-FR-012/BR-032; `docs/slices/007G2-general-meeting-current-evidence-and-document-scope-closure.md` | 007G2, 007H2, 007I | High | Yes | 2026-07-13 | If governance defines a narrower sensitivity/category matrix or permits partial artifacts, make that policy explicit/configurable without widening historical case/register reads or granting download authority. |
| A-086 | Credit Sanction Register `financial_year=FYyyyy-yy` uses the Indian April-March financial year: approval dates from April 1 of the first year through March 31 of the second year. | API §25.9 names and exemplifies the filter but does not define its date boundary; April-March is the source-compliant Indian financial reporting default and the sharpened slice explicitly directs recording it when silent. | `docs/source/api-contracts.md` §8.3/§25.9; `docs/slices/007H-credit-sanction-register.md` requirement 3 | 007H and 007J register UI | Medium | Yes | 2026-07-13 | If governance defines another reporting calendar or display syntax, version the filter without rewriting immutable approval dates. |
| A-087 | Credit Sanction Register rows expose the 15 functional-spec fields but store and project no Annexure/template code until OC-002 is resolved. | The process calls Annexure K the Credit Sanction Register while the annexure summary calls K the Grievance Form/Complaint Log; hard-coding either label would invent a disputed compliance mapping. | `docs/source/data-model.md` §36.2; `docs/source/functional-spec.md` §15.3/OC-002; `docs/working/maps/Appraisal and Sanction Map.md`; `docs/slices/007H-credit-sanction-register.md` requirement 5 | 007H, 007J, and future template configuration owner | High | Yes | 2026-07-13 | Blocked/deferred pending client confirmation. Keep template ids independent from annexure labels when governance resolves the mapping. |
| A-088 | A rejected Credit Sanction Register row has a nullable `sanction_decision_id` and `sanctioned_amount`; its immutable decision/reason/authority facts come from the terminal case and action. | Data-model §15.6 marks the sanction-decision FK non-null, but the delivered 007D contract and 007H requirements explicitly create no `SanctionDecision` for rejection, require §25.8 to return 404 when none exists, and forbid inventing a sanction id. A nullable protected link preserves every terminal outcome without fabricating a financial decision record. | `docs/source/data-model.md` §15.5-§15.6; `docs/source/api-contracts.md` §25.8; `docs/slices/007H-credit-sanction-register.md` requirements 1/4 and 007F run-ahead review | 007H and downstream register/report/export consumers | High | Yes | 2026-07-13 | If the source model is corrected to define a separate rejected sanction-decision record, version the contract and migrate only with explicit governance; do not rewrite historical register authority/reason facts. |
| A-089 | 007J does not wire prototype MP12 to the internal §25.8 sanction-decision endpoint. Borrowers continue to receive only their member-owned application status through `/api/v1/portal/applications/{id}/` until a separate source-authorised borrower outcome/terms projection is defined. | §25.8 is protected by internal approval case scope and returns no rejected decision; auth §40.1 names only own-application portal access and does not grant approval permissions or internal history. Granting borrowers `approvals.sanction.read` or widening the internal endpoint from a frontend slice would invent sensitive access and still fail rejected-outcome copy. | `docs/source/api-contracts.md` §25.8; `docs/source/auth-permissions.md` §§19/34.5/40.1; `docs/slices/007J-registers-and-approval-matrix-frontend-wiring.md`; architecture review `2026-07-13_222951_architecture_review` | 007J and a future borrower outcome/terms contract owner | High | Yes | 2026-07-13 | Define a borrower-owned projection with self scope, borrower-safe fields, approved/rejected semantics, and tests before replacing MP12's honest status view. Never expose internal approvers, reasons, register metadata, or permissions. |
| A-090 | The 007I General Meeting form can create new referenceable evidence only by uploading all three files through the exact-application `legal` document boundary; a recorder without upload permission can reuse the case's already accepted three document ids, but the UI does not offer arbitrary UUID entry or infer referenceability from register/case metadata. | No source-backed list endpoint projects application-scoped legal documents that are referenceable for §25.11. Offering free-form ids would imply authority the document owner has not granted, while requiring upload permission as part of the three-permission recorder rule would contradict A-085. | `docs/source/api-contracts.md` §§25.4/25.11/26.1; `docs/source/auth-permissions.md` §19.4; A-085; `docs/slices/007I-sanction-workbench-ui.md` | 007I and a future legal-document selection projection owner | Medium | Yes | 2026-07-13 | Partially superseded by architecture review `2026-07-14_010536_architecture_review`: an id being present on case metadata does not prove current document-owned referenceability. `007L` must require a document-owned selector or three new accepted uploads for changed evidence, while backend per-file validation remains authoritative. |
| A-091 | 007J adds two focused page components because the existing S23/S25 and S71 blocks mix large mock-backed hubs with unrelated business fixtures and cannot express authenticated loading, denial, pagination, or governed successor submission by composition alone. The components reuse the existing `Tabs`, `AlertBanner`, `StatusBadge`, `Modal`, `card`, `table-*`, `field-*`, and `btn-*` patterns; resource-specific canonical permissions map only to narrow hub-navigation capabilities, and backend sessions with only those capabilities see only the owned register/matrix surfaces. | FRONTEND_DESIGN_RULES permits a new component only when existing composition cannot express the function and requires the reason to be recorded. Separating the API-owned panels prevents mock fallback and prevents a register/matrix permission from disclosing unrelated hub fixtures. | `docs/working/FRONTEND_DESIGN_RULES.md`; `docs/source/api-contracts.md` §§25.1/25.9/25.10; `docs/source/auth-permissions.md` §§16/25.4; `docs/slices/007J-registers-and-approval-matrix-frontend-wiring.md` | 007J and future replacement of remaining prototype hub surfaces | High | Yes | 2026-07-14 | Keep canonical resource authorization inside each owned panel and use hub permissions only for navigation. Replace remaining mock tabs independently; do not widen one resource permission into another tab's authority. |
| A-092 | 007J2 composes one focused `LoanPolicySettingsPanel` because the 003E/006C retained-version list and complete successor form require independent authenticated loading, denial, validation, success, and read-only state without disturbing 007J's approval-matrix boundary. Workflow TAT is explicitly inert until 012EA, S72 document templates until 008A, and the duplicate user tab/fixtures are removed in favor of the existing 002G/002G2 Admin User Management screen. | S70 and M01 require real versioned policy truth, while S71 names approval rules only, S72 requires a document-file/version model that 003F communication content does not provide, and S73 already has an API-backed canonical screen. FRONTEND_DESIGN_RULES permits focused composition from existing table/card/modal/alert patterns when the mixed fixture hub cannot express independent authority states. | `docs/source/screen-spec.md` S70-S73; `docs/source/functional-spec.md` M01-FR-001-015/M18-FR-001-011; `docs/slices/003E-versioned-configuration-shell.md`; `docs/slices/006C-loan-limit-configuration-and-calculator.md`; `docs/slices/007J2-settings-hub-panels-wiring-or-lockdown.md` | 007J2, 008A, and 012EA | Medium | Yes | 2026-07-14 | Keep all deferred panels inert until their named owner lands. Never treat 003F communication templates as S72 document-template files or recreate users/roles inside SettingsHub. |
| A-093 | The 007J2 policy editor creates a complete new draft only through collection POST and exposes no PATCH or activation action. Therefore a client cannot overwrite an existing policy with stale form state; server validation/permission failures stay visible in the modal and the UI refetches retained truth after success. The 003E API defines no expected-version token for POST, so 007J2 does not fabricate a `STALE_WRITE` contract or claim that multiple independent drafts are a stale-write conflict. | Slice 007J2 requires every edit to create a new configuration version and handle stale writes. Create-only versioning makes destructive stale overwrite structurally unavailable, while inventing single-draft/concurrency rules would contradict M01-FR-001's one-or-more configurations and DECISION_POLICY's ban on unstated business rules. | `docs/slices/007J2-settings-hub-panels-wiring-or-lockdown.md` requirement 2; `docs/slices/003E-versioned-configuration-shell.md`; `docs/source/functional-spec.md` M01-FR-001/M01-FR-015; `docs/working/DECISION_POLICY.md` | 007J2 and any future source-backed loan-policy maker-checker governance owner | Medium | Yes | 2026-07-14 | If governance later defines one-successor-only, expected-version, or maker-checker proposal semantics for loan policy, add that server contract and UI handling without rewriting retained versions. |
| A-094 | Exception enrichment accepts zero to 20 ordered, distinct supporting document ids. Each supplied file must have public-upload provenance for the exact application, category `legal`, a source-defined sensitivity, document-download permission, canonical application object access, the §19.4 legal audience, and exact attachment inside the locked case transaction. The workflow scope passed to the documents owner is currently the approval workflow context; no exception type makes documents mandatory. | S25 requires supporting uploads and 007M requires a bounded optional list plus category/sensitivity/role/object checks, but source docs define neither an upload-time case/cycle field, a finer legal sensitivity matrix, a numeric bound, nor exception-type document rules. Twenty is a conservative transport bound; architecture review retained the established legal evidence boundary rather than inventing schema or governance. | `docs/source/screen-spec.md` S25; `docs/source/api-contracts.md` §§25.2/25.10/26; `docs/source/auth-permissions.md` §§19.2/19.4; A-085; `docs/slices/007M-exception-supporting-evidence-and-register-closure.md`; architecture review `2026-07-14_034706_architecture_review` | 007M and future governed document-category configuration | High | Yes | 2026-07-14 | If governance defines case/cycle upload provenance, narrower sensitivity/category rules, another bound, or per-type mandatory files, version the documents-owned policy without rewriting frozen register metadata or granting download through register visibility. |
| A-129 | Portal deficiency resubmission persists the canonical loan-application status as `submitted` and records `portal.application.resubmitted` plus a workflow transition from `incomplete_returned` to `submitted`; the portal labels that event “Application resubmitted”. It does not add a separate stored `resubmitted` status. | Member-portal MP11 calls the borrower-facing state Resubmitted, while the authoritative `loan_application_status` vocabulary omits that value and includes `submitted`/`incomplete_returned`; returning to `submitted` also reopens the existing Deputy Manager completeness queue without inventing a parallel queue rule. | `docs/source/screen-spec-member-portal.md` MP11/§8.2/§9.1; `docs/source/data-model.md` `loan_application_status`; `docs/source/functional-spec.md` M03 deficiency flow; `docs/slices/008L2-member-portal-deficiency-response-and-resubmission.md` | 008L2 and future application-state vocabulary governance | High | Yes | 2026-07-15 | If source governance adds a canonical persisted `resubmitted` enum, migrate the transition and staff queue together while preserving immutable resubmission audit/workflow history. |
| A-130 | MP11 is a focused composition rendered inside MP10 only for `incomplete_returned` applications. | The source requires a distinct MP11 interaction with independent loading, upload-progress, validation, auth, empty, error, response, and resubmit states; MP10's former read-only validation card cannot express those states without a focused composition. The implementation reuses existing portal cards, alerts, badges, inputs, buttons, spacing, colours, and typography. | `docs/source/screen-spec-member-portal.md` MP10-MP11; `docs/working/FRONTEND_DESIGN_RULES.md`; `docs/slices/008L2-member-portal-deficiency-response-and-resubmission.md` | 008L2 | Low | No | 2026-07-15 | Keep MP11 reachable only from the own returned-application status path and do not introduce a parallel visual system or client-owned workflow state. |
| A-131 | The §31.3 server-owned `next_action` uses `record_bank_transfer` after CFC approval and `none` after rejection. | The slice requires a server-owned next-action state but the opened source/digest does not define its wire vocabulary. These narrow values describe the already sequenced 009G action without claiming transfer execution or inventing a rejection workflow. | `docs/source/api-contracts.md` §31.3; `docs/slices/009F-cfc-authorization-rejection.md`; `docs/working/digests/epic-009-sap-loan-account-disbursement.md` | 009F and 009G | Medium | Yes | 2026-07-17 | If source governance defines a canonical action-object vocabulary, version this projection while preserving retained terminal decisions and never implying bank success from approval. |
| A-132 | 009H selects exactly one approved/effective borrower email template whose code starts `disbursement_advice_email_` and whose declared/used variables are exactly borrower name, application reference, loan account number, sanctioned amount, disbursement amount, disbursement date, and masked bank reference. The exact template-selection rule remains; the Manual-adapter acceptance sentence is superseded by architecture review `2026-07-18_152831_architecture_review` and corrective slice 009H7 because a no-provider mode cannot fabricate acceptance. | Source §31.5 fixes only channel/recipient, integrations §§10/19 require content templates and advice notification, and the source does not name the event's exact template code/version or production transport; the later review clarified that §§10.2/21 still require real provider acceptance or explicit manual confirmation. The established code convention plus an exact variable set avoids arbitrary caller text and bank-secret leakage while keeping real transport replaceable. | `docs/source/api-contracts.md` §§31.5/39; `docs/source/integrations.md` §§9.1/10/19.3/21; `docs/source/functional-spec.md` BR-054/M08-FR-010; `docs/slices/009H-disbursement-advice-and-communication.md` | 009H and 009I portal advice delivery | High | Yes | 2026-07-17 | If governance assigns another event code, language-selection rule, mandatory advice file, or live provider, version the template/adapter contract without rewriting retained communication snapshots or weakening exact replay. |
| A-133 | MP14 downloads the exact communication-owned finalized borrower advice subject and body as a deterministic UTF-8 text attachment. | The source requires a downloadable issued advice and exact retained delivery truth but does not define a governed PDF or document-template artifact. Reusing the finalized communication bytes avoids fabricating a second artifact or changing the advice already accepted by the provider. | `docs/source/api-contracts.md` §31.5; `docs/source/integrations.md` §§10/19.3/21; `docs/source/screen-spec-member-portal.md` MP14; `docs/slices/009I-member-portal-disbursement-status.md` | 009I and any future governed advice-document owner | Medium | Yes | 2026-07-18 | If governance introduces a mandatory advice PDF/template, version the communications-owned artifact and capability file identity without rewriting historical delivery evidence. |
| A-134 | 009K exposes one read-only, role-scoped disbursement workspace collection whose rows contain safe display facts and server-owned action descriptors; it does not make the browser reconstruct payment authority from the separate loan, SAP, notification, bank, readiness, and mutation endpoints. The two owned pages retain their existing queue/detail/card/field/button compositions; small local render helpers only remove repeated markup while preserving the approved classes and are necessary because server-described fields vary by action. | S36-S41 require one walkable staff workflow, but §§29-31 define mutation/detail fragments and no staff queue/read aggregate or source-bank selector. A narrow backend-for-frontend projection is the standard safe seam: existing owners remain authoritative, protected identifiers are supplied only as fixed action payload, and the client cannot invent role, readiness, money, or state decisions. | `docs/source/screen-spec.md` S36-S41/§9.6; `docs/source/api-contracts.md` §§29-31/45; `docs/source/integrations.md` §§9.1-9.7; `docs/slices/009K-disbursement-and-cfc-frontend-wiring.md`; `docs/working/FRONTEND_DESIGN_RULES.md` | 009K and future finance-workspace extensions | High | Yes | 2026-07-19 | If source governance later defines separate SAP/disbursement queue endpoints or a universal action-object schema, version the projection without widening scope or moving action predicates into the client. |
| A-135 | Initial-loan-payment SAP posting confirmation remains unavailable: successful transfer creates one honest `pending` obligation, but no role grant or external/manual confirmation adapter is assigned. | M07-FR-009 requires tracking the entry, while the source names neither its posting actor nor a confirmation interface. Assigning the repayment posting permission or claiming local payment as SAP success would invent financial authority/evidence. | `docs/source/functional-spec.md` M07-FR-009; `docs/source/auth-permissions.md` §§12.9/25.7/26.5; `docs/slices/009L-epic-009-staff-workflow-and-sap-posting-closure.md` | 009L and future owner governance | High | Yes | 2026-07-19 | Governance must name the actor, permission, adapter, and immutable provider/manual evidence before `posted` can be accepted. |
| A-136 | 010A exposes plural read collections at `/loan-accounts/{id}/repayment-schedule/` and `/loan-accounts/{id}/ledger/`, both with the existing strict list envelope and account-read authority. The first ledger row projects the canonical successful disbursement owner rather than copying it into a new ledger table. | Source §30 names only account/status endpoints, while S43/S46 and the 010A slice require paginated schedule/ledger reads without defining paths or a separate ledger schema. Resource subpaths plus owner-backed projection are the conventional narrow extension and avoid inventing a mutable financial duplicate before later servicing owners exist. | `docs/source/api-contracts.md` §30; `docs/source/data-model.md` §18.3; `docs/source/screen-spec.md` S43/S46; `docs/slices/010A-loan-account-schedule-and-ledger.md` | 010A and 010B-010H ledger movement owners | Medium | Yes | 2026-07-19 | Later servicing slices must append canonical movement projections or governed append-only entries with deterministic running balances; they must not rewrite the disbursement row or accept browser totals. |
| A-137 | 010A persists only the repayment-schedule statuses named by data-model §18.3: `pending`, `paid`, and `overdue`. It does not add the S43 display label `Partially paid` as a stored business state before the repayment-allocation owner defines its derivation and transition rules. | The source data table gives a three-value persisted vocabulary, while S43 lists a fourth display state without defining storage, thresholds, or transitions. Persisting the extra state here would invent financial lifecycle behavior in a read-foundation slice. | `docs/source/data-model.md` §18.3; `docs/source/screen-spec.md` S43; `docs/slices/010A-loan-account-schedule-and-ledger.md` | 010A and 010C repayment-allocation owner | Medium | Yes | 2026-07-19 | Yes — 010C | 010C keeps a partially paid installment stored as `pending` and exposes its server-owned paid amounts; it changes to `paid` only when principal, interest, and configured charges are fully paid. |
| A-138 | For 010B's SAP-posting SLA, “next working day” advances past Saturday and Sunday only; no holiday calendar is inferred. | The source requires next-working-day posting but defines no holiday calendar, jurisdictional calendar, or configuration owner. A weekday-only durable due date is the narrow standard default and remains visibly pending rather than claiming SAP automation. | `docs/source/user-flows.md` §27; `docs/source/functional-spec.md` BR-057; `docs/slices/010B-direct-repayment-posting.md` | 010B; a later configuration slice must replace this only when an approved holiday calendar exists | Medium | Yes | 2026-07-19 | Confirm SFPCL's governed holiday calendar before business-holiday-aware SLA reporting. |
| A-139 | 010C applies the account-level principal allocation across locked schedule rows in installment-number order, then applies the account-level interest allocation in the same order. A partially settled row remains stored as `pending`; it becomes `paid` only when all principal, interest, and configured charges on that row are paid. | The source mandates principal before interest and requires affected schedule truth to update, but does not define how an account-level receipt is distributed among multiple installment rows or a persisted partially-paid status. Oldest-installment-first is the conservative servicing default, and retaining `pending` uses the source-defined status vocabulary while paid amounts expose partial truth. | `docs/source/functional-spec.md` BR-056/§11.9; `docs/source/data-model.md` §§18.3/19.6/35.2; `docs/slices/010C-principal-first-allocation.md`; A-137 | 010C and future schedule-policy governance | High | Yes | 2026-07-19 | Confirm contractual installment-order and partial-status policy before changing this prospective allocation rule; never rewrite retained allocation or ledger evidence. |
| A-140 | 010D uses narrow technical permissions `finance.bank_statement.read`, `finance.bank_statement.import`, and `finance.bank_statement.match`, granted only to Credit Manager, Accounts Head, and Senior Manager – Finance. Manual matches retain `statement_match_status = manual_match_exception`; automatic exact matches retain `matched_exact`, while repayment allocation remains unchanged. | Source finance-document access and Stage 6 authorize Credit, Accounts, and Treasury to reconcile repayment evidence, but the §12.9 catalogue does not name statement-specific codes or a receipt-side evidence vocabulary. Narrow permissions avoid reusing allocation/SAP authority, and a separate evidence status prevents a manual match from falsely claiming financial allocation. | `docs/source/auth-permissions.md` §§12.9/19.3-19.4/20.2/26.6; `docs/slices/010D-bank-statement-matching-unmatched-receipts.md`; `docs/working/digests/epic-010-servicing-repayments-interest-monitoring.md` §010D | 010D and 010E/010C2 reconciliation owners | High | Yes | 2026-07-19 | If governance defines other permission codes, role scope, or receipt exception vocabulary, migrate prospective authority/projections without rewriting retained statement, receipt, or audit evidence. |
| A-141 | 010D retains `sfpcl_bank_account` as a normalized, nonblank, bounded provenance identifier supplied by an authorized importer; it does not treat that label as verified bank ownership or use it for posting, allocation, or balance movement. | The selected slice requires retaining the SFPCL bank account but the opened source/digest defines no collection-account registry or verification owner. Reusing the disbursement source-bank registry would conflate outgoing and incoming bank authority. | `docs/slices/010D-bank-statement-matching-unmatched-receipts.md`; `docs/working/digests/epic-010-servicing-repayments-interest-monitoring.md` §010D; `docs/source/data-model.md` §19.5 | 010D and a future governed collection-account configuration owner | High | Yes | 2026-07-19 | Governance must define the collection-account registry and activation authority before this provenance label can claim verified SFPCL account ownership. |
| A-142 | 010C2 introduces the critical `finance.repayment.manual_allocation_approve` permission with no default role grant; only an explicit governance assignment can produce terminal approval for an exact manually matched receipt, destination, and amount. | M09-FR-009 requires approval but the source role/permission catalogue names Credit Manager and Accounts for capture/allocation without naming the exceptional-allocation approver. Default-deny preserves the required approval mechanism without inventing checker authority. | `docs/source/functional-spec.md` M09-FR-009; `docs/source/auth-permissions.md` §§12.9/20.2/26.6; `docs/slices/010C2-manual-allocation-and-financial-reversal-controls.md` | 010C2 and future repayment-correction governance | High | Yes | 2026-07-20 | Governance must name the approving role before any default role grant is added; retained exact approval evidence must remain immutable. |
| A-143 | 010C2 introduces the critical `finance.repayment.reverse` permission with no default role grant; reversal is available only after governance explicitly assigns elevated financial-correction authority. | S44 requires elevated permission and a mandatory reason but the source permission catalogue and Stage 6 matrix do not identify the reversing role. Default-deny avoids silently converting ordinary Credit Manager/Accounts allocation authority into historical correction authority. | `docs/source/screen-spec.md` S44/S46; `docs/source/auth-permissions.md` §§12.9/26.6; `docs/slices/010C2-manual-allocation-and-financial-reversal-controls.md` | 010C2 and future repayment-correction governance | High | Yes | 2026-07-20 | Governance must name the reversing role before any default role grant is added; correction must remain a compensating append-only movement. |
| A-144 | 010E retains `subsidiary_company_id` as a required opaque UUID and does not claim referential validation against a company master. Produce-payment references are duplicate-protected per that UUID, while transfer references are globally duplicate-protected. | API §32.3 and data §19.5 call the value a foreign key, but the implemented schema and cited source sections define no canonical subsidiary-company table, lifecycle, or lookup authority. Persisting the required opaque identity preserves reconciliation evidence without inventing a company master or accepting a mutable company name as authority. | `docs/source/api-contracts.md` §32.3; `docs/source/data-model.md` §19.5; `docs/source/domain-model.md` §13.4; `docs/slices/010E-subsidiary-deduction-reconciliation.md` | 010E and a future subsidiary-company registry owner | High | Yes | 2026-07-20 | Governance must define the subsidiary-company registry and migration mapping before this UUID may be upgraded to an enforced foreign key; retained deduction identities and duplicate constraints must remain stable. |
| A-145 | 010E2 stores benchmark name, spread, and reset frequency only when explicitly supplied, uses the caller-supplied approved `effective_rate` as authoritative configuration, and creates rate-change obligations for loan accounts whose canonical status is exactly `active`. It does not infer a benchmark formula, reset cadence, penal rate, or future rate for sanctioned/unfunded accounts. | The source requires floating effective-rate versions and SMS/email notice on revision, but identifies benchmark/spread/reset and penal-interest policy as open and does not define a derivation formula. The implemented loan lifecycle makes `active` the post-transfer servicing status, while `sanctioned` is not yet funded. | `docs/source/functional-spec.md` M10-FR-001/002; `docs/source/data-model.md` §§18.5/25.3; `docs/source/domain-model.md` §19.2; `docs/source/screen-spec.md` §9.8; `docs/slices/010E2-effective-rate-versioning-and-borrower-notices.md` | 010E2 and 010F/010G calculation owners | High | Yes | 2026-07-20 | If governance supplies a benchmark formula, reset calendar, penal-rate policy, or a broader serviceable-loan notice population, version that policy prospectively without rewriting retained rate, consumption, or delivery evidence. |
| A-146 | 010F supports an explicitly approved `simple_daily` invoice configuration whose effective version supplies day-count basis, tax percentage, fixed fee, communication template, and owner-role codes. Gross interest is `principal snapshot × approved effective rate × inclusive FY days / day-count basis / 100`; retained in-period interest allocations reduce the invoice, then configured tax and fixed fee apply. No default configuration is seeded. | Functional §11.10 explicitly leaves day count, tax/fee, benchmark/spread/reset, and owner policy unresolved, while data §19.7 defines one principal/rate/amount snapshot. A required effective configuration makes the mechanism executable without silently choosing production accounting or ownership policy. | `docs/source/functional-spec.md` §11.10; `docs/source/domain-model.md` §13.5 ownership clarification; `docs/source/data-model.md` §19.7; `docs/slices/010F-interest-invoice-generation.md` | 010F and future approved accounting/owner governance | High | Yes | 2026-07-20 | Governance must approve and provision each effective invoice configuration before generation; any new calculation method must be versioned prospectively and must not rewrite retained invoices. |
| A-147 | 010G reuses the effective approved `simple_daily` calculation version delivered by 010F for method and day-count basis, but ignores invoice-only owner, tax, fee, document, and communication fields. A monthly accrual covers one complete calendar month only: the loan must be funded by the first day and must not close during that month. It does not prorate a disbursement/closure month because no approved partial-month principal or rate policy exists. | M10-FR-003 requires the configured method and explicitly leaves day count unresolved; the accrual source table stores one month/principal/rate/amount snapshot and defines no daily principal segments or partial-month rule. Requiring a complete eligible month and failing closed is safer than inventing proration, while reusing the already versioned day-count seam avoids a competing accounting configuration. | `docs/source/functional-spec.md` §11.10 M10-FR-003/Open Accounting Configuration; `docs/source/data-model.md` §19.8; `docs/slices/010G-monthly-interest-accrual.md`; A-146 | 010G and future approved accounting-policy governance | High | Yes | 2026-07-20 | Governance must explicitly approve any partial-month/principal-segmentation method or a dedicated accrual calculation configuration before it is introduced; changes must be prospective and retained accrual snapshots remain immutable. |
| A-148 | 010I classifies SOP ageing by calendar anniversaries of the earliest unpaid due date: before the first anniversary is `Current`, the first anniversary through the day before the second is `1–2 years`, the second anniversary through the third anniversary is `2–3 years`, and `>3 years` begins the following day. A 29 February anniversary falls on 28 February in a non-leap year. Optional standard buckets are returned only when a separately persisted effective scheme explicitly configures their 30/60/90-day upper bounds; no scheme is seeded. | The source fixes the bucket names and DPD subtraction formula but does not settle inclusive year boundaries, leap-day handling, or an operational-bucket configuration owner. Calendar anniversaries preserve the source's year language, keep exactly three years in the inclusive `2–3 years` band, and avoid silently enabling optional management reporting. | `docs/source/data-model.md` §35.4; `docs/source/functional-spec.md` M11-FR-003/004; `docs/slices/010I-dpd-calculation-and-monitoring-buckets.md` requirement 4 | 010I and future approved DPD policy governance | Medium | Yes | 2026-07-20 | Governance should confirm anniversary inclusivity, leap-day treatment, and provision an effective standard-bucket scheme if operational reporting needs 0–30/31–60/61–90/>90. Historical snapshots and their retained policy version must not be rewritten. |
| A-149 | 010J exposes the bounded automatic queue at `POST /api/v1/reminders/quarter-end-runs/`, one SMS or email channel and one approved effective template per request, with a hard 100-loan candidate bound. The source-defined per-loan reminder endpoint remains the manual creation interface. Caller `message_body` is validated for the source wire contract but cannot override the approved rendered template snapshot. | API §§34.3–34.4 define only the per-loan reminder path, while M11-FR-006 and the sharpened slice require a bounded quarter-end portfolio run without naming its transport path, batch shape, or maximum. A narrow collection action and the existing portfolio bound are conventional; one channel/template per run keeps dedupe and template evidence exact, and refusing caller content authority prevents bypass of approved communications governance. | `docs/source/api-contracts.md` §§34.3–34.4; `docs/source/functional-spec.md` M11-FR-006/007; `docs/slices/010J-reminder-queue.md` requirements 1–3 | 010J and 010M staff reminder wiring | Medium | Yes | 2026-07-20 | If governance defines another scheduling endpoint, batch size, multi-channel run, template-selection policy, or caller-authored exceptional content approval flow, version it prospectively without rewriting retained reminder or communication snapshots. |
| A-150 | 010I2 keeps the operational 0–30/31–60/61–90/>90 scheme optional when no effective scheme exists, but fails a new calculation when an effective scheme exists without `active` status. Every snapshot freezes the code-owned SOP anniversary convention plus the optional scheme identity, version, dates, and bounds in append-only calculation inputs. | M11-FR-004 says standard buckets are optional, while 010I2 requires missing/unapproved policy to fail without fabricating DPD. Treating total absence as optional and an effective retired scheme as an explicit non-approved conflict reconciles those contracts without silently enabling management buckets or rewriting retained calculations. | `docs/source/functional-spec.md` M11-FR-003/004; `docs/source/data-model.md` §35.4; `docs/source/codebase-design.md` §38.2; `docs/slices/010I2-dpd-pointer-and-policy-integrity-closure.md`; A-148 | 010I2 and future approved DPD policy governance | High | Yes | 2026-07-21 | Governance should define an explicit approval workflow/status if `active` is not the intended approval fact; changes must be prospective and preserve frozen snapshot bytes. |
| A-151 | 010L exposes direct repayment bank details only from a complete runtime `PORTAL_REPAYMENT_INSTRUCTIONS` value explicitly marked `approved`; absence or partial configuration returns unavailable instructions. It does not reuse the statement-import `sfpcl_bank_account` label or the governed outgoing disbursement source bank. Borrower UTR/proof submission remains disabled. | MP18 requires approved repayment details but the cited sources define no collection-account registry or approval actor. A-141 already forbids treating the statement label as verified ownership, and outgoing source-bank authority is a different financial purpose. A fail-closed injectable configuration seam completes safe read delivery without inventing a role, database approval lifecycle, or payment-submission policy. | `docs/source/screen-spec-member-portal.md` MP18; `docs/slices/010L-member-portal-repayment-view.md`; A-141 | 010L and a future governed collection-account configuration owner | High | Yes | 2026-07-21 | Governance must define the collection-account registry, activation authority, audit evidence, and secret provisioning before production enables details; any later UTR/proof flow requires its own approved contract. |
| A-152 | 010MA uses small local presentational components for shared pagination controls, the two independently paginated statement queues, and subsidiary reconciliation rows. They compose the existing Tabs, AlertBanner, StatusBadge, button, table, and card patterns and introduce no new visual primitive or business-policy owner. | The existing Tabs contract accepts separate React children, while the S43-S46 surfaces need independently stateful loading/error/empty/pagination rendering. No existing component expresses these financial projections without either duplicating pagination markup across the owned pages or coupling unrelated servicing queues. | `docs/slices/010MA-servicing-account-and-repayment-frontend-wiring.md`; `docs/working/FRONTEND_DESIGN_RULES.md`; `sfpcl-lms/src/components/loan/RepaymentLedger.tsx`; `sfpcl-lms/src/pages/repayments/RepaymentsHub.tsx` | 010MA-servicing-account-and-repayment-frontend-wiring | Low | No | 2026-07-21 | Revisit only if a future shared server-table primitive can preserve each queue's canonical pagination and state contract without adding client-owned financial behavior. |
| A-153 | 010N exposes global search as `POST /api/v1/global-search/` with `{search, page_size, pages}` in the JSON body, returns independently paginated authorised groups, and rate-limits by authenticated actor without storing or auditing query text. | API §8.4 defines resource-list GET search examples but does not define an S02 aggregate endpoint. A GET aggregate would place PAN, Aadhaar, bank, or cheque input in browser history and access logs, contradicting 010N's explicit no-query-logging/privacy contract. A read-only POST is the conservative privacy-preserving transport; group-specific page numbers retain §8.1 pagination without exposing denied group counts. | `docs/source/api-contracts.md` §§8.1/8.4; `docs/source/screen-spec.md` S02; `docs/slices/010N-global-search-api-and-ui.md` | 010N and 011M3 compliance-provider registration | High | Yes | 2026-07-21 | If governance standardises a different privacy-safe aggregate-search transport, preserve exact hashed lookup, per-group nondisclosure, and the prohibition on query-string/log persistence. |
| A-154 | M11-FR-009 extension and non-payment consequences are explicitly deferred from Epic 010 to Epic 011's default and recovery workflow owners. Epic 010 must expose truthful DPD, reminder, MIS, and account state but must not invent an extension approval or recovery consequence. | The source requirement names extension or non-payment action, while the implemented Epic 010 boundary owns monitoring and servicing projections rather than default/recovery decisions. The ready queue already assigns those decisions to 011C and 011D, so duplicating them in a servicing corrective would split authority and fabricate policy. | `docs/source/functional-spec.md` M11-FR-009; `docs/working/digests/epic-010-servicing-repayments-interest-monitoring.md`; `docs/slices/011C-default-declaration.md`; `docs/slices/011D-recovery-action-initiation.md` | Epic 010 requirement audit; 011C and 011D | High | Yes | 2026-07-21 | Epic 011 must implement the cited source behavior before closure; Epic 010 consumers may display only canonical downstream decisions once they exist. |
