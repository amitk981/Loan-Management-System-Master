# Ralph Handoff

## Last Run

2026-07-14_170201_normal_run

## Current Status

008E2 is complete. New signature capture resolves canonical borrower, selected nominee,
application witness, or active user identity, freezes its exact id/name and Compliance maker, and
keeps unresolved mismatch truth immutable through ordinary capture. Only a distinct Company
Secretary can resolve it; role changes do not collapse maker/checker identity.

Authorized unknown, wrong-stage, unrelated, and non-current signature ids share 404 while missing
action authority remains 403 before owner scope. Resolution returns a retained §6.3 workflow action
identity, exact replay is zero-write, and capture/checklist consumers share one legal selector.
A-109 preserves honest nullable legacy attribution without an invented remediation path.

## Validation

Evidence is in `.ralph/runs/2026-07-14_170201_normal_run/evidence/`: six RED/GREEN tracers, identity/
authority/error matrix, exact §6.3 contract assertions, and two PostgreSQL passes of both five-worker
races. Frontend build/typecheck/lint and all 293 tests pass. Django check/migration sync pass; all 789
backend tests pass with 26 expected PostgreSQL-only skips and 93% coverage against the 85% floor.

## Next Run

Run 008F, then 008G. Both are sharpened to consume the 008E2 legal selector and reject A-109 legacy/
null-maker signature history; 008F must also reject A-108 legacy stamp/notary evidence. A-101 still
blocks the real M05-to-full-Term-Sheet path and A-107 still limits signed-copy evidence claims.
