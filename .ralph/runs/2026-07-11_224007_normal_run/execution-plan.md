# Execution Plan

Selected slice: `006H3-appraisal-workbench-prototype-fidelity-restoration`

1. Recover the approved pre-006H workbench, eligibility checklist, and loan-limit calculator from
   Git history and map their queue/header/stage/card/disclosure patterns onto the current API types.
2. Add a focused Playwright appraisal visual-state matrix and baseline contract first, covering
   the slice's queue, assessment boundary, appraisal lifecycle, and operational states through the
   routed workbench at the existing Chromium viewport. Save the failing-first output.
3. Restore the approved staged composition using only existing classes/components while preserving
   resource `available_actions`, disabled reasons, exact mutation bodies, writable projection, and
   canonical four-read reload behavior. Do not restore mock facts or client business decisions.
4. Run focused frontend tests and Playwright collection/Chromium capture, then lint, typecheck,
   Vitest, build, and the configured backend gates with the mandated Ralph interpreter. Save logs
   and portable screenshot evidence in this run folder.
5. Perform a standards/spec review, record the fidelity checklist, changed files, risk assessment,
   review packet, final summary, and update the selected slice, state, progress, and handoff. Review
   the next one or two Not Started slices and sharpen only if their existing contracts need it.

Permissions checked: intended edits are limited to `sfpcl-lms/src/**`, `sfpcl-lms/e2e/**`,
`docs/working/**`, `docs/slices/**`, `.ralph/state.json`, `.ralph/progress.md`, and this run folder.
No protected or `docs/source/**` path will be modified.
