# Execution Plan

Selected slice: `008M2-documentation-workspace-contract-and-visual-closure`

## Repair diagnosis

The newest retained failure summary is the 008M repair that exceeded Ralph's 2,000-line limit at
2,084 changed lines. The later passing representation compressed the workspace owner, API client,
and tests into unreadable physical lines. Architecture review then reproduced the parallel action
contract, false queue-success state, dead S28-S34 controls, incomplete S26/S35 projection, missing
staff download matrix, Download/action suppression, and absent screenshots. 008L5 has separately
closed the current terminal-sanction bank fact consumed here.

## Permissions and boundaries

- Edit only allowed workspace paths under `sfpcl_credit/**`, `sfpcl-lms/src/**`,
  `sfpcl-lms/e2e/**`, `docs/working/**`, `docs/slices/**`, `.ralph/state.json`,
  `.ralph/progress.md`, and this run folder.
- Do not edit `docs/source/**` or any protected file.
- Reuse existing backend owners and frontend components; add no visual system or dependency.
- Keep the non-`.ralph/` diff within 30 files and 2,000 changed lines.

## Implementation sequence

1. Inventory the current workspace projection, owner-module allow/deny seams, endpoint contracts,
   signed staff download boundary, frontend modal/action patterns, and existing E2E seed/server
   harness. Record a ranked, falsifiable repair hypothesis set.
2. Add focused failing backend tests first for the standard §44 action shape/owner parity, S26/S35
   queue and redaction facts, action/status/download independence, and the exact staff download
   success/audit plus denial/zero-audit matrix. Save RED output under `evidence/terminal-logs/`.
3. Replace the compressed workspace projection with readable helpers that compose existing public
   owner decisions, strict pagination, current 008L5 bank truth, redacted timeline/comments, and
   current signed-download capabilities. Implement only the slice's staff read/action boundary.
4. Add focused failing frontend tests for mutually exclusive queue states, every returned action
   family and exact request/refetch/error behavior, Document Pack Download+Verify coexistence, and
   recursive redaction. Save RED output, then implement readable API DTOs/workspace code using the
   existing Modal/Tabs/forms and styling.
5. Add `sfpcl-lms/e2e/staff-documentation-workspace.e2e.spec.ts` against the real authenticated
   Django server and persisted S26-S35 seed facts. Declare all four exact screenshots and exercise
   one real action, a non-optimistic conflict, and a Django-denied restricted content request. Run
   local collection/non-browser feedback if Chromium is unavailable; never fabricate screenshots.
6. Run focused GREEN tests, recursive redaction/mock/readability/diff probes, then frontend build,
   typecheck, lint, tests and backend check, migrations, full coverage with the mandated interpreter.
7. Save self-contained evidence, sanitized §44 example, diff report, changed-files, risk assessment,
   review packet, and final summary. Update API contracts, prototype ledgers, digest, state, progress,
   handoff, and mark only 008M2 Complete after all runnable gates pass. Sharpen the next one or two
   Not Started slices only from already-opened source/digest material.
