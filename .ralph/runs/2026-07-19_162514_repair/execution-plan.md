# Execution Plan

Selected slice: `CR-012-epic-009-playwright-evidence-is-incomplete`

1. Preserve the quarantined implementation and use the previous repair's trusted-browser failure
   as the exact red-capable signal: `Initiate payment` remains disabled after `--make-ready`.
2. Confirm the real backend transition is already covered and that the failure is stale mounted
   browser state, not a product rule, permission, API, or seed defect.
3. Make the narrowest test-only correction: reload the authenticated app after the out-of-browser
   fixture mutation before reopening SAP & Disbursement. Do not alter production behavior.
4. Re-run the focused backend seed regression, exact Playwright collection/static boundary checks,
   and, where the sandbox permits, the declared real-Django browser spec. Leave the authoritative
   twice-run browser decision to the orchestrator.
5. Inspect the bounded diff, save terminal evidence, complete the risk assessment and final summary,
   and set the review result exactly to `Ready for independent validation`.
