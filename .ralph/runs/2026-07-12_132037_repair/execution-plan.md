# Execution Plan

Selected slice: 006X5-credit-public-action-write-matrix-closure

1. Reproduce the exact independent validation failure by applying the protected PostgreSQL
   acceptance predicate to the preserved six-test log; save the red-capable output.
2. Preserve all production behavior and matrix coverage. Refactor only the newly added stale
   projection race from a separately discovered test into the existing sanction concurrency test,
   keeping both race scenarios and their assertions while restoring the fixed five-test contract.
3. Run collection and the focused SQLite test seam locally, then run the exact PostgreSQL
   acceptance command when the configured server is available; save green evidence.
4. Run the configured backend and frontend quality gates in proportion to this test-only repair,
   verify protected paths/diff limits, and refresh the repair evidence and handoff artifacts.

No production code, schema, API, frontend, source document, protected file, or business rule will
be changed during this repair.
