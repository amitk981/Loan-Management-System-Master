# Execution Plan

Selected slice: `006Z10-portal-limit-interaction-and-boundary-proof`

Mode: repair of the quarantined implementation from run `2026-07-13_034816_repair`.

1. Preserve all existing slice changes and reproduce the exact fourth trusted Playwright scenario
   that timed out while clicking the fifth pre-collected `Mark Uploaded` locator.
2. Make the smallest spec-only correction: drive each dynamic document action through a fresh,
   visible locator so prior clicks cannot shift the remaining indexed locators.
3. Re-run the focused scenario, then the complete declared browser spec twice with the exact Ralph
   evidence environment and verify all four screenshots are non-empty.
4. Run proportionate frontend gates and browser collection; retain the prior green backend evidence
   because the demonstrated repair changes no backend or business logic.
5. Refresh this run's terminal logs, changed-files list, risk assessment, review packet, final
   summary, state/progress/handoff, and slice status without altering protected or source files.
