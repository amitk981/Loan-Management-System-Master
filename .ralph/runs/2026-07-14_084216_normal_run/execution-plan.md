# Execution Plan

Selected slice: 008B-document-generation-shell

1. Inspect the existing document-template, storage, application-authority, appraisal, sanction,
   audit, workflow, envelope, and pagination interfaces. Define one documents-owned generation
   module whose small public interface owns validation, merge projection, rendering/storage,
   replay identity, immutable metadata, and evidence.
2. TDD tracer 1: add one service/API behavior test for sanctioned Individual Term Sheet PDF
   generation, capture RED, implement the minimum model/migration/module/route path, and capture
   GREEN. Consume the public 008A2 borrower-variant and template-source-reference interfaces.
3. Continue vertical RED/GREEN cycles for all 13 Term Sheet facts, missing/unknown fields,
   template/effective/type/variant/file/format/state denials, permission/object-scope matrices,
   Word output, metadata-only bounded listing, deterministic safe names, and Loan Agreement's
   executed-Term-Sheet guard. Keep denial paths zero-write and rendered content out of evidence.
4. Add application/template/output-format replay identity and transactional cleanup. Prove one
   canonical result/evidence set under a declared five-request PostgreSQL race and prove storage
   failures leave neither metadata nor orphaned generated bytes.
5. Update the working API contract and any necessary assumption/digest notes. Run focused tests,
   Django check/migration sync, the full backend coverage gate, and all frontend gates; save RED,
   GREEN, migration, response, and gate evidence.
6. Review the diff against the slice/source contract and repository standards. Complete the Ralph
   risk assessment, review packet, changed-files list, final summary, slice status, state, progress,
   handoff, and sharpen the next one or two Not Started slices using only source material already
   opened.

Public behaviors under test: sanctioned generation succeeds exactly once; every authority and
validation denial fails closed without side effects; list scope is applied before pagination;
metadata is non-sensitive; replay/concurrency retains one result; storage failure is atomic from
the caller's perspective.
