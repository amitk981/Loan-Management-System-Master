# Execution Plan

Selected slice: 010H3-interest-policy-and-reclassification-integrity-closure
Mode: same-worktree repair

1. Preserve the existing 010H3 product candidate and reproduce the frozen-candidate hash failure
   with the smallest exact PostgreSQL acceptance test that restores the Annexure I fixture.
2. Correct only test-storage isolation in the new 010H3 PostgreSQL acceptance class; do not change
   production interest behavior or protected orchestration files.
3. Rerun the focused hash repro and the complete five-test PostgreSQL acceptance class, retaining
   repair evidence in this run folder.
4. Run the required semantic review-closure validator until it prints PASS, then complete the risk,
   review, and final-summary artifacts for independent validation.

Permissions checked: `sfpcl_credit/**` and `.ralph/runs/**` are allowed without approval;
`scripts/**`, Ralph configuration, source documents, and all other protected paths remain read-only.
