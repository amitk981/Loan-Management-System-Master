# Execution Plan

Selected slice: `008D-stamp-duty-and-notarisation-tracking`

## Scope and seams

- Add the source-defined stamp-duty and notarisation one-to-one current records, immutable version
  evidence, database constraints, and one migration in the `legal_documents` owner.
- Expose exactly the two POST routes from API contracts §26.9-§26.10 through thin HTTP adapters.
- Put target authority, current-renderer provenance, Stage 4 role/permission rules, payload
  validation, row locking, idempotent create/update, audit/workflow evidence, and lifecycle status
  projection behind one small legal-document mutation interface.
- Reuse a narrow checklist status-projection seam that preserves applicability, linkage,
  completion/verifier evidence, signatures, checklist status, and presentation metadata.
- Keep signatures, verification, checklist completion/approval, generated bytes, template/file
  provenance, and frontend wiring out of this slice.

## TDD tracer bullets

1. RED then GREEN: a Compliance Team actor can create a pending stamp record through HTTP; the
   response/list/checklist projections are metadata-only and the attributable ledgers are written.
2. RED then GREEN: exact stamp replay is zero-write; a changed submission updates the one current
   row while retaining the prior version/audit facts.
3. RED then GREEN: Company Secretary can record adequate stamp facts and completed notarisation;
   Compliance can prepare pending facts but cannot claim verified outcomes.
4. RED then GREEN: reject unknown/malformed fields, invalid money/type/status/dates, incomplete
   verified outcomes, missing/inaccessible/cross-application evidence, unrelated/read-only actors,
   non-current renderer provenance, and completed-checklist projection conflicts with zero writes.
5. RED then GREEN: PoA and Loan Agreement accept both owner statuses without hard-coded rate
   calculation or premature checklist completion; other document types retain nullable/not-required
   lifecycle states until explicitly recorded.
6. Add the declared PostgreSQL five-worker changed-submission acceptance test: one current record,
   five attributable real-mutation history entries, and a winner matching one submitted payload.

Each behavior is implemented one test at a time through public HTTP/module interfaces. RED and GREEN
outputs are saved under `evidence/terminal-logs/`.

## Verification and closure

- Run focused backend tests after each cycle, then Django check, migration-sync, full backend
  coverage, and all frontend build/typecheck/lint/test gates with the mandated interpreters/tools.
- Save API response examples and terminal evidence; no screenshots are required because frontend
  scope is explicitly none.
- Update `docs/working/API_CONTRACTS.md`, the Epic 008 digest if new durable extraction is needed,
  slice status, state, progress, handoff, changed-files, risk assessment, review packet, and final
  summary.
- Review the complete working-tree diff against the slice and repository standards, address all
  findings, and sharpen the next one or two eligible Not Started slices only from already-opened
  source/digest material.
