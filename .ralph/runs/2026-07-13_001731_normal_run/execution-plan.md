# Execution Plan

Selected slice: 006Z6-active-member-evidence-atomicity-and-history-closure

1. Add public-interface failing tests proving complete service/relaxation evidence participates in
   result provenance and that reason-only relaxation is rejected; save RED output.
2. Implement immutable service-evidence projections and a shared member object-authority seam used
   by Member Registry and active-status verification; keep portal projections redacted.
3. Add failing effective-history and denial-matrix tests, then enforce later-only effective dates,
   immutable closed rows, stale member/result behavior, maker-checker, and zero loser evidence.
4. Add/extend PostgreSQL verification-versus-evidence mutation races using locked evidence sets and
   prove one coherent current pointer/record with no loser history or audit evidence.
5. Run focused green tests, backend checks/migration sync/full coverage, frontend gates, dependency
   scan, and the declared PostgreSQL races twice; save all required evidence and history example.
6. Update the epic digest, selected slice status, Ralph state/progress/handoff, run risk/review/final
   packets, changed-files list, and sharpen the next one or two eligible Not Started slices using
   only already-open source-derived material.
