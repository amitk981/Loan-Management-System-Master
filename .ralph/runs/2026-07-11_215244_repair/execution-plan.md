# Execution Plan

Selected slice: 005E4-completeness-action-authority-and-browser-proof

1. Confirm the source-defined permission, role, state, object-scope, and resource predicates for pass, return, resolve, and rejection-note creation; inspect the 005E3 implementation and architecture-review finding.
2. Establish a fast, deterministic failing backend permission/action-parity test that exercises the exact shared-authority defect, and save its red output.
3. Implement one applications-module action-authority seam with the full six-field action contract; route all four projections and public write boundaries through it without changing checklist composition, history, canonical reload, or stale-write behavior.
4. Expand the focused backend matrix for permission-only actors, scope/state/resource denial, exact errors, and zero side effects; save focused green output and HTTP examples.
5. Make the trusted Playwright contract portable through `RALPH_EVIDENCE_DIR`, cover the default routed container's pass/return/resolve/reject, denied, stale, and API-error states, and declare all nine screenshot outputs without introducing styling or client-owned workflow decisions.
6. Run focused frontend tests and Playwright collection/local execution as sandbox capability permits, then run all configured backend/frontend quality gates and save terminal evidence.
7. Complete the prototype-fidelity checklist, changed-files list, risk assessment, review packet, final summary, state/progress/handoff/slice status, and sharpen the next one or two Not Started slices using only already-opened source material.
