# Visual Acceptance Attempt

The run prepared isolated real Django databases for the processing and finalized-disbursement
states and saved their authenticated envelopes. The prescribed in-app browser runtime reported
`No browser is available`; its one permitted discovery check returned an empty browser list.
Additionally, the sandbox denied both Django and Vite localhost listeners with `EPERM`.

No screenshot was fabricated. The component interaction tests cover processing, disbursed with
advice, safe error, empty, and expired-session rendering. Real-backend screenshots remain for the
orchestrator's external browser environment.
