# Execution Plan

Selected slice: 006Z5-active-member-evidence-and-verification-governance-closure

1. Confirm the source-backed active-member record, supply evidence, permission/object-scope,
   transaction, and redaction contracts from the cited sections and the Epic 006 digest.
2. Add failing-first module/API tests for complete evidence snapshots, BR-006 fail-closed behavior,
   explicit date/payload validation, object-scope non-disclosure, persisted relaxation evidence,
   effective-record history, immutable credit snapshots, and external redaction. Save red output.
3. Add the effective-dated verification/evidence models and one migration; deepen the public
   active-member module so calculation and verification use persisted reviewable facts and perform
   all member/current/history/audit/effective-record writes atomically.
4. Wire the HTTP boundary through MemberRegistry object access before verification lookup and return
   standard validation/conflict envelopes. Keep portal projections deliberately redacted.
5. Run focused green tests, migration checks, dependency scan, and the declared PostgreSQL races
   where the configured service is available; save all evidence and then run every configured gate.
6. Update API/digest/assumption documentation where the contract or an explicit governance gap
   requires it; sharpen the next one or two Not Started slices using only already-open sources.
7. Save changed-files, risk assessment, review packet, final summary, and update slice status,
   Ralph progress/state, and handoff only after the implementation and gates are green.
