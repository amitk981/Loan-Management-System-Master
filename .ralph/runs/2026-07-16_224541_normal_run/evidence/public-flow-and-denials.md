# Public Flow and Denial Results

- Genuine create → send → capability → download → complete → masked read returns the current
  immutable SAP decision and preserves one safe ledger tuple.
- Genuine existing-member reuse returns the retained code identity without rewriting code history.
- Mutating every safe send/completion audit-body field makes the decision absent.
- Changed file metadata/bytes, code creator, communication recipient, task relation, workflow, or
  duplicate communication/task/workflow makes the decision absent.
- Invalid current evidence returns the existing safe 409 taxonomy and exposes no masked customer
  code, code id, capability, or workbook; missing/inaccessible member scope remains the existing 403.
- Denied adapter delivery is atomic and zero-write apart from the already-created draft request.

The focused final run executed 89 tests: 86 passed and the three PostgreSQL-only races were skipped
under the declared `none` runtime capability. See `terminal-logs/22-focused-sap-tests-final-green.txt`.
