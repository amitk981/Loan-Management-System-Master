# Execution Plan

Selected slice: 008E2-signature-identity-mismatch-lifecycle-closure

1. Reproduce the unresolved-mismatch overwrite through the public signature API and save the
   failing RED output. Add one public-behaviour tracer at a time for canonical signer identity,
   immutable capture-maker/checker separation, nondisclosing resolution scope, and the §6.3 action
   response.
2. Deepen the `legal_documents` signature boundary: parse requests at the legal serializer seam,
   resolve action authority and accessible Stage-4 parent scope before signature existence, obtain
   signer facts from canonical application/identity owners, freeze maker identity, and centralise
   the application signature selector.
3. Make capture replay-only for unresolved/resolved signer rows; make Company Secretary resolution
   the sole mismatch-clearing action, enforce immutable-user maker/checker separation, and retain
   exact replay as zero-write with complete audit/version/workflow facts.
4. Add the bounded schema migration, API-contract documentation, and focused HTTP/direct-module
   matrices. Run and save GREEN focused tests, then the declared PostgreSQL five-worker capture and
   resolution races twice.
5. Run all configured backend and frontend gates, save evidence and review artifacts, check diff
   limits/protected paths, sharpen the next two Not Started slices only from already-open Epic 008
   material, then update slice status, state, progress, and handoff.
