# Execution Plan

Selected slice: 006X6-credit-authority-state-parity-matrix-closure

1. Inspect the existing 006X5 matrix, public credit module seams, authoritative PostgreSQL race
   harness, and the cited source sections needed to map each real six-field action to its write.
2. Add one failing executable matrix increment at a time, beginning with completeness and a
   non-permission denial variant; save the focused red output under `evidence/terminal-logs/`.
3. Implement/refactor only the test harness and any production predicate correction that the real
   action/write pairing exposes. Each case must use the same actor/resource for projection and
   write and capture before/after state plus evidence cardinalities.
4. Expand through role, exact permission, object scope, maker-checker, frozen provenance,
   immutable review consistency, rejection payload, and stale state, then save the focused green
   output and generated action/write table.
5. Run the declared PostgreSQL acceptance suite twice without skips, dependency scan, focused and
   full backend gates, and all configured frontend gates; retain terminal logs in the run folder.
6. Review the diff against the slice/source contract, record changed files and risk/traceability,
   update the Epic 006 digest, sharpen the next one or two eligible Not Started slices using only
   already-open source material, and update slice status, state, progress, and handoff.

Permissions checked: intended edits are limited to `sfpcl_credit/**`, `docs/working/**`,
`docs/slices/**`, `.ralph/state.json`, `.ralph/progress.md`, and this run folder; all are listed as
allowed without approval. Protected and forbidden paths remain read-only.
