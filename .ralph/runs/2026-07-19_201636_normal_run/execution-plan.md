# Execution Plan

Selected slice: `CR-013-epic-009-terminal-owner-boundary-correction`

1. Preserve the retained review failures as public-seam tests, one vertical TDD cycle at a time:
   checksum-drift list/detail/mutation nondisclosure first, then stale Senior Finance/CFC workspace
   count-row-action parity. Save every focused RED and GREEN command under
   `evidence/terminal-logs/`.
2. Introduce one backend-owned eligibility decision/identity selector consumed by Loan Account,
   S36, S37, combined Senior Finance, and CFC list counts, pages, rows, details, actions, and
   mutations. Keep SAP, transfer, disbursement, and authority rules behind their owning public
   modules; do not duplicate private predicates in views/projectors.
3. Add a migration-state regression, then implement deterministic legacy SAP delivery-checksum
   reconciliation that preserves coherent pre-field completions and fails closed for incomplete or
   tampered evidence.
4. Replace the runtime Epic 009 fixture's test-module/TestCase/private-helper dependency with public
   fixture/domain builders. Make all E2E seed families idempotent and order-safe, with fresh-database
   regression coverage for both command orders.
5. Complete the bounded five-branch matrix (1/21/101, adjacent invalids, page boundaries, scalar
   drift, paired action/mutation, 400/403/404/409, and query ceiling) using shared scenario builders,
   and keep the trusted PostgreSQL label at exactly six tests.
6. Run focused backend greens with the mandated Ralph interpreter, `manage.py check`, migration
   sync, impacted frontend tests/typecheck/lint/build, and Playwright collection/local feedback as
   the sandbox permits. Do not run the complete backend suite or full coverage.
7. Inspect targeted diffs and stats, save self-contained test and review evidence, complete the High
   risk assessment and final summary, and set the review-packet result exactly to
   `Ready for independent validation` only after focused gates are green.
