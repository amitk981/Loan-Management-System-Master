# Execution Plan

Selected slice: `006Z10-portal-limit-interaction-and-boundary-proof`

1. Inspect the credit-owned borrower projection, portal adapter, MP05 routed container, existing
   unit/container tests, and declared Playwright contract. Map each missing boundary and lifecycle
   assertion to the narrowest existing test seam.
2. Add backend public-boundary matrix tests first for future/closed/manual/stale/mismatched
   authority, changed provenance, duplicate shareholding, contradictory profile/land facts, missing
   policy, and invalid requested amount. Snapshot the complete member/application/assessment/audit/
   workflow/configuration evidence set before and after every denial and save the failing run.
3. Add mounted MP05 tests first for exact create-or-update/submit/refetch traces, independent
   400/403/409 behavior, reload/provenance/redaction, transport/unavailable/loading states, and
   contradictory amount/flag authority. Save the failing run.
4. Make only the production changes required by those public tests, preserving existing UI
   composition and delegating every money/authority decision to the credit projection. Run focused
   green backend and frontend tests and save terminal evidence.
5. Strengthen `e2e/portal-application-limit-authority.e2e.spec.ts` with the four declared screenshot
   states, discriminating server-flag fixtures, real submit/refetch, and reload; run Playwright
   collection locally without fabricating screenshots.
6. Run all configured frontend and backend gates with the mandated interpreters. Record logs and
   self-contained evidence, verify protected paths and diff limits, then update the slice, state,
   progress, handoff, digest, changed-files list, risk assessment, review packet, and final summary.
7. Before completion, inspect and sharpen the next one or two eligible `Not Started` slices using
   only already-opened Epic 006/007 material, without changing their statuses.
