# Execution Plan

Selected slice: `009L7-epic-009-read-boundary-convergence-closure`

## Outcome

Close Epic 009 on one executable read boundary: public Loan Account reads retain their dedicated
permission and exact object scope, staff initiation uses a separate owner interface, and every
collection counts/pages only identities that its complete scalar owner will project. Replace the
browser seed's dependency on private `TestCase` helpers and prove targeted plus ordinary full-suite
Playwright collection selects the required fixture families.

## Impact Boundary

- Backend owners: Loan Account read authority/projection, SAP current-send/completion selection,
  staff disbursement candidate composition, and CFC current-evidence selection.
- Backend tests: permanent public-HTTP versions of all three architecture-review probes, the five
  collection-branch count/page/scalar/action matrix, and the exact six-test PostgreSQL label.
- Browser fixture boundary: reusable public Epic 009 fixture/domain builder, guarded management
  command, and non-browser Playwright seed-selection regression.
- Frontend production UI and styling are out of scope; the existing browser spec and nine declared
  screenshots remain the trusted external acceptance contract.
- No Epic 010 servicing behavior, SAP-posting confirmation, schema rule invention, dependency,
  protected-file, or `docs/source/` change.

## TDD Sequence

1. Add the public Loan Account permission/scope regression and save focused RED output; restore
   `finance.loan_account.read` as mandatory and introduce a distinct initiation-candidate owner;
   save GREEN output.
2. Promote the two scalar-drift review probes to permanent public-seam tests one at a time, saving
   RED/GREEN evidence while moving send/file integrity into the owner decisions used before count.
3. Extend the retained matrix across Loan Account, S36, S37, combined Senior Finance, and CFC
   branches for mixed sizes, adjacent invalid rows, page edges, scalar variants, actions/mutations,
   error contracts, ordering, and bounded queries. Keep each added behavior on a focused
   RED-to-GREEN tracer.
4. Define a public Epic 009 fixture builder, add a regression forbidding runtime imports/calls into
   `TestCase` internals, then refactor the guarded management command onto that interface.
5. Add a non-browser Playwright configuration regression that initially fails for ordinary
   `npm run e2e`; make seed selection provision the union of fixture families for targeted and
   multi/all-spec collection without destructive reseeding.
6. Run focused backend tests with `/Users/amitkallapa/LMS/.ralph/venv/bin/python`, the declared
   PostgreSQL six-test label twice where the local service permits, frontend config/unit tests,
   typecheck, lint, build, backend `manage.py check`, and migration sync. Do not run the complete
   backend suite or coverage locally; Ralph owns those gates.

## Evidence and Review

- Save every focused RED/GREEN command under `evidence/terminal-logs/` and record test counts.
- Attempt the declared browser contract only within sandbox constraints; never fabricate the nine
  screenshots. Ralph will run the exact browser contract twice outside the sandbox.
- Inspect diff stats and targeted hunks, then complete `risk-assessment.md`, `review-packet.md`, and
  `final-summary.md`. Set the review result exactly to `Ready for independent validation` only
  after focused gates are green.
