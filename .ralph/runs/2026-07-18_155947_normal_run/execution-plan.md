# Execution Plan

Selected slice: 009G6-legal-migration-exception-fingerprint-closure

1. Preserve the architecture-review same-model option probe as the first public-guard regression and run it alone to capture RED evidence.
2. Deepen the legal-owned migration guard so a retained operation is accepted only when the complete before/after `DocumentChecklist` model state differs by exactly its named constraint transition; do not change migrations, models, schema, or runtime behavior.
3. Add one-property mutation matrices for fields, full constraint definitions, indexes, options, bases, and managers, advancing test-by-test through RED then GREEN while retaining exact historical and future legal-owner acceptance.
4. Re-run the focused 009G4/009G5 manifest and ownership guard tests, migration synchronization, zero-SQL legal anchor proof, Django check, and Python compilation with the mandated backend interpreter. Save all terminal evidence in this run folder.
5. Review the bounded diff against the slice and cited source, audit protected paths and migration/model immutability, then update the Epic 009 digest, slice status, state, progress, handoff, changed-files list, risk assessment, review packet, and final summary. Recheck the next two Not Started slices and sharpen only if their existing requirements are incomplete.

No frontend, API, model, migration, dependency, or database change is planned. The test surface is the public `legal_checklist_state_ownership_violations` interface; private fingerprint helpers remain implementation detail.
