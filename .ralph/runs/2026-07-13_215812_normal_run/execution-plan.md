# Execution Plan

Selected slice: 007H2-sanction-decision-and-register-object-scope-closure

1. Trace the sanction-decision and register views through the approval-owned coherent-case/read-index
   selector, and catalogue existing public endpoint tests, seeded role permissions, and documentation
   contracts without changing protected or source files.
2. RED/GREEN tracer: add a public API regression proving a same-permission Director receives a
   nondisclosing denial for another application's sanction decision, then minimally route decision
   lookup through the canonical approval-case object decision.
3. RED/GREEN scope matrix: add public register regressions proving unrelated same-permission rows are
   filtered before financial-year/decision filters, counts, page bounds, and pagination; minimally
   delegate the register queryset to the approval-owned coherent-case selector.
4. Extend the public role/object matrix for assigned/effective/acted Directors and persisted
   management/legal/audit readers, plus permission-only/object-only negatives and the distinct
   sanction/exception reason projection. Preserve zero writes and immutable 15-field rows.
5. Update role-seed expectations, API_CONTRACTS.md, the Epic 007 digest, and 007I/007J run-ahead
   contracts with the exact scoped-read semantics. Sharpen the next one or two Not Started slices
   using only source material already opened.
6. Run scoped tests during each RED/GREEN cycle, backend checks/migration sync and full coverage,
   then frontend build/typecheck/lint/tests. Save self-contained terminal evidence, changed-files,
   risk assessment, review packet, final summary, state/progress/handoff, and mark only 007H2 complete.
7. Review the completed diff against both repository standards and the slice specification. Do not
   invoke git add, commit, push, or modify any protected/source path; integration remains owned by
   the Ralph orchestrator.
