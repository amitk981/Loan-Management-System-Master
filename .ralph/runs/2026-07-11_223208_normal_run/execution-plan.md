# Execution Plan — 006H7 Credit Action Parity and Container Proof

1. Establish the failing-first backend parity and mounted default-container tests. Cover the public write/projection boundary, canonical four-read refresh, exact mutation payloads, disabled/denied/validation/stale behavior, and response-only PATCH exclusion. Save red output under `evidence/terminal-logs/`.
2. Refactor the eligibility, loan-limit, and appraisal deep modules so each public mutation and its six-field action projection consume the same transition evaluation. Preserve existing public contracts, transaction ordering, immutable history, sanction dependency direction, and disabled reasons.
3. Pin the standard React Testing Library packages, replace the source/static child proxy with tests mounting the default `AppraisalWorkbench`, and remove the React status/provenance workflow matrix so backend `enabled` is authoritative with `/auth/me` permission/role intersection only as a usability guard.
4. Run focused green tests, dependency scan, then all configured frontend and backend gates with the mandated Python interpreter. Save self-contained logs and exact HTTP examples.
5. Review the diff for protected paths and scope, produce changed-files/risk/review/final artifacts, update slice/state/progress/handoff, and sharpen the next one or two eligible Not Started slices using only already-opened source extracts.

Permissions checked before editing: planned paths are allowed by `.ralph/permissions.json`; no protected, approval-required, forbidden, or `docs/source/` path will be modified.

Selected slice: 006H7-credit-action-parity-and-container-proof

The selected agent must replace this template with a slice-specific execution plan before editing product code.
