# Visual Evidence Attempt

- Attempted to start the real Vite application on `127.0.0.1:5173` for browser inspection.
- The sandbox rejected the listener with `Error: listen EPERM: operation not permitted 127.0.0.1:5173`.
- The in-app browser runtime was initialized according to its skill instructions, but browser discovery returned an empty list (`[]`).
- No screenshots were fabricated. Visual coverage is retained through the jsdom interaction tests for readiness blockers, initiation/replay, CFC authorisation, duplicate UTR, denial, and empty states. The orchestrator or owner must capture real screenshots in an environment that permits a local listener and browser.
