# Execution Plan

Selected slice: `008M-documentation-hub-frontend-wiring`

Repair target: replace the demonstrated non-atomic, client-derived documentation workspace while
preserving the quarantined slice's valid mock-removal work. Do not broaden beyond 008M.

1. Build a tight regression loop around the exact review failure: one backend contract test must
   require a single application-locked staff workspace response containing only server-owned
   applicability, blockers, workflow states, role-correct actions, and latest-current signed
   download capabilities; focused frontend tests must reject multi-request composition, synthesized
   routes, swallowed errors, optimistic conflicts, and missing workflow rows.
2. Inspect the existing 008K5 checklist reconciliation, 008L4 latest-current selector/capability,
   security package projections, generation and approval action routes, and existing prototype page
   structure. Reuse those owners through a narrow top-level coordinator instead of copying policy.
3. Write and run the focused backend regression first with the mandated Ralph Python interpreter,
   save RED output, then implement the smallest read/action projection required by S26-S35. Keep the
   DTO allowlist closed and the read under one canonical application/checklist lock.
4. Rewrite the frontend API seam to consume that one projection and its server-issued action URLs.
   Restore the original DocumentationHub/checklist/modal composition and change only labels, real
   data, visibility, and role/action wiring. Preserve explicit loading, empty, forbidden, error,
   restricted, blocked, replay/conflict, success-refetch, and terminal-status-plus-download states.
5. Add focused interaction tests for generation, bank verification, PoA, tri-party, SH-4, CDSL,
   blank/cancelled-cheque custody, final ordered approvals, current capability download, denied
   mutations, and owned mock removal. Save RED/GREEN logs and run focused backend/frontend loops.
6. Run frontend lint, typecheck, full Vitest, and build plus Django check, migration drift, and full
   backend coverage. Collect honest visual evidence using the available local browser path; never
   fabricate screenshots if the sandbox blocks Chromium.
7. Complete the repair run artifacts, changed-files list, risk assessment, review packet, final
   summary, state/progress/handoff/slice status, and sharpen the next one or two Not Started slices
   using only already-opened digest facts. Confirm protected paths and diff limits before handoff.
