# Execution Plan

Selected slice: 008L3-portal-action-and-resubmission-contract-closure

1. Reproduce the independent trusted-browser failure with the exact declared two-spec command and
   identify whether the expected Playwright Chromium revision or another compatible executable is
   already available locally.
2. Apply only the smallest 008L3-scoped browser-harness correction that makes the declared command
   use the orchestrator-available browser without weakening either spec, screenshot assertion, or
   twice-run acceptance contract.
3. Run the two declared Playwright specs twice, verify all four non-empty screenshots, and save the
   repair RED/GREEN terminal evidence.
4. Re-run proportionate frontend gates and Playwright collection, then update the repair run's
   changed-files, risk assessment, review packet, and final summary while preserving the original
   implementation and state records.
