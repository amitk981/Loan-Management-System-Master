# Execution Plan

Selected slice: CR-003-member-governance-container-pr-ci-timeout

1. Preserve a RED-capable timing reproduction for the current oversized routed mounted journey,
   using a constrained per-test timeout and storing terminal output under `evidence/terminal-logs/`.
2. Split the journey at observable production-container boundaries so each focused test has one
   responsibility while their union retains registration, navigation, exact POST/PATCH ledgers,
   canonical create/update readbacks, and cleanup isolation.
3. Retain deterministic labeled-control fixture entry for bulk fields and one small, ordinary
   `userEvent` edit as genuine human-interaction coverage.
4. Execute the affected focused group, including the immediately following parameterized case, at
   least 20 consecutive times and save GREEN evidence.
5. Run all configured frontend and backend gates, save logs and review artifacts, then update the
   selected slice, Ralph state/progress, and handoff. Sharpen the next one or two Not Started slices
   only from already-opened source/digest material.

No production code, visual design, API contract, backend behavior, or dependency change is planned.
