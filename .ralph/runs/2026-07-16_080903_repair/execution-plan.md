# Execution Plan

Selected slice: 008M3-documentation-workspace-executable-action-closure

1. Preserve the architecture-review action-parity probe as failing-first evidence, then add focused backend regressions for owner-decision parity, opaque action identity, nondisclosing stale/tampered scope, upload/re-upload, correction/return, S35 conditions, and every checklist/approval role and blocker named by the slice.
2. Introduce the narrowest workspace command contract that resolves and locks canonical application, checklist, item, document, security, bank, and approval facts server-side; advertise an action only when the same owner decision can execute it, and keep 008M4's broader query/module redesign out of this repair.
3. Add frontend failing-first regressions for all sibling mutations plus independent Download, upload/correction field handling, rejected-action visibility, tampered/conflict errors, and exactly one successful refetch; then wire the existing checklist/modal patterns without new styling or layout.
4. Extend the real-Django Playwright contract for a genuine enabled click, upload/correction, a multi-action Document Pack row, rejected-action persistence, and the four declared screenshots. Attempt local collection/non-browser feedback if Chromium services are unavailable; leave screenshot authority to the orchestrator rather than fabricate evidence.
5. Run focused red/green commands, backend checks and full coverage, frontend test/typecheck/lint/build gates, migration sync, and the declared browser contract where locally possible. Save terminal evidence and sanitized API examples.
6. Update API contracts and the Epic 008 digest if the public action shape changes; complete changed-files, risk, review, final summary, progress/state/handoff, mark only 008M3 Complete, and sharpen the next one or two eligible Not Started slices using already-open source material.

Constraints: stay within 30 changed files / 2,000 changed lines; do not change protected or source files; use `/Users/amitkallapa/LMS/.ralph/venv/bin/python` for every backend command; do not commit, add, push, install packages, or absorb 008M4 structural/design work.
