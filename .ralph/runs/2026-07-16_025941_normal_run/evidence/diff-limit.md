# Diff Limit

The implementation remains below Ralph's 30-file / 2,000-line / one-migration limits. It adds one
applications migration containing two nullable UUID snapshot columns; no dependency was added.
The final exact counts are recorded in `changed-files.txt` and the review packet.
