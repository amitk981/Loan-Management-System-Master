# Review-only probe result

Command:
`/Users/amitkallapa/LMS/.ralph/venv/bin/python manage.py test <run>/evidence/review-probes --pattern 'test_disbursement_review_probes.py' --verbosity 2`

Result: expected non-zero review signal. Django created an isolated in-memory database, applied all
migrations, and reported no system-check issues. 18 tests ran in 10.979 seconds:

- 14 retained public transfer/advice tests passed.
- `TransferReplayContractProbe.test_exact_replay_uses_api_45_2_shape` errored because
  `idempotency_replayed` is absent.
- `AdviceCurrentTruthProbes.test_cfc_cannot_send_advice_under_source_role_matrix` failed because
  the CFC request returned HTTP 200 instead of 403.
- `AdviceCurrentTruthProbes.test_changed_canonical_email_makes_old_recipient_replay_conflict`
  failed because replay returned HTTP 200 instead of 409.
- `AdviceCurrentTruthProbes.test_changed_rendered_snapshot_makes_replay_conflict` failed because
  replay returned HTTP 200 instead of 409.

The first attempted command used an unsupported single-file Django label and was corrected to
directory discovery; it did not create a test database or alter product state.
