# Execution Plan

Selected slice: 006X10-credit-object-scope-executable-row-closure

1. Replace the static name inventory with a runtime-resolvable case registry containing eight
   unique callable test methods, and add a completeness test that rejects missing, duplicate, or
   non-test callables.
2. In TDD cycles, split the two paired appraisal helpers and the mixed loan-limit/sanction methods
   so each advertised test executes only one persisted arrangement, one exact six-field disabled
   projection, its matching public write denial/category, and a complete evidence comparison.
3. Replace the synthetic omission proof with real eligibility-case executions that deliberately
   omit projection, write, category, or evidence and assert the focused local failure.
4. Save red/green focused output, run each of the eight identifiers alone, then normal/reverse and
   separate-process selections plus focused HTTP denial and dependency scans.
5. Run all configured backend/frontend gates, save the review/risk/changed-file artifacts, update
   the Epic 006 digest and Ralph state/progress/handoff/slice status, and sharpen the next two
   Not Started slices using only already-opened Epic 006/007 material.

Permissions checked: planned edits are limited to `sfpcl_credit/**`, `docs/working/**`,
`docs/slices/**`, `.ralph/state.json`, `.ralph/progress.md`, and `.ralph/runs/**`, all permitted by
`.ralph/permissions.json`; no protected or forbidden path will be changed.
