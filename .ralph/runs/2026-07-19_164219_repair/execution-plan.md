# Execution Plan

Selected slice: CR-012-epic-009-playwright-evidence-is-incomplete

1. Preserve the quarantined CR-012 implementation and use the retained trusted-browser command as
   the red-capable feedback loop: the real initiation POST must not return HTTP 400.
2. Minimise at the guarded backend fixture seam by adding a regression that submits the exact action
   descriptor projected to the browser. Capture red/green output with the mandated Ralph interpreter.
3. Repair only the demonstrated browser/API handoff: ensure required server-projected form values
   are present before submission and make any non-2xx Django response report its safe envelope.
4. Run focused backend and frontend regressions, exact Playwright collection/static contract checks,
   typecheck, lint, and build. Attempt the declared spec locally only if Chromium launches; never
   fabricate screenshots or weaken the nine-state/hash contract.
5. Inspect targeted diffs and protected paths, save terminal evidence, and complete the risk
   assessment, review packet, and final summary with the review result exactly
   `Ready for independent validation`.
