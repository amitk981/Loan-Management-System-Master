# Execution Plan

Selected slice: CR-015-epic-010-terminal-servicing-owner-finalizer
Mode: same-worktree repair

1. Reproduce the exact complete-suite failure with the permanent DPD bulk-portfolio query-budget test and retain the prior failing gate output as RED evidence.
2. Preserve CR-015's account-lock-then-scope-reauthorization boundary and remove only the redundant per-account status query for the include-all-active portfolio path.
3. Run the exact focused regression with the required backend interpreter, save GREEN evidence, then run the directly affected DPD monitoring module.
4. Refresh repair artifacts, run the mandatory review-closure validator until PASS, and leave complete-suite revalidation to the orchestrator.
