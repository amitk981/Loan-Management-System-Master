# Execution Plan

Selected slice: CR-012-epic-009-playwright-evidence-is-incomplete

1. Preserve the quarantined CR-012 implementation and use the retained trusted-browser failure as
   the red-capable feedback loop. Confirm the missing sanctioned account is an actor-scope mismatch,
   not missing fixture data or a frontend pagination defect.
2. Record the cross-stack impact analysis before code edits. Add a focused guarded-fixture
   regression proving that Senior Finance can read the sanctioned account while Credit Manager
   correctly cannot read it before activation.
3. Change only the declared Epic 009 Playwright flow: use Senior Finance for the sanctioned list and
   summary, then use Credit Manager for the active summary after genuine transfer success. Preserve
   real staff logins, real Django endpoints, nine immediate assertions/captures, stale-file cleanup,
   and the nine-distinct-hash manifest.
4. Run the focused backend seed regression with the mandated venv, Playwright collection/static
   boundary checks, the impacted frontend tests, typecheck, lint, build, Django check, and targeted
   diff/protected-path review. Retain compact terminal evidence; do not fabricate screenshots if the
   sandbox cannot launch Chromium.
5. Complete risk-assessment.md, review-packet.md, and final-summary.md. Leave the review result exactly
   `Ready for independent validation` so Ralph can execute the trusted browser contract twice outside
   the sandbox.
