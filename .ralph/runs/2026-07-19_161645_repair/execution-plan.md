# Execution Plan

Selected slice: CR-012-epic-009-playwright-evidence-is-incomplete

1. Preserve the quarantined normal-run implementation and use its trusted-browser log as the
   red-capable feedback loop. Confirm that the only demonstrated failure is the sanctioned-summary
   assertion resolving two legitimate elements under Playwright strict mode.
2. Record the repair impact analysis before editing candidate code. Keep the repair limited to the
   Epic 009 Playwright spec; do not change production UI, backend behavior, API shapes, permissions,
   money/workflow rules, or the already implemented guarded E2E fixture.
3. Replace the ambiguous text assertion with the narrowest existing semantic assertion that proves
   the sanctioned account status. Re-run the exact trusted browser spec against real Django and
   require all nine screenshots plus the unique-hash manifest.
4. Run focused Playwright collection/static checks and review the candidate diff for owned-route
   fulfilment, browser-storage token injection, protected-path edits, and unrelated scope.
5. Save repair evidence and complete `risk-assessment.md`, `review-packet.md`, and
   `final-summary.md`. Set the review result exactly to `Ready for independent validation`; the
   orchestrator will rerun the complete independent gates and both trusted browser repetitions.
