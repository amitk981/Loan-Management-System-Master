# Execution Plan

Selected slice: 006Z8-portal-limit-provenance-module-and-interaction-closure

Mode: repair

1. Preserve the quarantined 006Z8 implementation and use the two independent trusted-browser
   failures as the red-capable signal: the available scenario records two identical initial
   projection GETs where the contract requires exactly one.
2. Inspect only the declared Playwright fixture and the routed MP05/container request lifecycle;
   rank and test remount, React development effects, fixture navigation, and request-listener
   duplication hypotheses one variable at a time.
3. Tighten the smallest existing regression seam so it catches the demonstrated duplicate GET,
   save failing evidence, and apply only the minimal demonstrated repair.
4. Re-run the focused regression and declared Playwright spec, saving green output and all genuine
   screenshots. Then run Playwright collection and the configured frontend/backend quality gates.
5. Verify no debug instrumentation or protected edits remain; save changed-files, risk assessment,
   review packet, final summary, and terminal evidence for this repair run.
6. Refresh Ralph state/progress/handoff and keep the already-sharpened next slice executable without
   broadening this repair or invoking any git mutation command.
