# Execution Plan

Selected slice: `010C2-manual-allocation-and-financial-reversal-controls`

## Repair boundary

Preserve the quarantined product implementation and repair only the demonstrated semantic-closure
artifact failure. Independent validation rejected the permanent-test entry because it used a Django
dotted test label instead of the required candidate-relative test path plus exact `::` selector.

## Feedback loop

1. Run the retained closure validator against the quarantined evidence to reproduce the exact
   selector-binding failure.
2. Confirm the named class and method exist in the permanent candidate test file.
3. Copy the prior run's complete finding/acceptance evidence into this repair run, changing each test
   reference to `sfpcl_credit/tests/test_repayment_adjustment_api.py::ClassName::method_name`.
4. Re-run the same closure validator and the exact permanent test selector with the mandated backend
   interpreter.

## Evidence and handoff

- Save the repair validator and exact-test output under `evidence/terminal-logs/`.
- Complete `review-closure-evidence.md`, `risk-assessment.md`, `review-packet.md`, and
  `final-summary.md` for this repair run.
- Do not edit production code, the fixed-point slice, protected files, or orchestrator-owned
  mechanical bookkeeping.
