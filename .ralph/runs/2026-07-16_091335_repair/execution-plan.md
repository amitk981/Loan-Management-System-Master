# Execution Plan

Selected slice: 008M3-documentation-workspace-executable-action-closure

1. Preserve the quarantined 008M3 implementation and use the failed trusted-browser locator for
   `Term Sheet` → `Record borrower signature` as the exact repair feedback signal.
2. Reproduce the fixture/API/UI mismatch through a deterministic non-browser seam using the real
   guarded E2E seed and current documentation-workspace projection; rank and test only hypotheses
   that explain the missing action in that seeded state.
3. Add the narrowest failing regression at the seam that owns the defect, save red evidence, and
   repair only the demonstrated fixture/projection/presentation mismatch without absorbing 008M4
   architecture or design scope.
4. Run the focused regression green, Playwright collection, and the locally available configured
   frontend/backend gates. Save green evidence and leave real Chromium/twice-run screenshot
   authority to the independent orchestrator if macOS browser services remain sandbox-blocked.
5. Refresh this repair run's changed-files, risk assessment, review packet, final summary, progress,
   state, handoff, and selected-slice status while preserving prior slice evidence and sharpening
   only the next already-open Not Started slice(s) when needed.

Constraints: edit only paths allowed by `.ralph/permissions.json`; do not change protected/source
files, commit/add/push, install packages, exceed 30 files or 2,000 changed lines, or broaden beyond
the browser failure. Use `/Users/amitkallapa/LMS/.ralph/venv/bin/python` for every backend command.
