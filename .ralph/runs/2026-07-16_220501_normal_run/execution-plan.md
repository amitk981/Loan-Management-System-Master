# Execution Plan

Selected slice: 008M6-documentation-corrected-copy-and-stage-evidence-closure

1. Reproduce the architecture-review gaps in focused backend tests: mutate each retained signed-copy
   file/owner/action/audit/workflow/version/target fact and prove the correction blocker returns;
   exercise missing/duplicate/stale/cross-scope evidence, exact and changed replay, two valid
   corrections, and multi-role stage attribution with zero-write denials.
2. Save the failing-first test output under `evidence/terminal-logs/`, then implement one
   legal-owner current-evidence decision for review actions and signed-copy successors. Make
   blocker/current projections and the existing completion/approval/readiness consumers use that
   decision without changing renderer ownership or immutable history.
3. Bind opaque correction/return/condition commands to the current item/document and approval
   stage, deriving the frozen canonical role from that stage. Reconcile singular retained
   audit/workflow/version facts before projecting blockers or conditions, while preserving exact
   replay and conflict behavior.
4. Run focused backend green tests and impacted frontend tests, then backend check and migration
   sync plus frontend typecheck, lint, test, and build. Do not run the complete backend suite; the
   orchestrator owns the authoritative coverage run. Preserve local terminal output as evidence.
5. Inspect the declared Playwright contract and run collection/non-browser feedback available in
   the sandbox. Do not fabricate screenshots; the orchestrator will execute the localhost browser
   contract twice and capture the five declared images outside the sandbox.
6. Review the diff against the slice and source digest, record a sanitized chain/ledger manifest,
   changed-file list, risk assessment, review packet, final summary, and traceability. Mark 008M6
   complete and update Ralph state/progress/handoff only after focused gates pass; sharpen the next
   one or two eligible Not Started slices using only already-opened digests/source extracts.
