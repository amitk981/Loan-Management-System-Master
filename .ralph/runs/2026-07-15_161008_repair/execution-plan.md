# Execution Plan

Selected slice: 008L3-portal-action-and-resubmission-contract-closure

1. Preserve the completed 008L3 implementation and the prior repair's navigation and visible
   login-ready selector fixes.
2. Reproduce the two strict-mode heading failures with the exact declared two-spec Playwright
   command and save the output as failing-first repair evidence.
3. Change only the two demonstrated ambiguous locators: make the MP11 `Deficiency Response`
   heading exact and scope the MP07 `My Documents` assertion to the intended page heading level.
4. Re-run the exact two-spec command; if Chromium remains sandbox-blocked, collect both specs and
   run focused frontend tests, lint, typecheck, and build while deferring the declared twice-run
   browser contract to independent validation as required.
5. Save changed-files, risk assessment, review packet, final summary, and terminal evidence; keep
   slice/state/progress/handoff truth unchanged unless the repair changes it.
