# Execution Plan

1. Reproduce Ralph's exact non-`.ralph/` changed-line calculation against the preserved 008M2 worktree and confirm the prior 2,001/2,000 failure.
2. Inspect the current diff by file and remove only the smallest non-functional line needed to bring the slice within the configured limit; do not alter the trusted-browser repair behavior or broaden scope.
3. Re-run the exact diff-limit feedback loop, `git diff --check`, and focused checks appropriate to the mechanically changed file.
4. Save self-contained repair evidence and update this run's changed-files, risk assessment, review packet, and final summary; keep slice/state/progress/handoff truth consistent with 008M2 remaining complete.
