# Execution Plan

Selected slice: CR-012-epic-009-playwright-evidence-is-incomplete

1. Preserve the quarantined CR-012 candidate and reproduce the exact prior trusted-browser failure
   from its retained log: real initiation POST/refresh succeed, but the transient banner assertion
   cannot observe the successful post-initiation state.
2. Tighten the Playwright regression at the existing real-browser seam by validating the genuine
   Django initiation response and a state-specific visible postcondition before switching actors.
3. Run the smallest deterministic feedback checks: exact Playwright collection/static contract,
   focused frontend tests, and the focused guarded Epic 009 seed/backend regression with the mandated
   interpreter. Attempt the declared browser spec locally if Chromium is available; do not fabricate
   screenshots if the sandbox denies launch.
4. Run proportional frontend gates (typecheck, lint, build), inspect targeted candidate diffs and
   protected paths, and save terminal evidence.
5. Complete risk assessment, review packet, and final summary. Set the review result exactly to
   `Ready for independent validation`; the orchestrator will run the full backend gate and the
   declared browser contract twice outside the sandbox.
