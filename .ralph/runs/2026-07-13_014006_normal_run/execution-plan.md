# Execution Plan

Selected slice: 006Z8-portal-limit-provenance-module-and-interaction-closure

1. Inspect the existing credit/member portal public seams, MP05 routed container, API client, tests,
   and declared browser spec without opening unrelated source documents.
2. TDD the backend tracer bullet: prove an unchanged stored-date authority remains current after the
   calendar advances, then move all borrower-limit decisions behind a credit-owned projection and
   cover future/closed/stale/provenance-mismatched records plus portal redaction/delegation.
3. TDD the mounted frontend tracer bullet: prove the controlled requested amount is submitted to the
   exact endpoint, success triggers one canonical GET, and 400/403/409 errors do not retry or merge.
   Extend coverage for loading/unavailable/error/conflicting server values/redaction and preserve the
   existing three-card/advisory/review compositions without styling changes.
4. Update the existing Playwright contract and screenshots only as required by the declared trusted
   browser acceptance; collect locally, recording any sandbox browser-launch limitation honestly.
5. Run focused RED/GREEN cycles, dependency/static regressions, then all configured frontend and
   backend gates with the mandated Python interpreter. Save terminal evidence and browser collection.
6. Update API/digest documentation if contracts or durable extracts change, sharpen the next one or
   two Not Started slices using already-open material, and complete Ralph state/progress/handoff,
   slice status, changed-files, risk assessment, review packet, and final summary artifacts.

Risk controls: no protected/source edits; no client-owned financial or authority calculation; no
internal identifiers/evidence/staff actions in the portal response; no new styles or dependencies.
