# Visual Evidence

The self-contained `nominee-selection-visual-evidence.html` records the existing-pattern staff
selector, portal selector, empty state, and safe detail summary. The corresponding static React
render assertions pass in `NomineeSelectionViews.test.tsx` and `ApplicationDetail.test.tsx`.

PNG screenshots could not be captured because the in-app browser runtime reported no available
browser instances. The browser skill prohibited switching to an unrelated browser backend. This
limitation does not affect the render tests, typecheck, lint, or production build.
